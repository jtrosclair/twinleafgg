"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Volcarona = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const energy_card_1 = require("../../game/store/card/energy-card");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const costs_1 = require("../../game/store/prefabs/costs");
class Volcarona extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Larvesta';
        this.cardType = R;
        this.hp = 100;
        this.weakness = [{ type: W }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Solar Birth',
                cost: [C],
                damage: 0,
                text: 'Search your deck for a Basic Pokémon and put it onto your Bench. Then, search your deck for up to 2 basic Energy cards and attach them to that Pokémon. Shuffle your deck afterward.'
            },
            {
                name: 'Flamethrower',
                cost: [R, C],
                damage: 60,
                text: 'Discard an Energy attached to this Pokémon.'
            }
        ];
        this.set = 'AOR';
        this.setNumber = '17';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Volcarona';
        this.fullName = 'Volcarona AOR';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Solar Birth
        // Refs: set-roaring-skies/ninjask.ts (bench search), set-steam-siege/xerneas.ts (energy attach from deck)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const slots = (0, prefabs_1.GET_PLAYER_BENCH_SLOTS)(player);
            if (slots.length === 0) {
                return state;
            }
            // Step 1: Search for a Basic Pokemon to bench
            const blocked = [];
            player.deck.cards.forEach((card, index) => {
                if (!(card instanceof pokemon_card_1.PokemonCard && card.stage === card_types_1.Stage.BASIC)) {
                    blocked.push(index);
                }
            });
            return store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_PUT_ONTO_BENCH, player.deck, { superType: card_types_1.SuperType.POKEMON }, { min: 0, max: 1, allowCancel: true, blocked }), (selected) => {
                const cards = selected || [];
                if (cards.length === 0) {
                    (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                    return;
                }
                const pokemonCard = cards[0];
                const targetSlot = slots[0];
                player.deck.moveCardTo(pokemonCard, targetSlot);
                targetSlot.pokemonPlayedTurn = state.turn;
                // Step 2: Search for up to 2 basic Energy cards and attach to that Pokemon
                const hasBasicEnergy = player.deck.cards.some(c => c instanceof energy_card_1.EnergyCard && c.energyType === card_types_1.EnergyType.BASIC);
                if (!hasBasicEnergy) {
                    (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                    return;
                }
                const energyBlocked = [];
                player.deck.cards.forEach((card, index) => {
                    if (!(card instanceof energy_card_1.EnergyCard && card.energyType === card_types_1.EnergyType.BASIC)) {
                        energyBlocked.push(index);
                    }
                });
                store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_ATTACH, player.deck, { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC }, { min: 0, max: 2, allowCancel: true, blocked: energyBlocked }), (energySelected) => {
                    const energyCards = energySelected || [];
                    energyCards.forEach(card => {
                        player.deck.moveCardTo(card, targetSlot);
                    });
                    (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                });
            });
        }
        // Attack 2: Flamethrower
        // Ref: set-steam-siege/xerneas.ts (Rainbow Spear)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            (0, costs_1.DISCARD_X_ENERGY_FROM_THIS_POKEMON)(store, state, effect, 1);
        }
        return state;
    }
}
exports.Volcarona = Volcarona;
