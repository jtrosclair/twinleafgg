"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Entei = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const energy_card_1 = require("../../game/store/card/energy-card");
const game_1 = require("../../game");
const attach_energy_prompt_1 = require("../../game/store/prompts/attach-energy-prompt");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const show_cards_prompt_1 = require("../../game/store/prompts/show-cards-prompt");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Entei extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = R;
        this.hp = 120;
        this.weakness = [{ type: W }];
        this.retreat = [C, C];
        this.attacks = [
            {
                name: 'Burning Roar',
                cost: [C],
                damage: 0,
                text: 'Discard the top 4 cards of your deck. If any of those cards are [R] Energy cards, attach them to your Pokémon in any way you like.'
            },
            {
                name: 'Combat Blaze',
                cost: [R, R],
                damage: 20,
                damageCalculation: '+',
                text: 'This attack does 20 more damage for each of your opponent\'s Benched Pokémon.'
            }
        ];
        this.set = 'AOR';
        this.setNumber = '14';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Entei';
        this.fullName = 'Entei AOR';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Burning Roar
        // Ref: set-emerging-powers/gigalith.ts (Shear - mill and attach matching energy)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const cardsToDiscard = player.deck.cards.slice(0, 4);
            const fireEnergies = [];
            // Show discarded cards to both players
            store.prompt(state, [
                new show_cards_prompt_1.ShowCardsPrompt(player.id, game_1.GameMessage.CARDS_SHOWED_BY_EFFECT, cardsToDiscard),
                new show_cards_prompt_1.ShowCardsPrompt(opponent.id, game_1.GameMessage.CARDS_SHOWED_BY_THE_OPPONENT, cardsToDiscard),
            ], () => { });
            cardsToDiscard.forEach(card => {
                if (card instanceof energy_card_1.EnergyCard && card.energyType === card_types_1.EnergyType.BASIC && card.provides.includes(card_types_1.CardType.FIRE)) {
                    fireEnergies.push(card);
                    player.deck.moveCardTo(card, player.hand);
                }
                else {
                    player.deck.moveCardTo(card, player.discard);
                }
            });
            if (fireEnergies.length > 0) {
                const count = fireEnergies.length;
                store.prompt(state, new attach_energy_prompt_1.AttachEnergyPrompt(player.id, game_1.GameMessage.ATTACH_ENERGY_CARDS, player.hand, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC, name: 'Fire Energy' }, { min: count, max: count, allowCancel: false }), transfers => {
                    transfers = transfers || [];
                    for (const transfer of transfers) {
                        const target = game_1.StateUtils.getTarget(state, player, transfer.to);
                        const energyCard = transfer.card;
                        const attachEffect = new play_card_effects_1.AttachEnergyEffect(player, energyCard, target);
                        store.reduceEffect(state, attachEffect);
                    }
                    // Any fire energy that wasn't attached (shouldn't happen) goes to discard
                    fireEnergies.forEach(card => {
                        if (player.hand.cards.includes(card)) {
                            player.hand.moveCardTo(card, player.discard);
                        }
                    });
                });
            }
        }
        // Attack 2: Combat Blaze
        // Ref: set-surging-sparks/iron-crown.ts (Deleting Slash - bench count)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const benchedCount = opponent.bench.reduce((count, b) => count + (b.cards.length > 0 ? 1 : 0), 0);
            effect.damage += 20 * benchedCount;
        }
        return state;
    }
}
exports.Entei = Entei;
