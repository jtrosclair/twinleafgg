"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.FlashEnergy = void 0;
const game_error_1 = require("../../game/game-error");
const game_message_1 = require("../../game/game-message");
const play_card_action_1 = require("../../game/store/actions/play-card-action");
const energy_card_1 = require("../../game/store/card/energy-card");
const card_types_1 = require("../../game/store/card/card-types");
const check_effects_1 = require("../../game/store/effects/check-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class FlashEnergy extends energy_card_1.EnergyCard {
    constructor() {
        super(...arguments);
        this.provides = [];
        this.energyType = card_types_1.EnergyType.SPECIAL;
        this.set = 'AOR';
        this.setNumber = '83';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Flash Energy';
        this.fullName = 'Flash Energy AOR';
        this.text = 'This card can only be attached to Lightning Pokémon. This card provides [L] Energy only while this card is attached to a Lightning Pokémon. The Lightning Pokémon this card is attached to has no Weakness. (If this card is attached to anything other than a Lightning Pokémon, discard this card.)';
    }
    // Ref: set-furious-fists/strong-energy.ts (type-restricted special energy pattern)
    reduceEffect(store, state, effect) {
        // Provide energy when attached to Lightning Pokemon
        if (effect instanceof check_effects_1.CheckProvidedEnergyEffect && effect.source.cards.includes(this)) {
            const checkPokemonType = new check_effects_1.CheckPokemonTypeEffect(effect.source);
            store.reduceEffect(state, checkPokemonType);
            if (checkPokemonType.cardTypes.includes(card_types_1.CardType.LIGHTNING)) {
                effect.energyMap.push({ card: this, provides: [card_types_1.CardType.LIGHTNING] });
            }
        }
        // Prevent attaching to non-Lightning Pokemon
        if (effect instanceof play_card_effects_1.AttachEnergyEffect && effect.energyCard === this) {
            const checkPokemonType = new check_effects_1.CheckPokemonTypeEffect(effect.target);
            store.reduceEffect(state, checkPokemonType);
            if (!checkPokemonType.cardTypes.includes(card_types_1.CardType.LIGHTNING)) {
                throw new game_error_1.GameError(game_message_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
        }
        // Discard card when not attached to Lightning Pokemon
        if (effect instanceof check_effects_1.CheckTableStateEffect) {
            state.players.forEach(player => {
                player.forEachPokemon(play_card_action_1.PlayerType.BOTTOM_PLAYER, cardList => {
                    if (!cardList.cards.includes(this) || (0, prefabs_1.IS_SPECIAL_ENERGY_BLOCKED)(store, state, player, this, cardList)) {
                        return;
                    }
                    const checkPokemonType = new check_effects_1.CheckPokemonTypeEffect(cardList);
                    store.reduceEffect(state, checkPokemonType);
                    if (!checkPokemonType.cardTypes.includes(card_types_1.CardType.LIGHTNING)) {
                        cardList.moveCardTo(this, player.discard);
                    }
                });
            });
        }
        // Remove weakness from attached Lightning Pokemon
        // Ref: set-primal-clash/aegislash.ts (Miracle Guard - CheckPokemonStatsEffect weakness removal)
        if (effect instanceof check_effects_1.CheckPokemonStatsEffect) {
            const target = effect.target;
            if (!target.cards.includes(this)) {
                return state;
            }
            // Find owner
            let owner = null;
            state.players.forEach(p => {
                if (p.active === target || p.bench.includes(target)) {
                    owner = p;
                }
            });
            if (!owner || (0, prefabs_1.IS_SPECIAL_ENERGY_BLOCKED)(store, state, owner, this, target)) {
                return state;
            }
            const checkPokemonType = new check_effects_1.CheckPokemonTypeEffect(target);
            store.reduceEffect(state, checkPokemonType);
            if (checkPokemonType.cardTypes.includes(card_types_1.CardType.LIGHTNING)) {
                effect.weakness = [];
            }
        }
        return state;
    }
}
exports.FlashEnergy = FlashEnergy;
