"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.ShiningJirachi = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class ShiningJirachi extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.usedStellarReign = false;
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = P;
        this.hp = 70;
        this.weakness = [{ type: P }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Stellar Reign',
                cost: [P],
                damage: 10,
                text: 'If your opponent’s Active Pokémon is an evolved Pokémon, devolve it by putting all of the Evolution cards on it into your opponent’s hand.'
            }
        ];
        this.set = 'SLG';
        this.setNumber = '42';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Shining Jirachi';
        this.fullName = 'Shining Jirachi SLG';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Stellar Reign
        // Refs: set-boundaries-crossed/golurk.ts (Devolution Punch), set-ancient-origins/porygon-z-2.ts (Digital Reboot)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            this.usedStellarReign = true;
        }
        if (effect instanceof game_phase_effects_1.AfterAttackEffect && this.usedStellarReign) {
            this.usedStellarReign = false;
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const pokemons = opponent.active.getPokemons();
            if (pokemons.length > 1) {
                // Devolve all evolution stages
                while (opponent.active.getPokemons().length > 1) {
                    const currentPokemons = opponent.active.getPokemons();
                    (0, prefabs_1.DEVOLVE_POKEMON)(store, state, opponent.active, opponent.hand);
                    const newPokemons = opponent.active.getPokemons();
                    if (newPokemons.length >= currentPokemons.length) {
                        break; // Safety: avoid infinite loops
                    }
                }
            }
        }
        if (effect instanceof game_phase_effects_1.EndTurnEffect) {
            this.usedStellarReign = false;
        }
        return state;
    }
}
exports.ShiningJirachi = ShiningJirachi;
