"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.DamageMover = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const trainer_prefabs_1 = require("../../game/store/prefabs/trainer-prefabs");
class DamageMover extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.set = 'SLG';
        this.setNumber = '58';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Damage Mover';
        this.fullName = 'Damage Mover SLG';
        this.text = 'Move 3 damage counters from 1 of your Pokémon to another of your Pokémon.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-unified-minds/jynx.ts (Ominous Posture), set-unified-minds/grimsley.ts (MoveDamagePrompt)
        if ((0, trainer_prefabs_1.WAS_TRAINER_USED)(effect, this)) {
            const player = effect.player;
            // Check that the player has at least 1 damaged Pokemon and at least 2 total Pokemon
            const damagedPokemon = [];
            let totalPokemon = 0;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card, target) => {
                totalPokemon++;
                if (cardList.damage > 0) {
                    damagedPokemon.push({ target, damage: cardList.damage });
                }
            });
            if (damagedPokemon.length === 0 || totalPokemon < 2) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            const maxAllowedDamage = [];
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card, target) => {
                const checkHpEffect = new check_effects_1.CheckHpEffect(player, cardList);
                store.reduceEffect(state, checkHpEffect);
                maxAllowedDamage.push({ target, damage: checkHpEffect.hp });
            });
            return store.prompt(state, new game_1.MoveDamagePrompt(player.id, game_1.GameMessage.MOVE_DAMAGE, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], maxAllowedDamage, { allowCancel: false, min: 3, max: 3, singleSourceTarget: true, singleDestinationTarget: true }), transfers => {
                if (transfers === null) {
                    return;
                }
                for (const transfer of transfers) {
                    const source = game_1.StateUtils.getTarget(state, player, transfer.from);
                    const target = game_1.StateUtils.getTarget(state, player, transfer.to);
                    if (source.damage >= 10) {
                        source.damage -= 10;
                        target.damage += 10;
                    }
                }
            });
        }
        return state;
    }
}
exports.DamageMover = DamageMover;
