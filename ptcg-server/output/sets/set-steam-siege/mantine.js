"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Mantine = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_effects_1 = require("../../game/store/effects/game-effects");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Mantine extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = W;
        this.hp = 100;
        this.weakness = [{ type: L }];
        this.resistance = [{ type: F, value: -20 }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Healing Wave',
                cost: [W],
                damage: 0,
                text: 'Discard as many cards as you like from your hand. Heal 10 damage from this Pokémon for each card you discarded in this way.'
            },
            {
                name: 'Dwindling Wave',
                cost: [W, W, W],
                damage: 90,
                damageCalculation: '-',
                text: 'This attack does 90 damage minus 10 damage for each damage counter on this Pokémon.'
            }
        ];
        this.set = 'STS';
        this.setNumber = '27';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Mantine';
        this.fullName = 'Mantine STS';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Healing Wave
        // Ref: set-fates-collide/umbreon-ex.ts (Veil of Darkness - discard from hand)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            if (player.hand.cards.length > 0) {
                store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DISCARD, player.hand, {}, { allowCancel: true, min: 0, max: player.hand.cards.length }), (selected) => {
                    const cards = selected || [];
                    if (cards.length > 0) {
                        player.hand.moveCardsTo(cards, player.discard);
                        const healEffect = new game_effects_1.HealEffect(player, player.active, 10 * cards.length);
                        store.reduceEffect(state, healEffect);
                    }
                });
            }
        }
        // Attack 2: Dwindling Wave
        // Ref: set-plasma-storm/beartic.ts (Powerful Rage - damage counters on self)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const damageCounters = player.active.damage / 10;
            effect.damage = Math.max(0, 90 - 10 * damageCounters);
        }
        return state;
    }
}
exports.Mantine = Mantine;
