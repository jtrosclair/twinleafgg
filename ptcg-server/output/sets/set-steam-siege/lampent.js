"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Lampent = void 0;
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const card_types_1 = require("../../game/store/card/card-types");
const state_utils_1 = require("../../game/store/state-utils");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const game_message_1 = require("../../game/game-message");
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
class Lampent extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Litwick';
        this.cardType = P;
        this.hp = 80;
        this.weakness = [{ type: D }];
        this.resistance = [{ type: F, value: -20 }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Mirror Mirror',
                cost: [C],
                damage: 0,
                text: 'Each player either draws or discards cards until that player has 4 cards in his or her hand. (Your opponent does this first.)'
            },
            {
                name: 'Flickering Flames',
                cost: [P, C],
                damage: 20,
                text: 'Your opponent\'s Active Pokémon is now Asleep.'
            }
        ];
        this.set = 'STS';
        this.setNumber = '49';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Lampent';
        this.fullName = 'Lampent STS';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Mirror Mirror
        // Ref: AGENTS-patterns.md (draw until X cards, discard pattern)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = state_utils_1.StateUtils.getOpponent(state, player);
            // Helper to adjust a player's hand to 4 cards
            const adjustHand = (p, callback) => {
                const handSize = p.hand.cards.length;
                if (handSize < 4) {
                    // Draw until 4 cards
                    (0, prefabs_1.DRAW_CARDS_UNTIL_CARDS_IN_HAND)(p, 4);
                    callback();
                }
                else if (handSize > 4) {
                    // Discard until 4 cards
                    const discardCount = handSize - 4;
                    store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(p, game_message_1.GameMessage.CHOOSE_CARD_TO_DISCARD, p.hand, {}, { min: discardCount, max: discardCount, allowCancel: false }), selected => {
                        const cards = selected || [];
                        cards.forEach(c => {
                            p.hand.moveCardTo(c, p.discard);
                        });
                        callback();
                    });
                }
                else {
                    // Already at 4
                    callback();
                }
            };
            // Opponent first, then player
            adjustHand(opponent, () => {
                adjustHand(player, () => { });
            });
        }
        // Attack 2: Flickering Flames
        // Ref: AGENTS-patterns.md (opponent's active is now Asleep)
        if ((0, prefabs_1.AFTER_ATTACK)(effect, 1, this)) {
            (0, prefabs_1.ADD_SLEEP_TO_PLAYER_ACTIVE)(store, state, effect.opponent, this);
        }
        return state;
    }
}
exports.Lampent = Lampent;
