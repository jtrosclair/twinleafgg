"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Anorith = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attack_effects_1 = require("../../game/store/prefabs/attack-effects");
class Anorith extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.RESTORED;
        this.evolvesFrom = 'Claw Fossil Anorith';
        this.cardType = F;
        this.hp = 80;
        this.weakness = [{ type: G }];
        this.retreat = [C];
        this.powers = [{
                name: 'Restored Barrier',
                powerType: game_1.PowerType.ABILITY,
                text: 'Each of your Restored Pok\u00e9mon has no Weakness.'
            }];
        this.attacks = [
            {
                name: 'X-Scissor',
                cost: [C],
                damage: 20,
                damageCalculation: '+',
                text: 'Flip a coin. If heads, this attack does 20 more damage.'
            }
        ];
        this.set = 'STS';
        this.setNumber = '56';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Anorith';
        this.fullName = 'Anorith STS';
    }
    reduceEffect(store, state, effect) {
        // Ability: Restored Barrier (passive - remove weakness from Restored Pokemon)
        // Ref: set-roaring-skies/altaria-2.ts (Clear Humming)
        if (effect instanceof check_effects_1.CheckPokemonStatsEffect) {
            let anorithOwner = null;
            state.players.forEach(p => {
                p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (cardList.getPokemonCard() === this) {
                        anorithOwner = p;
                    }
                });
            });
            if (!anorithOwner) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, anorithOwner, this)) {
                return state;
            }
            // Check if the target Pokemon belongs to the same player
            let targetOwner = null;
            state.players.forEach(p => {
                if (p.active === effect.target || p.bench.includes(effect.target)) {
                    targetOwner = p;
                }
            });
            if (targetOwner !== anorithOwner) {
                return state;
            }
            // Only remove weakness from Restored Pokemon
            const targetCard = effect.target.getPokemonCard();
            if (targetCard && targetCard.stage === card_types_1.Stage.RESTORED) {
                effect.weakness = [];
            }
        }
        // Attack 1: X-Scissor
        // Ref: set-fates-collide/carbink.ts (Stone Edge)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            (0, attack_effects_1.FLIP_A_COIN_IF_HEADS_DEAL_MORE_DAMAGE)(store, state, effect, 20);
        }
        return state;
    }
}
exports.Anorith = Anorith;
