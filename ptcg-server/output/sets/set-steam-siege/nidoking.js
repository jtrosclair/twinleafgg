"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Nidoking = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Nidoking extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_2;
        this.evolvesFrom = 'Nidorino';
        this.cardType = P;
        this.hp = 150;
        this.weakness = [{ type: P }];
        this.retreat = [C, C, C, C];
        this.powers = [{
                name: 'King\'s Palace',
                powerType: game_1.PowerType.ABILITY,
                text: 'Your Nidoqueen\'s attacks do 20 more damage to your opponent\'s Active Pokémon (before applying Weakness and Resistance).'
            }];
        this.attacks = [
            {
                name: 'Power Lariat',
                cost: [P, P, C],
                damage: 60,
                damageCalculation: '+',
                text: 'This attack does 30 more damage for each Evolution Pokémon on your Bench.'
            }
        ];
        this.set = 'STS';
        this.setNumber = '45';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Nidoking';
        this.fullName = 'Nidoking STS';
    }
    reduceEffect(store, state, effect) {
        // Ability: King's Palace (passive - boost Nidoqueen's damage)
        // Ref: set-noble-victories/hydreigon.ts (passive ability intercepting effects)
        if (effect instanceof attack_effects_1.DealDamageEffect) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Only apply if attacking opponent's active
            if (effect.target !== opponent.active) {
                return state;
            }
            // Check if the attacking Pokemon is a Nidoqueen
            const attackingCard = player.active.getPokemonCard();
            if (!attackingCard || attackingCard.name !== 'Nidoqueen') {
                return state;
            }
            // Check if this Nidoking is in play for the same player
            let nidokingInPlay = false;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card) => {
                if (card === this) {
                    nidokingInPlay = true;
                }
            });
            if (!nidokingInPlay) {
                return state;
            }
            // Check if ability is blocked
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            effect.damage += 20;
        }
        // Attack 1: Power Lariat
        // Ref: set-plasma-storm/exploud.ts (count Pokemon matching condition)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            let evolutionCount = 0;
            player.bench.forEach(benched => {
                if (benched.cards.length > 0) {
                    const pokemonCard = benched.getPokemonCard();
                    if (pokemonCard && pokemonCard.stage !== card_types_1.Stage.BASIC) {
                        evolutionCount++;
                    }
                }
            });
            effect.damage += 30 * evolutionCount;
        }
        return state;
    }
}
exports.Nidoking = Nidoking;
