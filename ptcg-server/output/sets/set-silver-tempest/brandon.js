"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Brandon = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_error_1 = require("../../game/game-error");
const game_message_1 = require("../../game/game-message");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Brandon extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.regulationMark = 'F';
        this.set = 'SIT';
        this.setNumber = '151';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Brandon';
        this.fullName = 'Brandon SIT 151';
        this.text = 'You can use this card only when it is the last card in your hand. Draw a card for each Benched Pokémon (both yours and your opponent\'s). You may play only 1 Supporter card during your turn.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-primal-clash/archies-ace-in-the-hole.ts (last card in hand check + draw cards)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Check if this is the last card in hand
            const otherCards = player.hand.cards.filter(c => c !== this);
            if (otherCards.length !== 0) {
                throw new game_error_1.GameError(game_message_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            // Count benched Pokemon for both players
            const playerBenched = player.bench.filter(b => b.cards.length > 0).length;
            const opponentBenched = opponent.bench.filter(b => b.cards.length > 0).length;
            const totalBenched = playerBenched + opponentBenched;
            if (totalBenched > 0) {
                (0, prefabs_1.DRAW_CARDS)(player, totalBenched);
            }
        }
        return state;
    }
}
exports.Brandon = Brandon;
