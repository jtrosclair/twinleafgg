"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Medicham = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const check_effects_1 = require("../../game/store/effects/check-effects");
class Medicham extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Meditite';
        this.cardType = P;
        this.hp = 120;
        this.weakness = [{ type: D }];
        this.resistance = [{ type: F, value: -30 }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'Chakra Awakening',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'If you have exactly 4 cards in your hand, this Pokémon\'s attacks cost ColorlessColorlessColorless less.'
            }];
        this.attacks = [
            {
                name: 'Vigorous Kick',
                cost: [C, C, C],
                damage: 80,
                damageCalculation: '+',
                text: 'If your opponent has any Pokémon VMAX in play, this attack does 90 more damage.'
            }
        ];
        this.regulationMark = 'F';
        this.set = 'SIT';
        this.setNumber = '73';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Medicham';
        this.fullName = 'Medicham SIT 73';
    }
    reduceEffect(store, state, effect) {
        // Ability: Chakra Awakening - reduce attack cost by CCC if exactly 4 cards in hand
        // Ref: set-fusion-strike/galarian-darmanitan.ts (CheckAttackCostEffect - conditional cost change)
        // Ref: set-unbroken-bonds/persian.ts (IS_ABILITY_BLOCKED check in CheckAttackCostEffect)
        if (effect instanceof check_effects_1.CheckAttackCostEffect) {
            const player = effect.player;
            // Check if Medicham is in play
            let isMedichamInPlay = false;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card) => {
                if (card === this) {
                    isMedichamInPlay = true;
                }
            });
            if (!isMedichamInPlay) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // Check if the attack being checked belongs to Medicham (active must be this Medicham)
            const activeCard = player.active.getPokemonCard();
            if (activeCard !== this) {
                return state;
            }
            // Reduce cost only if player has exactly 4 cards in hand
            if (player.hand.cards.length === 4) {
                // Remove up to 3 Colorless from the cost
                let removed = 0;
                effect.cost = effect.cost.filter(c => {
                    if (c === card_types_1.CardType.COLORLESS && removed < 3) {
                        removed++;
                        return false;
                    }
                    return true;
                });
            }
        }
        // Attack 1: Vigorous Kick
        // Ref: set-silver-tempest/sigilyph.ts (CardTag.POKEMON_VMAX check on opponent's Pokemon)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Check if any of opponent's Pokemon is a VMAX
            let hasVmax = false;
            opponent.forEachPokemon(game_1.PlayerType.TOP_PLAYER, (cardList, card) => {
                if (card.tags.includes(card_types_1.CardTag.POKEMON_VMAX)) {
                    hasVmax = true;
                }
            });
            if (hasVmax) {
                effect.damage += 90;
            }
        }
        return state;
    }
}
exports.Medicham = Medicham;
