"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Relicanth = void 0;
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_message_1 = require("../../game/game-message");
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const game_1 = require("../../game");
class Relicanth extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = W;
        this.hp = 90;
        this.weakness = [{ type: L }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Fossil Finding',
                cost: [C],
                damage: 0,
                text: 'Shuffle up to 4 in any combination of Unidentified Fossil and Rare Fossil cards from your discard pile into your deck.'
            },
            {
                name: 'Water Pulse',
                cost: [W, C],
                damage: 30,
                text: 'Your opponent\'s Active Pokémon is now Asleep.'
            }
        ];
        this.regulationMark = 'F';
        this.set = 'SIT';
        this.setNumber = '44';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Relicanth';
        this.fullName = 'Relicanth SIT 44';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Fossil Finding
        // Ref: set-fusion-strike/virizion.ts (choose from discard + SHUFFLE_DECK pattern)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            // Find Unidentified Fossil and Rare Fossil cards in discard
            const fossilsInDiscard = player.discard.cards.filter(c => c instanceof trainer_card_1.TrainerCard &&
                c.trainerType === game_1.TrainerType.ITEM &&
                (c.name === 'Unidentified Fossil' || c.name === 'Rare Fossil'));
            if (fossilsInDiscard.length === 0) {
                return state;
            }
            const maxPick = Math.min(4, fossilsInDiscard.length);
            // Build blocked array for non-fossil cards
            const blocked = [];
            player.discard.cards.forEach((c, index) => {
                const isFossil = c instanceof trainer_card_1.TrainerCard &&
                    c.trainerType === game_1.TrainerType.ITEM &&
                    (c.name === 'Unidentified Fossil' || c.name === 'Rare Fossil');
                if (!isFossil) {
                    blocked.push(index);
                }
            });
            return store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_message_1.GameMessage.CHOOSE_CARD_TO_DECK, player.discard, { superType: card_types_1.SuperType.TRAINER }, { min: 0, max: maxPick, allowCancel: false, blocked }), selected => {
                const cards = selected || [];
                if (cards.length > 0) {
                    cards.forEach(c => player.discard.moveCardTo(c, player.deck));
                }
                (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            });
        }
        // Attack 2: Water Pulse
        // Ref: AGENTS-patterns.md (YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_ASLEEP)
        if ((0, prefabs_1.AFTER_ATTACK)(effect, 1, this)) {
            (0, prefabs_1.ADD_SLEEP_TO_PLAYER_ACTIVE)(store, state, effect.opponent, this);
        }
        return state;
    }
}
exports.Relicanth = Relicanth;
