"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.RoboSubstituteTeamFlareGear = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_effects_1 = require("../../game/store/effects/game-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
// Ref: set-phantom-forces/robo-substitutue.ts (Robo Substitute - existing implementation)
class RoboSubstituteTeamFlareGear extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.superType = card_types_1.SuperType.TRAINER;
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = card_types_1.CardType.COLORLESS;
        this.pokemonType = card_types_1.PokemonType.NORMAL;
        this.evolvesFrom = '';
        this.cardTag = [];
        this.tools = [];
        this.evolvesTo = [];
        this.evolvesToStage = [];
        this.archetype = [];
        this.hp = 30;
        this.weakness = [];
        this.retreat = [];
        this.resistance = [];
        this.attacks = [];
        this.attacksThisTurn = 0;
        this.maxAttacksThisTurn = 1;
        this.allowSubsequentAttackChoice = false;
        this.maxTools = 1;
        this.movedToActiveThisTurn = false;
        this.evolvesFromBase = [];
        this.set = 'PHF';
        this.setNumber = '102';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Robo Substitute';
        this.fullName = 'Robo Substitute Team Flare Gear PHF';
        this.text = 'Play this card as if it were a 30 HP Colorless Basic Pokémon. At any time during your turn (before your attack), you may discard this card from play. This card can\'t retreat. If this card is Knocked Out, your opponent can\'t take any Prize Cards for it.';
        this.powers = [
            {
                name: 'Robo Substitute',
                text: 'Play this card as if it were a 30 HP [C] Basic Pokémon. At any time during your turn (before your attack), you may discard this card from play. This card can\'t retreat. If this card is Knocked Out, your opponent can\'t take any Prize cards for it.',
                useWhenInPlay: true,
                exemptFromAbilityLock: true,
                powerType: game_1.PowerType.TRAINER_ABILITY
            }
        ];
    }
    reduceEffect(store, state, effect) {
        // Ability: Discard from play when activated
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const cardList = game_1.StateUtils.findCardList(state, this);
            const player = effect.player;
            if (player.bench.every(b => b.cards.length === 0)) {
                // If no bench Pokemon, move cards to deck (game will end)
                effect.player.active.moveCardsTo(effect.player.active.cards, player.deck);
            }
            else {
                player.switchPokemon(cardList);
                const pokeDollCardList = game_1.StateUtils.findCardList(state, this);
                pokeDollCardList.moveCardsTo(pokeDollCardList.cards, effect.player.discard);
            }
        }
        // Play from hand as a Pokemon
        if (effect instanceof play_card_effects_1.PlayItemEffect && effect.trainerCard === this) {
            const player = effect.player;
            const emptySlots = player.bench.filter(b => b.cards.length === 0);
            if (emptySlots.length === 0) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            const playPokemonEffect = new play_card_effects_1.PlayPokemonEffect(player, this, emptySlots[0]);
            store.reduceEffect(state, playPokemonEffect);
        }
        // Cannot retreat
        if (effect instanceof game_effects_1.RetreatEffect && effect.player.active.cards.includes(this)) {
            throw new game_1.GameError(game_1.GameMessage.CANNOT_RETREAT);
        }
        // No prize cards on KO
        if (effect instanceof game_effects_1.KnockOutEffect && effect.target.cards.includes(this)) {
            effect.prizeCount = 0;
            return state;
        }
        return state;
    }
}
exports.RoboSubstituteTeamFlareGear = RoboSubstituteTeamFlareGear;
