"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.TargetWhistleTeamFlareGear = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
// Ref: set-phantom-forces/target-whistle.ts (Target Whistle), set-chilling-reign/echoing-horn.ts (Echoing Horn)
function* playCard(next, store, state, effect) {
    const player = effect.player;
    const opponent = game_1.StateUtils.getOpponent(state, player);
    const slots = opponent.bench.filter(b => b.cards.length === 0);
    // Check if opponent's discard has Basic Pokemon
    const hasBasicInDiscard = opponent.discard.cards.some(c => c.superType === card_types_1.SuperType.POKEMON && c.stage === card_types_1.Stage.BASIC);
    if (!hasBasicInDiscard) {
        throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
    }
    // Check if bench has open slots
    if (slots.length === 0) {
        throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
    }
    effect.preventDefault = true;
    let cards = [];
    yield store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_HAND, opponent.discard, { superType: card_types_1.SuperType.POKEMON, stage: card_types_1.Stage.BASIC }, { min: 1, max: 1, allowCancel: false }), selected => {
        cards = selected || [];
        next();
    });
    if (cards.length > 0) {
        opponent.discard.moveCardTo(cards[0], slots[0]);
        slots[0].pokemonPlayedTurn = state.turn;
    }
}
class TargetWhistleTeamFlareGear extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.set = 'PHF';
        this.setNumber = '106';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Target Whistle';
        this.fullName = 'Target Whistle Team Flare Gear PHF';
        this.text = 'Put a Basic Pokémon from your opponent\'s discard pile onto his or her Bench.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-phantom-forces/target-whistle.ts (Target Whistle)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const generator = playCard(() => generator.next(), store, state, effect);
            return generator.next().value;
        }
        return state;
    }
}
exports.TargetWhistleTeamFlareGear = TargetWhistleTeamFlareGear;
