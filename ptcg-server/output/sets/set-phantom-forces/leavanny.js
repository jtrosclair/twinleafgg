"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Leavanny = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const trainer_card_1 = require("../../game/store/card/trainer-card");
const game_effects_1 = require("../../game/store/effects/game-effects");
const check_effects_1 = require("../../game/store/effects/check-effects");
const choose_pokemon_prompt_1 = require("../../game/store/prompts/choose-pokemon-prompt");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Leavanny extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_2;
        this.evolvesFrom = 'Swadloon';
        this.cardType = G;
        this.hp = 130;
        this.weakness = [{ type: R }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Coordinate',
                cost: [G],
                damage: 0,
                text: 'Choose up to 2 of your Benched Pokémon that have no Pokémon Tools attached to them. For each of those Pokémon, search your deck for a Pokémon Tool card, and attach it to that Pokémon. Shuffle your deck afterward.'
            },
            {
                name: 'Leaf Storm',
                cost: [G, G, C],
                damage: 70,
                text: 'Heal 20 damage from each of your Grass Pokémon.'
            }
        ];
        this.set = 'PHF';
        this.setNumber = '7';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Leavanny';
        this.fullName = 'Leavanny PHF';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Coordinate
        // Ref: Custom implementation - search tools for benched Pokemon without tools
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            // Find benched Pokemon without tools
            const benchWithoutTools = player.bench.filter(b => b.cards.length > 0 && b.tools.length === 0);
            if (benchWithoutTools.length === 0 || player.deck.cards.length === 0) {
                return state;
            }
            const hasToolsInDeck = player.deck.cards.some(c => c instanceof trainer_card_1.TrainerCard && c.trainerType === card_types_1.TrainerType.TOOL);
            if (!hasToolsInDeck) {
                (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                return state;
            }
            // Choose up to 2 benched Pokemon without tools
            return store.prompt(state, new choose_pokemon_prompt_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.BENCH], { min: 0, max: Math.min(2, benchWithoutTools.length), allowCancel: true }), targets => {
                if (!targets || targets.length === 0) {
                    (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                    return;
                }
                // Filter to only those without tools
                const validTargets = targets.filter(t => t.tools.length === 0);
                // For each target, search deck for a tool and attach it
                const attachToolToNextTarget = (index) => {
                    if (index >= validTargets.length) {
                        (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                        return;
                    }
                    const target = validTargets[index];
                    let cards = [];
                    store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_HAND, player.deck, { superType: card_types_1.SuperType.TRAINER, trainerType: card_types_1.TrainerType.TOOL }, { min: 0, max: 1, allowCancel: true }), selected => {
                        cards = selected || [];
                        if (cards.length > 0) {
                            const tool = cards[0];
                            player.deck.moveCardTo(tool, target);
                            // moveCardTo puts the card in target.cards; move it to tools instead
                            const idx = target.cards.indexOf(tool);
                            if (idx !== -1) {
                                target.cards.splice(idx, 1);
                            }
                            target.tools.push(tool);
                        }
                        attachToolToNextTarget(index + 1);
                    });
                };
                attachToolToNextTarget(0);
            });
        }
        // Attack 2: Leaf Storm
        // Ref: set-plasma-freeze/sceptile.ts (Energy Bloom)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                const checkType = new check_effects_1.CheckPokemonTypeEffect(cardList);
                store.reduceEffect(state, checkType);
                if (checkType.cardTypes.includes(card_types_1.CardType.GRASS) && cardList.damage > 0) {
                    const healEffect = new game_effects_1.HealEffect(player, cardList, 20);
                    store.reduceEffect(state, healEffect);
                }
            });
        }
        return state;
    }
}
exports.Leavanny = Leavanny;
