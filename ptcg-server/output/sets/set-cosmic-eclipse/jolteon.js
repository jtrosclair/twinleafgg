"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Jolteon = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Jolteon extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Eevee';
        this.cardType = L;
        this.hp = 100;
        this.weakness = [{ type: F }];
        this.resistance = [{ type: M, value: -20 }];
        this.retreat = [C];
        this.powers = [{
                name: 'Speed Cheer',
                powerType: game_1.PowerType.ABILITY,
                text: 'The attacks of your Pokémon-GX in play that evolve from Eevee cost Colorless less. You can\'t apply more than 1 Speed Cheer Ability at a time.'
            }];
        this.attacks = [
            {
                name: 'Head Bolt',
                cost: [L, C],
                damage: 70,
                text: ''
            }
        ];
        this.set = 'CEC';
        this.setNumber = '70';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Jolteon';
        this.fullName = 'Jolteon CEC';
    }
    reduceEffect(store, state, effect) {
        // Ability: Speed Cheer (passive - reduce attack cost for Eevee GX)
        // Ref: set-cosmic-eclipse/sunflora.ts (Solar Power - CheckAttackCostEffect)
        if (effect instanceof check_effects_1.CheckAttackCostEffect) {
            const player = effect.player;
            // Check if this Jolteon is in play on the same side
            let isInPlay = false;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                if (cardList.getPokemonCard() === this) {
                    isInPlay = true;
                }
            });
            if (!isInPlay) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // "Can't apply more than 1 Speed Cheer Ability at a time"
            let firstJolteon;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                const pokemonCard = cardList.getPokemonCard();
                if (pokemonCard && pokemonCard instanceof Jolteon
                    && pokemonCard.powers.length > 0 && pokemonCard.powers[0].name === 'Speed Cheer'
                    && !firstJolteon) {
                    firstJolteon = pokemonCard;
                }
            });
            if (firstJolteon !== this) {
                return state;
            }
            // Check if the active Pokemon is a GX that evolves from Eevee
            const activePokemon = player.active.getPokemonCard();
            if (activePokemon && activePokemon.tags.includes(card_types_1.CardTag.POKEMON_GX)
                && activePokemon.evolvesFrom === 'Eevee') {
                // Remove 1 Colorless from the attack cost
                const index = effect.cost.indexOf(card_types_1.CardType.COLORLESS);
                if (index !== -1) {
                    effect.cost.splice(index, 1);
                }
            }
        }
        return state;
    }
}
exports.Jolteon = Jolteon;
