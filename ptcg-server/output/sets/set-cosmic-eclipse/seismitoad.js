"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Seismitoad = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
function* useBulldoze(next, store, state, self, effect) {
    const player = effect.player;
    let cards = [];
    if (player.deck.cards.length === 0) {
        throw new game_1.GameError(game_1.GameMessage.CANNOT_USE_POWER);
    }
    const deckTop = new game_1.CardList();
    yield store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARDS_TO_PUT_ON_TOP_OF_THE_DECK, player.deck, {}, { min: 1, max: 1, allowCancel: false }), selected => {
        cards = selected || [];
        next();
    });
    player.deck.moveCardsTo(cards, deckTop);
    player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, cardList => {
        if (cardList.getPokemonCard() === self) {
            cardList.addBoardEffect(card_types_1.BoardEffect.ABILITY_USED);
        }
    });
    return store.prompt(state, new game_1.ShuffleDeckPrompt(player.id), order => {
        player.deck.applyOrder(order);
        if (order === null) {
            return state;
        }
        deckTop.applyOrder(order);
        deckTop.moveToTopOfDestination(player.deck);
    });
}
class Seismitoad extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_2;
        this.evolvesFrom = 'Palpitoad';
        this.cardType = F;
        this.hp = 160;
        this.weakness = [{ type: G }];
        this.retreat = [C, C, C];
        this.powers = [{
                name: 'Bulldoze',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'Once during your turn (before your attack), you may search your deck for a card, shuffle your deck, then put that card on top of it.'
            }];
        this.attacks = [
            {
                name: 'Tremulous Fist',
                cost: [F, C, C, C],
                damage: 80,
                damageCalculation: '+',
                text: 'This attack does 30 more damage for each of your Benched Pokémon that has any damage counters on it.'
            }
        ];
        this.set = 'CEC';
        this.setNumber = '117';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Seismitoad';
        this.fullName = 'Seismitoad CEC';
    }
    reduceEffect(store, state, effect) {
        // Ability: Bulldoze
        // Ref: set-celestial-storm/magcargo.ts (Smooth Over - search deck, shuffle, put on top)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            const generator = useBulldoze(() => generator.next(), store, state, this, effect);
            return generator.next().value;
        }
        // Attack 1: Tremulous Fist
        // Ref: set-cosmic-eclipse/kricketune.ts (damage based on bench condition)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            let damagedBenchCount = 0;
            player.bench.forEach(b => {
                if (b.cards.length > 0 && b.damage > 0) {
                    damagedBenchCount++;
                }
            });
            effect.damage += damagedBenchCount * 30;
        }
        return state;
    }
}
exports.Seismitoad = Seismitoad;
