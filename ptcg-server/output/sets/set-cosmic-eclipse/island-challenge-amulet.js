"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.IslandChallengeAmulet = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const game_effects_1 = require("../../game/store/effects/game-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
class IslandChallengeAmulet extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.TOOL;
        this.set = 'CEC';
        this.setNumber = '194';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Island Challenge Amulet';
        this.fullName = 'Island Challenge Amulet CEC';
        this.text = 'The Pokémon-GX or Pokémon-EX this card is attached to gets -100 HP, and when it is Knocked Out by damage from an opponent\'s attack, that player takes 1 fewer Prize card.';
    }
    reduceEffect(store, state, effect) {
        // HP reduction for GX/EX Pokemon
        // Ref: set-team-up/buff-padding.ts (CheckHpEffect tool pattern)
        if (effect instanceof check_effects_1.CheckHpEffect && effect.target.tools.includes(this)) {
            const sourceCard = effect.target.getPokemonCard();
            // Try to reduce ToolEffect, to check if something is blocking the tool from working
            try {
                const toolEffect = new play_card_effects_1.ToolEffect(effect.player, this);
                store.reduceEffect(state, toolEffect);
            }
            catch (_a) {
                return state;
            }
            if (!sourceCard) {
                return state;
            }
            // Only applies to GX or EX Pokemon
            if (!sourceCard.tags.includes(card_types_1.CardTag.POKEMON_GX) && !sourceCard.tags.includes(card_types_1.CardTag.POKEMON_EX)) {
                return state;
            }
            effect.hp -= 100;
        }
        // Prize reduction when KO'd by opponent's attack
        // Ref: set-twilight-masquerade/legacy-energy.ts (KnockOutEffect prize reduction)
        if (effect instanceof game_effects_1.KnockOutEffect && effect.target.tools.includes(this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const sourceCard = effect.target.getPokemonCard();
            if (state.phase !== game_1.GamePhase.ATTACK || state.players[state.activePlayer] !== opponent) {
                return state;
            }
            if (!sourceCard) {
                return state;
            }
            // Only applies to GX or EX Pokemon
            if (!sourceCard.tags.includes(card_types_1.CardTag.POKEMON_GX) && !sourceCard.tags.includes(card_types_1.CardTag.POKEMON_EX)) {
                return state;
            }
            // Try to reduce ToolEffect, to check if something is blocking the tool from working
            try {
                const toolEffect = new play_card_effects_1.ToolEffect(player, this);
                store.reduceEffect(state, toolEffect);
            }
            catch (_b) {
                return state;
            }
            effect.prizeCount -= 1;
        }
        return state;
    }
}
exports.IslandChallengeAmulet = IslandChallengeAmulet;
