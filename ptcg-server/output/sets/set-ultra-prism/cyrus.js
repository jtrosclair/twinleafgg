"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Cyrus = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const choose_pokemon_prompt_1 = require("../../game/store/prompts/choose-pokemon-prompt");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const trainer_prefabs_1 = require("../../game/store/prefabs/trainer-prefabs");
class Cyrus extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.tags = [card_types_1.CardTag.PRISM_STAR];
        this.set = 'UPR';
        this.setNumber = '120';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Cyrus \u25C7';
        this.fullName = 'Cyrus \u25C7 UPR';
        this.text = 'You can play this card only if your Active Pokémon is a Water or Metal Pokémon. Your opponent chooses 2 Benched Pokémon and shuffles the others, and all cards attached to them, into their deck. You may play only 1 Supporter card during your turn (before your attack). \u25C7 (Prism Star) Rule: You can\'t have more than 1 \u25C7 card with the same name in your deck. If a \u25C7 card would go to the discard pile, put it in the Lost Zone instead.';
    }
    // Ref: set-ultra-prism/cyrus-prism-star.ts (Cyrus Prism Star)
    reduceEffect(store, state, effect) {
        if ((0, trainer_prefabs_1.WAS_TRAINER_USED)(effect, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Check if active Pokemon is Water or Metal
            const checkType = new check_effects_1.CheckPokemonTypeEffect(player.active);
            store.reduceEffect(state, checkType);
            if (!checkType.cardTypes.includes(card_types_1.CardType.WATER) && !checkType.cardTypes.includes(card_types_1.CardType.METAL)) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            player.hand.moveCardTo(effect.trainerCard, player.supporter);
            effect.preventDefault = true;
            const opponentBenched = opponent.bench.filter(c => c.cards.length > 0);
            if (opponentBenched.length > 2) {
                store.prompt(state, new choose_pokemon_prompt_1.ChoosePokemonPrompt(opponent.id, game_1.GameMessage.CHOOSE_CARDS, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.BENCH], { allowCancel: false, min: Math.min(opponentBenched.length, 2), max: 2 }), targets => {
                    if (!targets || targets.length === 0) {
                        return;
                    }
                    opponent.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, card => {
                        if (card !== opponent.active && !targets.includes(card)) {
                            card.clearEffects();
                            (0, prefabs_1.MOVE_CARDS)(store, state, card, opponent.deck);
                        }
                    });
                    (0, prefabs_1.SHUFFLE_DECK)(store, state, opponent);
                });
            }
            else {
                // 2 or fewer benched Pokemon: nothing to shuffle away
            }
            return state;
        }
        return state;
    }
}
exports.Cyrus = Cyrus;
