"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Shaymin = void 0;
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const card_types_1 = require("../../game/store/card/card-types");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const game_message_1 = require("../../game/game-message");
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
class Shaymin extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = G;
        this.hp = 80;
        this.weakness = [{ type: R }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Coax',
                cost: [C],
                damage: 0,
                text: 'Search your deck for up to 3 Basic Pokémon of different types and put them onto your Bench. Then, shuffle your deck.'
            },
            {
                name: 'Soothing Scent',
                cost: [G, C],
                damage: 30,
                text: 'Your opponent\'s Active Pokémon is now Asleep.'
            }
        ];
        this.set = 'UPR';
        this.setNumber = '15';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Shaymin';
        this.fullName = 'Shaymin UPR';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Coax
        // Refs: set-breakpoint/dunsparce.ts (Call for Family), set-surging-sparks/energy-search-pro.ts (differentTypes)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const emptyBenchSlots = player.bench.filter(b => b.cards.length === 0).length;
            if (emptyBenchSlots === 0 || player.deck.cards.length === 0) {
                return state;
            }
            const maxPokemon = Math.min(3, emptyBenchSlots);
            return store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_message_1.GameMessage.CHOOSE_CARD_TO_PUT_ONTO_BENCH, player.deck, { superType: card_types_1.SuperType.POKEMON, stage: card_types_1.Stage.BASIC }, { min: 0, max: maxPokemon, allowCancel: false, differentTypes: true }), selected => {
                const cards = selected || [];
                cards.forEach(card => {
                    const emptySlot = player.bench.find(b => b.cards.length === 0);
                    if (emptySlot) {
                        player.deck.moveCardTo(card, emptySlot);
                        emptySlot.pokemonPlayedTurn = state.turn;
                    }
                });
                return (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            });
        }
        // Attack 2: Soothing Scent
        // Ref: AGENTS-patterns.md (special conditions)
        if ((0, prefabs_1.AFTER_ATTACK)(effect, 1, this)) {
            (0, prefabs_1.ADD_SLEEP_TO_PLAYER_ACTIVE)(store, state, effect.opponent, this);
        }
        return state;
    }
}
exports.Shaymin = Shaymin;
