"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.AncientCrystal = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class AncientCrystal extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.TOOL;
        this.set = 'UPR';
        this.setNumber = '118';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Ancient Crystal';
        this.fullName = 'Ancient Crystal UPR';
        this.text = 'The Regirock, Regice, Registeel, or Regigigas this card is attached to takes 30 less damage from your opponent\'s attacks (after applying Weakness and Resistance).';
    }
    // Ref: set-x-and-y/hard-charm.ts (Tool damage reduction pattern)
    reduceEffect(store, state, effect) {
        if (effect instanceof attack_effects_1.DealDamageEffect && effect.target.tools.includes(this)) {
            const player = game_1.StateUtils.findOwner(state, effect.target);
            if ((0, prefabs_1.IS_TOOL_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // Check if attached to Regirock, Regice, Registeel, or Regigigas
            const pokemonCard = effect.target.getPokemonCard();
            if (!pokemonCard) {
                return state;
            }
            const validNames = ['Regirock', 'Regice', 'Registeel', 'Regigigas'];
            if (!validNames.some(name => pokemonCard.name.startsWith(name))) {
                return state;
            }
            // Only reduce damage from opponent's attacks
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if (effect.source === opponent.active || opponent.bench.includes(effect.source)) {
                effect.damage = Math.max(0, effect.damage - 30);
            }
        }
        return state;
    }
}
exports.AncientCrystal = AncientCrystal;
