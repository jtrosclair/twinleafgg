"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Xerneas = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const choose_pokemon_prompt_1 = require("../../game/store/prompts/choose-pokemon-prompt");
class Xerneas extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = P;
        this.hp = 120;
        this.weakness = [{ type: M }];
        this.retreat = [C, C];
        this.attacks = [
            {
                name: 'Breath of Life',
                cost: [C, C],
                damage: 0,
                text: 'Search your deck for up to 3 basic Energy cards of different types and attach them to your Pokémon in any way you like. Then, shuffle your deck.'
            },
            {
                name: 'Aurora Horns',
                cost: [P, C, C],
                damage: 100,
                text: ''
            }
        ];
        this.regulationMark = 'E';
        this.set = 'CEL';
        this.setNumber = '12';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Xerneas';
        this.fullName = 'Xerneas CEL';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Breath of Life
        // Ref: set-dragons-exalted/beautifly.ts (Triple Energy)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            if (player.deck.cards.length === 0) {
                return state;
            }
            // Filter basic energy in deck
            const basicEnergyInDeck = player.deck.cards.filter(c => c instanceof game_1.EnergyCard && c.energyType === card_types_1.EnergyType.BASIC);
            if (basicEnergyInDeck.length === 0) {
                return (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            }
            // Get unique types available
            const typesAvailable = new Set();
            basicEnergyInDeck.forEach(c => {
                if (c instanceof game_1.EnergyCard) {
                    c.provides.forEach(t => typesAvailable.add(t));
                }
            });
            const maxCards = Math.min(3, typesAvailable.size);
            // Block non-basic energy
            const blocked = [];
            player.deck.cards.forEach((card, index) => {
                if (!(card instanceof game_1.EnergyCard) || card.energyType !== card_types_1.EnergyType.BASIC) {
                    blocked.push(index);
                }
            });
            return store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_ATTACH, player.deck, { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC }, { min: 0, max: maxCards, allowCancel: false, blocked, differentTypes: true }), selectedCards => {
                const cards = selectedCards || [];
                if (cards.length === 0) {
                    return (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                }
                // Attach each energy to a Pokemon of player's choice
                const attachNext = (index) => {
                    if (index >= cards.length) {
                        (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                        return;
                    }
                    const card = cards[index];
                    store.prompt(state, new choose_pokemon_prompt_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON_TO_ATTACH_CARDS, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { min: 1, max: 1, allowCancel: false }), targets => {
                        if (targets && targets.length > 0) {
                            player.deck.moveCardTo(card, targets[0]);
                        }
                        attachNext(index + 1);
                    });
                };
                attachNext(0);
            });
        }
        return state;
    }
}
exports.Xerneas = Xerneas;
