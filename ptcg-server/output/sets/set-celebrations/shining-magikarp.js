"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.ShiningMagikarp = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class ShiningMagikarp extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = W;
        this.hp = 30;
        this.weakness = [{ type: L }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Gold Scale',
                cost: [W],
                damage: 0,
                text: 'Your opponent may draw 2 cards. Either way, you may draw 2 cards.'
            },
            {
                name: 'Dragon Bond',
                cost: [P],
                damage: 0,
                text: 'Search your deck for a card named Gyarados, Dark Gyarados, or Shining Gyarados. Show it to your opponent and put it into your hand. Shuffle your deck afterward.'
            }
        ];
        this.set = 'CEL';
        this.setNumber = '66A';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Shining Magikarp';
        this.fullName = 'Shining Magikarp CEL';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Gold Scale
        // Ref: set-call-of-legends/cheerleaders-cheer.ts (opponent may draw), set-secret-wonders/mew.ts (you may draw)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Opponent may draw 2 cards
            if (opponent.deck.cards.length > 0) {
                (0, prefabs_1.CONFIRMATION_PROMPT)(store, state, opponent, result => {
                    if (result) {
                        (0, prefabs_1.DRAW_CARDS)(opponent, 2);
                    }
                }, game_1.GameMessage.WANT_TO_DRAW_CARDS);
            }
            // You may draw 2 cards
            if (player.deck.cards.length > 0) {
                (0, prefabs_1.CONFIRMATION_PROMPT)(store, state, player, result => {
                    if (result) {
                        (0, prefabs_1.DRAW_CARDS)(player, 2);
                    }
                }, game_1.GameMessage.WANT_TO_DRAW_CARDS);
            }
        }
        // Attack 2: Dragon Bond
        // Refs: set-plasma-blast/axew.ts (search deck by name), set-guardians-rising/tapu-lele-gx.ts (show cards to opponent)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if (player.deck.cards.length === 0) {
                return state;
            }
            const GYARADOS_NAMES = ['Gyarados', 'Dark Gyarados', 'Shining Gyarados'];
            // Block cards that don't match the allowed names
            const blocked = [];
            player.deck.cards.forEach((c, index) => {
                if (!GYARADOS_NAMES.includes(c.name)) {
                    blocked.push(index);
                }
            });
            return store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_HAND, player.deck, { superType: card_types_1.SuperType.POKEMON }, { min: 0, max: 1, allowCancel: false, blocked }), selected => {
                const cards = selected || [];
                if (cards.length > 0) {
                    store.prompt(state, [new game_1.ShowCardsPrompt(opponent.id, game_1.GameMessage.CARDS_SHOWED_BY_THE_OPPONENT, cards)], () => {
                        cards.forEach(card => {
                            store.log(state, game_1.GameLog.LOG_PLAYER_PUTS_CARD_IN_HAND, { name: player.name, card: card.name });
                        });
                    });
                    (0, prefabs_1.MOVE_CARDS)(store, state, player.deck, player.hand, { cards, sourceCard: this });
                }
                return (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            });
        }
        return state;
    }
}
exports.ShiningMagikarp = ShiningMagikarp;
