"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Klefki = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const trainer_card_1 = require("../../game/store/card/trainer-card");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attack_effects_1 = require("../../game/store/prefabs/attack-effects");
const state_utils_1 = require("../../game/store/state-utils");
const show_cards_prompt_1 = require("../../game/store/prompts/show-cards-prompt");
class Klefki extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = Y;
        this.hp = 60;
        this.weakness = [{ type: M }];
        this.resistance = [{ type: D, value: -20 }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Look for Keys',
                cost: [Y],
                damage: 0,
                text: 'Reveal cards from the top of your deck until you reveal an Item card. Put it into your hand. Shuffle the other cards back into your deck.'
            },
            {
                name: 'Play Rough',
                cost: [Y, C],
                damage: 20,
                damageCalculation: '+',
                text: 'Flip a coin. If heads, this attack does 20 more damage.'
            }
        ];
        this.set = 'ROS';
        this.setNumber = '48';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Klefki';
        this.fullName = 'Klefki ROS';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Look for Keys
        // Ref: set-dark-explorers/random-receiver.ts (reveal from top until finding specific card type)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = state_utils_1.StateUtils.getOpponent(state, player);
            if (player.deck.cards.length === 0) {
                return state;
            }
            const cards = [];
            let itemCard;
            for (let i = 0; i < player.deck.cards.length; i++) {
                const card = player.deck.cards[i];
                cards.push(card);
                if (card instanceof trainer_card_1.TrainerCard && card.trainerType === card_types_1.TrainerType.ITEM) {
                    itemCard = card;
                    break;
                }
            }
            // Show revealed cards to both players
            store.prompt(state, [
                new show_cards_prompt_1.ShowCardsPrompt(player.id, game_1.GameMessage.CARDS_SHOWED_BY_EFFECT, cards),
                new show_cards_prompt_1.ShowCardsPrompt(opponent.id, game_1.GameMessage.CARDS_SHOWED_BY_THE_OPPONENT, cards)
            ], () => { });
            if (itemCard !== undefined) {
                (0, prefabs_1.MOVE_CARDS)(store, state, player.deck, player.hand, { cards: [itemCard] });
            }
            return (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
        }
        // Attack 2: Play Rough
        // Ref: set-primal-clash/solrock.ts (Knock Away)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            (0, attack_effects_1.FLIP_A_COIN_IF_HEADS_DEAL_MORE_DAMAGE)(store, state, effect, 20);
        }
        return state;
    }
}
exports.Klefki = Klefki;
