"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Unfezant = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attack_effects_1 = require("../../game/store/prefabs/attack-effects");
class Unfezant extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_2;
        this.evolvesFrom = 'Tranquill';
        this.cardType = C;
        this.hp = 130;
        this.weakness = [{ type: L }];
        this.resistance = [{ type: F, value: -20 }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Quick Attack',
                cost: [C],
                damage: 30,
                damageCalculation: '+',
                text: 'Flip a coin. If heads, this attack does 30 more damage.'
            },
            {
                name: 'Strong Winds',
                cost: [C, C, C],
                damage: 0,
                text: 'Shuffle all cards attached to each player\'s Pok\u00e9mon into that player\'s deck.'
            }
        ];
        this.set = 'ROS';
        this.setNumber = '80';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Unfezant';
        this.fullName = 'Unfezant ROS';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Quick Attack
        // Ref: set-primal-clash/rhyperior.ts (Stone Edge)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            (0, attack_effects_1.FLIP_A_COIN_IF_HEADS_DEAL_MORE_DAMAGE)(store, state, effect, 30);
        }
        // Attack 2: Strong Winds
        // Ref: AGENTS-patterns.md (shuffle attached cards into deck)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Shuffle all attached cards (not the Pokemon themselves) from player's Pokemon into player's deck
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                const pokemons = cardList.getPokemons();
                const cardsToShuffle = cardList.cards.filter(c => !pokemons.includes(c));
                cardsToShuffle.forEach(c => {
                    cardList.moveCardTo(c, player.deck);
                });
                // Also move tools
                const toolsToShuffle = cardList.tools.slice();
                toolsToShuffle.forEach(t => {
                    cardList.moveCardTo(t, player.deck);
                });
            });
            // Shuffle all attached cards from opponent's Pokemon into opponent's deck
            opponent.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                const pokemons = cardList.getPokemons();
                const cardsToShuffle = cardList.cards.filter(c => !pokemons.includes(c));
                cardsToShuffle.forEach(c => {
                    cardList.moveCardTo(c, opponent.deck);
                });
                // Also move tools
                const toolsToShuffle = cardList.tools.slice();
                toolsToShuffle.forEach(t => {
                    cardList.moveCardTo(t, opponent.deck);
                });
            });
            (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            (0, prefabs_1.SHUFFLE_DECK)(store, state, opponent);
        }
        return state;
    }
}
exports.Unfezant = Unfezant;
