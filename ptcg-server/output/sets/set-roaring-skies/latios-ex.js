"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.LatiosEx = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class LatiosEx extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_EX];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = N;
        this.hp = 170;
        this.weakness = [{ type: Y }];
        this.retreat = [C, C];
        this.PREVENT_EFFECTS_MARKER = 'LATIOS_EX_ROS_PREVENT_EFFECTS_MARKER';
        this.CLEAR_PREVENT_EFFECTS_MARKER = 'LATIOS_EX_ROS_CLEAR_PREVENT_EFFECTS_MARKER';
        this.attacks = [
            {
                name: 'Fast Raid',
                cost: [P],
                damage: 40,
                canUseOnFirstTurn: true,
                text: 'If you go first, you can use this attack on your first turn.'
            },
            {
                name: 'Light Pulse',
                cost: [W, P, P, C],
                damage: 110,
                text: 'Prevent all effects of your opponent\'s attacks, except damage, done to this Pok\u00e9mon during your opponent\'s next turn.'
            }
        ];
        this.set = 'ROS';
        this.setNumber = '58';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Latios-EX';
        this.fullName = 'Latios-EX ROS';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Fast Raid
        // Ref: set-lost-thunder/sableye.ts (Quick Hunt - canUseOnFirstTurn flag)
        // No additional logic needed, the canUseOnFirstTurn flag handles this.
        // Attack 2: Light Pulse
        // Ref: set-steam-siege/magearna-ex.ts (Mystic Heart - prevent effects except damage)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            player.active.marker.addMarker(this.PREVENT_EFFECTS_MARKER, this);
            opponent.marker.addMarker(this.CLEAR_PREVENT_EFFECTS_MARKER, this);
        }
        // Prevent effects of attacks (except damage) done to this Pokemon
        if (effect instanceof attack_effects_1.AbstractAttackEffect
            && effect.target.cards.includes(this)
            && effect.target.marker.hasMarker(this.PREVENT_EFFECTS_MARKER, this)) {
            // Allow Weakness & Resistance
            if (effect instanceof attack_effects_1.ApplyWeaknessEffect) {
                return state;
            }
            // Allow damage
            if (effect instanceof attack_effects_1.PutDamageEffect) {
                return state;
            }
            // Allow damage
            if (effect instanceof attack_effects_1.DealDamageEffect) {
                return state;
            }
            effect.preventDefault = true;
            return state;
        }
        // Cleanup
        if (effect instanceof game_phase_effects_1.EndTurnEffect && effect.player.marker.hasMarker(this.CLEAR_PREVENT_EFFECTS_MARKER, this)) {
            effect.player.marker.removeMarker(this.CLEAR_PREVENT_EFFECTS_MARKER, this);
            const opponent = game_1.StateUtils.getOpponent(state, effect.player);
            opponent.forEachPokemon(game_1.PlayerType.TOP_PLAYER, (cardList) => {
                cardList.marker.removeMarker(this.PREVENT_EFFECTS_MARKER, this);
            });
        }
        return state;
    }
}
exports.LatiosEx = LatiosEx;
