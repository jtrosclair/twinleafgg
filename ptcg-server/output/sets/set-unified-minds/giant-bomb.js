"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.GiantBomb = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const state_1 = require("../../game/store/state/state");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
class GiantBomb extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.TOOL;
        this.set = 'UNM';
        this.setNumber = '196';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Giant Bomb';
        this.fullName = 'Giant Bomb UNM';
        this.text = 'If this card is attached to 1 of your Pokemon, discard it at the end of your opponent\'s turn. If the Pokemon this card is attached to is your Active Pokemon and takes 180 or more damage from an opponent\'s attack (even if this Pokemon is Knocked Out), put 10 damage counters on the Attacking Pokemon.';
    }
    // Ref: set-stellar-crown/deluxe-bomb.ts (tool AfterDamageEffect + damage retaliation + discard), set-dragons-majesty/dragon-talon.ts (tool ToolEffect pattern)
    reduceEffect(store, state, effect) {
        // Check for 180+ damage and retaliate with 10 damage counters
        if (effect instanceof attack_effects_1.AfterDamageEffect && effect.target.tools.includes(this)) {
            const player = effect.player;
            const targetPlayer = game_1.StateUtils.findOwner(state, effect.target);
            // Try to reduce ToolEffect, to check if something is blocking the tool from working
            try {
                const stub = new play_card_effects_1.ToolEffect(effect.player, this);
                store.reduceEffect(state, stub);
            }
            catch (_a) {
                return state;
            }
            if (effect.damage <= 0 || player === targetPlayer || targetPlayer.active !== effect.target) {
                return state;
            }
            if (state.phase === state_1.GamePhase.ATTACK && effect.damage >= 180) {
                // 10 damage counters = 100 damage
                effect.source.damage += 100;
            }
        }
        // Discard at end of opponent's turn
        if (effect instanceof game_phase_effects_1.EndTurnEffect) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Check if this tool is attached to one of the opponent's Pokemon
            opponent.forEachPokemon(game_1.PlayerType.TOP_PLAYER, (cardList) => {
                if (cardList.tools.includes(this)) {
                    cardList.moveCardTo(this, opponent.discard);
                }
            });
        }
        return state;
    }
}
exports.GiantBomb = GiantBomb;
