"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Lumineon = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Lumineon extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Finneon';
        this.cardType = W;
        this.hp = 90;
        this.weakness = [{ type: G }];
        this.retreat = [];
        this.attacks = [
            {
                name: 'Neon Trickery',
                cost: [C, C],
                damage: 50,
                text: 'You may move an Energy from your opponent\'s Active Pokémon to 1 of their Benched Pokémon.'
            }
        ];
        this.set = 'UNM';
        this.setNumber = '40';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Lumineon';
        this.fullName = 'Lumineon UNM';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Neon Trickery
        // Ref: set-breakpoint/slowking.ts (Royal Flash - move energy from opponent's active to bench)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const hasEnergy = opponent.active.cards.some(c => c instanceof game_1.EnergyCard);
            const hasBenched = opponent.bench.some(b => b.cards.length > 0);
            if (!hasEnergy || !hasBenched) {
                return state;
            }
            // "You may" - optional
            store.prompt(state, new game_1.ConfirmPrompt(player.id, game_1.GameMessage.WANT_TO_USE_ABILITY), wantToUse => {
                if (wantToUse) {
                    // Choose an energy from opponent's active
                    store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_ATTACH, opponent.active, { superType: card_types_1.SuperType.ENERGY }, { min: 1, max: 1, allowCancel: false }), selected => {
                        const cards = selected || [];
                        if (cards.length === 0) {
                            return;
                        }
                        // Choose a benched Pokemon to move the energy to
                        store.prompt(state, new game_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON_TO_ATTACH_CARDS, game_1.PlayerType.TOP_PLAYER, [game_1.SlotType.BENCH], { min: 1, max: 1, allowCancel: false }), targets => {
                            if (targets && targets.length > 0) {
                                opponent.active.moveCardTo(cards[0], targets[0]);
                            }
                        });
                    });
                }
            });
        }
        return state;
    }
}
exports.Lumineon = Lumineon;
