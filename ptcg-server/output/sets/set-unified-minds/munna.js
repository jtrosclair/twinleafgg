"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Munna = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Munna extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = P;
        this.hp = 60;
        this.weakness = [{ type: P }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Future Sight',
                cost: [C],
                damage: 0,
                text: 'Look at the top 4 cards of either player\'s deck and put them back in any order.'
            }
        ];
        this.set = 'UNM';
        this.setNumber = '88';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Munna';
        this.fullName = 'Munna UNM';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Future Sight
        // Ref: set-burning-shadows/porygon2.ts (Calculate - look at top cards and reorder)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Ask player which deck to look at
            return store.prompt(state, new game_1.ConfirmPrompt(player.id, game_1.GameMessage.WANT_TO_USE_ABILITY), lookAtOwnDeck => {
                const targetDeck = lookAtOwnDeck ? player.deck : opponent.deck;
                if (targetDeck.cards.length === 0) {
                    return state;
                }
                const deckTop = new game_1.CardList();
                targetDeck.moveTo(deckTop, Math.min(4, targetDeck.cards.length));
                return store.prompt(state, new game_1.OrderCardsPrompt(player.id, game_1.GameMessage.CHOOSE_CARDS_ORDER, deckTop, { allowCancel: false }), order => {
                    if (order === null) {
                        return state;
                    }
                    deckTop.applyOrder(order);
                    deckTop.moveToTopOfDestination(targetDeck);
                });
            });
        }
        return state;
    }
}
exports.Munna = Munna;
