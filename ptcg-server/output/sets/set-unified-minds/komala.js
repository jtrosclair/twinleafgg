"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Komala = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Komala extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = C;
        this.hp = 90;
        this.weakness = [{ type: F }];
        this.retreat = [C];
        this.powers = [{
                name: 'Drowsing',
                powerType: game_1.PowerType.ABILITY,
                text: 'If this Pokémon remains Asleep between turns, put 6 damage counters on your opponent\'s Active Pokémon.'
            }];
        this.attacks = [
            {
                name: 'Snooze',
                cost: [C],
                damage: 0,
                text: 'This Pokémon is now Asleep.'
            }
        ];
        this.set = 'UNM';
        this.setNumber = '185';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Komala';
        this.fullName = 'Komala UNM';
    }
    reduceEffect(store, state, effect) {
        // Ability: Drowsing (passive - triggers between turns if remains Asleep)
        // Ref: set-flashfire/snorlax.ts (BetweenTurnsEffect Asleep check pattern)
        if (effect instanceof game_phase_effects_1.BetweenTurnsEffect) {
            const player = effect.player;
            const cardList = game_1.StateUtils.findCardList(state, this);
            // Only works if this is the active Pokemon
            if (cardList !== player.active) {
                return state;
            }
            // Check if this Pokemon is currently Asleep
            if (!player.active.specialConditions.includes(card_types_1.SpecialCondition.ASLEEP)) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // Check if this Pokemon is still the top card (not evolved)
            if (player.active.getPokemonCard() !== this) {
                return state;
            }
            // Put 6 damage counters on opponent's active (60 damage)
            const opponent = game_1.StateUtils.getOpponent(state, player);
            opponent.active.damage += 60;
        }
        // Attack 1: Snooze
        // Ref: AGENTS-patterns.md (This Pokemon is now Asleep)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            (0, prefabs_1.ADD_SLEEP_TO_PLAYER_ACTIVE)(store, state, effect.player, this);
        }
        return state;
    }
}
exports.Komala = Komala;
