"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.EarRingingBell = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const check_effects_1 = require("../../game/store/effects/check-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
class EarRingingBell extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.TOOL;
        this.set = 'UNM';
        this.setNumber = '194';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Ear-Ringing Bell';
        this.fullName = 'Ear-Ringing Bell UNM';
        this.text = 'If the Pokemon this card is attached to is your Active Pokemon and is damaged by an opponent\'s attack (even if that Pokemon is Knocked Out), the Attacking Pokemon is now Confused.';
    }
    // Ref: set-sun-and-moon/poison-barb.ts (Poison Barb - tool AfterDamageEffect + special condition on attacker)
    reduceEffect(store, state, effect) {
        if (effect instanceof attack_effects_1.AfterDamageEffect && effect.target.tools.includes(this)) {
            const player = effect.player;
            const targetPlayer = game_1.StateUtils.findOwner(state, effect.target);
            // Only trigger if: attacked by opponent, target is active, damage > 0
            if (effect.damage <= 0 || player === targetPlayer || targetPlayer.active !== effect.target) {
                return state;
            }
            // Check if tool is blocked
            try {
                const stub = new play_card_effects_1.ToolEffect(effect.player, this);
                store.reduceEffect(state, stub);
            }
            catch (_a) {
                return state;
            }
            if (state.phase === game_1.GamePhase.ATTACK) {
                // Apply Confused to the attacking Pokemon
                const confuseEffect = new check_effects_1.AddSpecialConditionsPowerEffect(player, this, effect.source, [card_types_1.SpecialCondition.CONFUSED]);
                store.reduceEffect(state, confuseEffect);
            }
        }
        return state;
    }
}
exports.EarRingingBell = EarRingingBell;
