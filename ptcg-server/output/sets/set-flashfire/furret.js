"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Furret = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const card_list_1 = require("../../game/store/state/card-list");
class Furret extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Sentret';
        this.cardType = C;
        this.hp = 90;
        this.weakness = [{ type: F }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Buried Treasure Hunt',
                cost: [C],
                damage: 0,
                text: 'Look at the top 4 cards of your deck and put 2 of them into your hand. Discard the other cards.'
            },
            {
                name: 'Slam',
                cost: [C, C],
                damage: 30,
                damageCalculation: 'x',
                text: 'Flip 2 coins. This attack does 30 damage times the number of heads.'
            }
        ];
        this.set = 'FLF';
        this.setNumber = '82';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Furret';
        this.fullName = 'Furret FLF';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-legendary-treasures/phione.ts (card selection pattern)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            if (player.deck.cards.length === 0) {
                return state;
            }
            const topCards = new card_list_1.CardList();
            const count = Math.min(4, player.deck.cards.length);
            player.deck.moveTo(topCards, count);
            const pickCount = Math.min(2, topCards.cards.length);
            store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_HAND, topCards, {}, { min: pickCount, max: pickCount, allowCancel: false }), (selected) => {
                const cards = selected || [];
                topCards.moveCardsTo(cards, player.hand);
                topCards.moveTo(player.discard);
            });
        }
        // Ref: set-x-and-y/malamar.ts (Multiple coin flip pattern)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            (0, prefabs_1.MULTIPLE_COIN_FLIPS_PROMPT)(store, state, effect.player, 2, results => {
                const heads = results.filter(r => r).length;
                effect.damage = 30 * heads;
            });
        }
        return state;
    }
}
exports.Furret = Furret;
