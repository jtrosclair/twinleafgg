"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Magmortar = void 0;
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const card_types_1 = require("../../game/store/card/card-types");
const state_utils_1 = require("../../game/store/state-utils");
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
class Magmortar extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Magmar';
        this.cardType = R;
        this.hp = 140;
        this.weakness = [{ type: W }];
        this.retreat = [C, C, C];
        this.attacks = [
            {
                name: 'Burst Punch',
                cost: [R, C],
                damage: 30,
                text: 'Your opponent\'s Active Pokémon is now Burned.'
            },
            {
                name: 'Ground Burn',
                cost: [R, R, C],
                damage: 80,
                damageCalculation: '+',
                text: 'Each player discards the top card of their deck. This attack does 100 more damage for each Energy card discarded in this way.'
            }
        ];
        this.regulationMark = 'D';
        this.set = 'RCL';
        this.setNumber = '30';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Magmortar';
        this.fullName = 'Magmortar RCL';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Burst Punch
        // Ref: AGENTS-patterns.md (Special Conditions)
        if ((0, prefabs_1.AFTER_ATTACK)(effect, 0, this)) {
            (0, prefabs_1.ADD_BURN_TO_PLAYER_ACTIVE)(store, state, effect.opponent, this);
        }
        // Attack 2: Ground Burn
        // Ref: set-battle-styles/houndoom.ts (discard top card, bonus for Energy)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = state_utils_1.StateUtils.getOpponent(state, player);
            let energyCount = 0;
            // Each player discards the top card of their deck
            if (player.deck.cards.length > 0) {
                const playerTop = player.deck.cards[0];
                player.deck.moveCardTo(playerTop, player.discard);
                if (playerTop.superType === card_types_1.SuperType.ENERGY) {
                    energyCount++;
                }
            }
            if (opponent.deck.cards.length > 0) {
                const opponentTop = opponent.deck.cards[0];
                opponent.deck.moveCardTo(opponentTop, opponent.discard);
                if (opponentTop.superType === card_types_1.SuperType.ENERGY) {
                    energyCount++;
                }
            }
            effect.damage += 100 * energyCount;
        }
        return state;
    }
}
exports.Magmortar = Magmortar;
