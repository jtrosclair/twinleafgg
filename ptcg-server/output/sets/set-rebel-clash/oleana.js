"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Oleana = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
function* playCard(next, store, state, self, effect) {
    const player = effect.player;
    const opponent = game_1.StateUtils.getOpponent(state, player);
    // Move to supporter zone first (manual because we use generator)
    player.hand.moveCardTo(effect.trainerCard, player.supporter);
    effect.preventDefault = true;
    // Discard 2 other cards from hand
    const nonSelfCards = player.hand.cards.filter(c => c !== effect.trainerCard);
    if (nonSelfCards.length >= 2) {
        yield store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DISCARD, player.hand, {}, { min: 2, max: 2, allowCancel: false }), (selected) => {
            const cards = selected || [];
            player.hand.moveCardsTo(cards, player.discard);
            next();
        });
    }
    // Show opponent's hand to us
    if (opponent.hand.cards.length > 0) {
        (0, prefabs_1.SHOW_CARDS_TO_PLAYER)(store, state, player, opponent.hand.cards);
    }
    // Let player choose a Trainer card from opponent's hand to put on bottom of deck
    const trainerCards = opponent.hand.cards.filter(c => c.superType === card_types_1.SuperType.TRAINER);
    if (trainerCards.length > 0) {
        yield store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DECK, opponent.hand, { superType: card_types_1.SuperType.TRAINER }, { min: 0, max: 1, allowCancel: false }), (selected) => {
            const cards = selected || [];
            cards.forEach(card => {
                // Put on bottom of deck
                opponent.hand.moveCardTo(card, opponent.deck);
            });
            next();
        });
    }
    // Clean up supporter
    return state;
}
class Oleana extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.regulationMark = 'D';
        this.set = 'RCL';
        this.setNumber = '163';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Oleana';
        this.fullName = 'Oleana RCL';
        this.text = 'You can play this card only if you discard 2 other cards from your hand. Your opponent reveals their hand. Put a Trainer card you find there on the bottom of their deck. You may play only 1 Supporter card during your turn.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-hidden-fates/jessie-and-james.ts (generator pattern - discard from hand + interact with opponent's hand)
        // Ref: set-sword-and-shield/thievul.ts (SHOW_CARDS_TO_PLAYER + ChooseCardsPrompt + put on bottom of deck)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            const nonSelfCards = player.hand.cards.filter(c => c !== effect.trainerCard);
            if (nonSelfCards.length < 2) {
                return state;
            }
            const generator = playCard(() => generator.next(), store, state, this, effect);
            return generator.next().value;
        }
        return state;
    }
}
exports.Oleana = Oleana;
