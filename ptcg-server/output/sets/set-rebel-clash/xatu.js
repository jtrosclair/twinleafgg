"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Xatu = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const check_effects_1 = require("../../game/store/effects/check-effects");
class Xatu extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Natu';
        this.cardType = P;
        this.hp = 120;
        this.weakness = [{ type: D }];
        this.resistance = [{ type: F, value: -30 }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Energy Warp',
                cost: [C],
                damage: 0,
                text: 'Move an Energy from 1 of your opponent\'s Benched Pokémon to their Active Pokémon.'
            },
            {
                name: 'Psychic',
                cost: [P],
                damage: 10,
                damageCalculation: '+',
                text: 'This attack does 30 more damage for each Energy attached to your opponent\'s Active Pokémon.'
            }
        ];
        this.regulationMark = 'D';
        this.set = 'RCL';
        this.setNumber = '77';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Xatu';
        this.fullName = 'Xatu RCL';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Energy Warp
        // Ref: set-unbroken-bonds/tentacruel.ts (MoveEnergyPrompt with TOP_PLAYER for moving opponent's energy)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Check if opponent has bench with energy
            let hasBenchEnergy = false;
            opponent.bench.forEach(b => {
                if (b.cards.length > 0 && b.cards.some(c => c instanceof game_1.EnergyCard)) {
                    hasBenchEnergy = true;
                }
            });
            if (!hasBenchEnergy) {
                return state;
            }
            // Block active as source (only move from bench) and block bench as destination (only move to active)
            const blockedFrom = [
                { player: game_1.PlayerType.TOP_PLAYER, slot: game_1.SlotType.ACTIVE, index: 0 }
            ];
            const blockedTo = [];
            opponent.bench.forEach((_, index) => {
                blockedTo.push({ player: game_1.PlayerType.TOP_PLAYER, slot: game_1.SlotType.BENCH, index });
            });
            store.prompt(state, new game_1.MoveEnergyPrompt(player.id, game_1.GameMessage.MOVE_ENERGY_CARDS, game_1.PlayerType.TOP_PLAYER, [game_1.SlotType.BENCH, game_1.SlotType.ACTIVE], { superType: game_1.SuperType.ENERGY }, { min: 1, max: 1, allowCancel: false, blockedFrom, blockedTo }), result => {
                const transfers = result || [];
                transfers.forEach(transfer => {
                    const source = game_1.StateUtils.getTarget(state, player, transfer.from);
                    const target = game_1.StateUtils.getTarget(state, player, transfer.to);
                    source.moveCardTo(transfer.card, target);
                });
            });
        }
        // Attack 2: Psychic
        // Ref: set-guardians-rising/tapu-koko-gx.ts (CheckProvidedEnergyEffect counting energies)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const checkEnergy = new check_effects_1.CheckProvidedEnergyEffect(opponent, opponent.active);
            store.reduceEffect(state, checkEnergy);
            // Use reduce to count total energy provided (accounts for multi-energy cards like DCE)
            const energyCount = checkEnergy.energyMap.reduce((sum, em) => sum + em.provides.length, 0);
            effect.damage += 30 * energyCount;
        }
        return state;
    }
}
exports.Xatu = Xatu;
