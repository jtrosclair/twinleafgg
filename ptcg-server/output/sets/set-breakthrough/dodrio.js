"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Dodrio = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Dodrio extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Doduo';
        this.cardType = C;
        this.hp = 90;
        this.weakness = [{ type: L }];
        this.resistance = [{ type: F, value: -20 }];
        this.retreat = [C];
        this.powers = [{
                name: 'Retreat Aid',
                powerType: game_1.PowerType.ABILITY,
                text: 'As long as this Pokémon is on your Bench, your Active Pokémon\'s Retreat Cost is ColorlessColorless less.'
            }];
        this.attacks = [
            {
                name: 'Fury Attack',
                cost: [C, C, C],
                damage: 40,
                damageCalculation: 'x',
                text: 'Flip 3 coins. This attack does 40 damage times the number of heads.'
            }
        ];
        this.set = 'BKT';
        this.setNumber = '117';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Dodrio';
        this.fullName = 'Dodrio BKT';
    }
    reduceEffect(store, state, effect) {
        // Ability: Retreat Aid (passive - reduce retreat cost by 2 when on bench)
        // Ref: set-flashfire/forretress.ts (CheckRetreatCostEffect pattern)
        if (effect instanceof check_effects_1.CheckRetreatCostEffect) {
            const player = effect.player;
            // Check if Dodrio is on this player's bench
            let isOnBench = false;
            player.bench.forEach(benchSlot => {
                if (benchSlot.cards.includes(this) && benchSlot.getPokemonCard() === this) {
                    isOnBench = true;
                }
            });
            if (!isOnBench) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // Remove up to 2 Colorless from retreat cost
            let toRemove = 2;
            for (let i = effect.cost.length - 1; i >= 0 && toRemove > 0; i--) {
                effect.cost.splice(i, 1);
                toRemove--;
            }
        }
        // Attack 1: Fury Attack
        // Ref: AGENTS-patterns.md (multiple coin flips)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            (0, prefabs_1.MULTIPLE_COIN_FLIPS_PROMPT)(store, state, effect.player, 3, results => {
                const heads = results.filter(r => r).length;
                effect.damage = 40 * heads;
            });
        }
        return state;
    }
}
exports.Dodrio = Dodrio;
