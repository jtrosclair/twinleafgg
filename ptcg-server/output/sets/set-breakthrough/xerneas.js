"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Xerneas = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_effects_1 = require("../../game/store/effects/game-effects");
const check_effects_1 = require("../../game/store/effects/check-effects");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Xerneas extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = Y;
        this.hp = 120;
        this.weakness = [{ type: M }];
        this.resistance = [{ type: D, value: -20 }];
        this.retreat = [C, C];
        this.HEALED_MARKER = 'XERNEAS_HEALED_MARKER';
        this.attacks = [
            {
                name: 'Rainbow Force',
                cost: [Y, C, C],
                damage: 10,
                damageCalculation: '+',
                text: 'This attack does 30 more damage for each different type of Pokémon on your Bench.'
            },
            {
                name: 'Power Creation',
                cost: [Y, Y, C, C],
                damage: 80,
                damageCalculation: '+',
                text: 'If this Pokémon was healed during this turn, this attack does 80 more damage.'
            }
        ];
        this.set = 'BKT';
        this.setNumber = '107';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Xerneas';
        this.fullName = 'Xerneas BKT';
    }
    reduceEffect(store, state, effect) {
        // Track if this Pokemon was healed this turn
        if (effect instanceof game_effects_1.HealEffect && effect.target.cards.includes(this) && effect.damage > 0) {
            effect.target.marker.addMarker(this.HEALED_MARKER, this);
        }
        // Attack 1: Rainbow Force
        // Ref: AGENTS-patterns.md (different types on bench)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const types = new Set();
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                if (cardList === player.active) {
                    return;
                }
                if (cardList.cards.length === 0) {
                    return;
                }
                const checkType = new check_effects_1.CheckPokemonTypeEffect(cardList);
                store.reduceEffect(state, checkType);
                checkType.cardTypes.forEach(t => types.add(t));
            });
            effect.damage += 30 * types.size;
        }
        // Attack 2: Power Creation
        // Ref: set-breakthrough/seaking.ts (Soaking Horn - healed marker)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            if (effect.player.active.marker.hasMarker(this.HEALED_MARKER, this)) {
                effect.damage += 80;
            }
        }
        // Clean up marker at end of turn
        if (effect instanceof game_phase_effects_1.EndTurnEffect) {
            effect.player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                cardList.marker.removeMarker(this.HEALED_MARKER, this);
            });
        }
        return state;
    }
}
exports.Xerneas = Xerneas;
