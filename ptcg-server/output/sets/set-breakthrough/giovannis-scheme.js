"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.GiovannisScheme = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class GiovannisScheme extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.set = 'BKT';
        this.setNumber = '138';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Giovanni\'s Scheme';
        this.fullName = 'Giovanni\'s Scheme BKT';
        this.text = 'Choose 1:\n• Draw cards until you have 5 cards in your hand.\n• During this turn, your Pokémon\'s attacks do 20 more damage to your opponent\'s Active Pokémon (before applying Weakness and Resistance). You may play only 1 Supporter card during your turn (before your attack).';
        this.GIOVANNIS_SCHEME_MARKER = 'GIOVANNIS_SCHEME_MARKER';
    }
    // Refs: set-twilight-masquerade/kieran.ts (choose option + damage boost marker), set-ancient-origins/baltoy-2.ts (SelectPrompt pattern)
    reduceEffect(store, state, effect) {
        // Clean up marker at end of turn
        if (effect instanceof game_phase_effects_1.EndTurnEffect && effect.player.marker.hasMarker(this.GIOVANNIS_SCHEME_MARKER, this)) {
            effect.player.marker.removeMarker(this.GIOVANNIS_SCHEME_MARKER, this);
            return state;
        }
        // Apply +20 damage on DealDamageEffect
        if (effect instanceof attack_effects_1.DealDamageEffect) {
            const player = effect.player;
            if (player.marker.hasMarker(this.GIOVANNIS_SCHEME_MARKER, this) && effect.damage > 0) {
                const opponent = game_1.StateUtils.getOpponent(state, player);
                if (effect.target === opponent.active) {
                    effect.damage += 20;
                }
            }
        }
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            const options = [
                {
                    message: game_1.GameMessage.WANT_TO_DRAW_CARDS,
                    action: () => {
                        (0, prefabs_1.DRAW_CARDS_UNTIL_CARDS_IN_HAND)(player, 5);
                    }
                },
                {
                    message: game_1.GameMessage.WANT_TO_DEAL_MORE_DAMAGE,
                    action: () => {
                        player.marker.addMarker(this.GIOVANNIS_SCHEME_MARKER, this);
                    }
                }
            ];
            return store.prompt(state, new game_1.SelectPrompt(player.id, game_1.GameMessage.CHOOSE_OPTION, options.map(opt => opt.message), { allowCancel: false }), choice => {
                const option = options[choice];
                option.action();
            });
        }
        return state;
    }
}
exports.GiovannisScheme = GiovannisScheme;
