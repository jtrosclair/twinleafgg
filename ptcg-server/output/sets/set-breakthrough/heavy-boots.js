"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.HeavyBoots = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const check_effects_1 = require("../../game/store/effects/check-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
class HeavyBoots extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.TOOL;
        this.set = 'BKT';
        this.setNumber = '141';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Heavy Boots';
        this.fullName = 'Heavy Boots BKT';
        this.text = 'If the Retreat Cost of the Pokémon this card is attached to is 3 or more, that Pokémon gets +20 HP and can\'t be Confused. (If that Pokémon is currently Confused, remove that Special Condition.)';
    }
    isToolActive(store, state, target) {
        const sourceCard = target.getPokemonCard();
        if (!sourceCard || sourceCard.retreat.length < 3) {
            return false;
        }
        try {
            const owner = game_1.StateUtils.findOwner(state, target);
            const toolEffect = new play_card_effects_1.ToolEffect(owner, this);
            store.reduceEffect(state, toolEffect);
        }
        catch (_a) {
            return false;
        }
        return true;
    }
    // Refs: set-team-up/buff-padding.ts (CheckHpEffect for HP bonus), set-furious-fists/sparkling-robe.ts (confusion prevention)
    reduceEffect(store, state, effect) {
        // +20 HP if retreat cost is 3+
        if (effect instanceof check_effects_1.CheckHpEffect && effect.target.tools.includes(this)) {
            if (this.isToolActive(store, state, effect.target)) {
                effect.hp += 20;
            }
        }
        // Prevent Confused from being applied
        if (effect instanceof attack_effects_1.AddSpecialConditionsEffect && effect.target.tools.includes(this)) {
            if (this.isToolActive(store, state, effect.target)) {
                effect.specialConditions = effect.specialConditions.filter(sc => sc !== card_types_1.SpecialCondition.CONFUSED);
            }
        }
        if (effect instanceof check_effects_1.AddSpecialConditionsPowerEffect && effect.target.tools.includes(this)) {
            if (this.isToolActive(store, state, effect.target)) {
                effect.specialConditions = effect.specialConditions.filter(sc => sc !== card_types_1.SpecialCondition.CONFUSED);
            }
        }
        // Clear existing Confused during table state check
        if (effect instanceof check_effects_1.CheckTableStateEffect) {
            state.players.forEach(player => {
                player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, cardList => {
                    if (cardList.tools.includes(this)
                        && cardList.specialConditions.includes(card_types_1.SpecialCondition.CONFUSED)
                        && this.isToolActive(store, state, cardList)) {
                        cardList.removeSpecialCondition(card_types_1.SpecialCondition.CONFUSED);
                    }
                });
            });
        }
        return state;
    }
}
exports.HeavyBoots = HeavyBoots;
