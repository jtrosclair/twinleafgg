"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Granbull = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Granbull extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Snubbull';
        this.cardType = Y;
        this.hp = 120;
        this.weakness = [{ type: M }];
        this.resistance = [{ type: D, value: -20 }];
        this.retreat = [C, C, C];
        this.attacks = [
            {
                name: 'Startling Bark',
                cost: [C, C],
                damage: 0,
                text: 'Flip a coin. If heads, choose 3 random cards from your opponent\'s hand. Your opponent reveals those cards and shuffles them into his or her deck.'
            },
            {
                name: 'Tantrum',
                cost: [Y, C, C],
                damage: 120,
                text: 'This Pokémon is now Confused.'
            }
        ];
        this.set = 'BKT';
        this.setNumber = '99';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Granbull';
        this.fullName = 'Granbull BKT';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Startling Bark
        // Ref: set-roaring-skies/inkay.ts (Rip Off - random card from hand + shuffle into deck)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            (0, prefabs_1.COIN_FLIP_PROMPT)(store, state, player, result => {
                if (result && opponent.hand.cards.length > 0) {
                    const count = Math.min(3, opponent.hand.cards.length);
                    const handCopy = [...opponent.hand.cards];
                    const cardsToShuffle = [];
                    for (let i = 0; i < count; i++) {
                        const randomIndex = Math.floor(Math.random() * handCopy.length);
                        cardsToShuffle.push(handCopy[randomIndex]);
                        handCopy.splice(randomIndex, 1);
                    }
                    (0, prefabs_1.SHOW_CARDS_TO_PLAYER)(store, state, player, cardsToShuffle);
                    cardsToShuffle.forEach(card => {
                        opponent.hand.moveCardTo(card, opponent.deck);
                    });
                    (0, prefabs_1.SHUFFLE_DECK)(store, state, opponent);
                }
            });
        }
        // Attack 2: Tantrum
        // Ref: AGENTS-patterns.md (This Pokemon is now Confused)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            (0, prefabs_1.ADD_CONFUSION_TO_PLAYER_ACTIVE)(store, state, effect.player, this);
        }
        return state;
    }
}
exports.Granbull = Granbull;
