"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Zoroark = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Zoroark extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Zorua';
        this.cardType = D;
        this.hp = 100;
        this.weakness = [{ type: F }];
        this.resistance = [{ type: P, value: -20 }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'Stand In',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'Once during your turn (before your attack), if this Pokémon is on your Bench, you may switch this Pokémon with your Active Pokémon.'
            }];
        this.attacks = [
            {
                name: 'Mind Jack',
                cost: [C, C],
                damage: 10,
                damageCalculation: '+',
                text: 'This attack does 30 more damage for each of your opponent\'s Benched Pokémon.'
            }
        ];
        this.set = 'BKT';
        this.setNumber = '91';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Zoroark';
        this.fullName = 'Zoroark BKT';
        this.STAND_IN_MARKER = 'ZOROARK_BKT_STAND_IN_MARKER';
    }
    reduceEffect(store, state, effect) {
        // Ability: Stand In
        // Ref: set-breakpoint/zoroark.ts (Stand In - switch from bench to active)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            const cardList = game_1.StateUtils.findCardList(state, this);
            const benchIndex = player.bench.indexOf(cardList);
            if (benchIndex === -1) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_USE_POWER);
            }
            (0, prefabs_1.USE_ABILITY_ONCE_PER_TURN)(player, this.STAND_IN_MARKER, this);
            (0, prefabs_1.ABILITY_USED)(player, this);
            player.active.clearEffects();
            player.switchPokemon(player.bench[benchIndex]);
        }
        (0, prefabs_1.REMOVE_MARKER_AT_END_OF_TURN)(effect, this.STAND_IN_MARKER, this);
        // Attack 1: Mind Jack
        // Ref: set-breakpoint/zoroark.ts (Mind Jack - count opponent bench)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const opponentBenched = opponent.bench.reduce((count, b) => count + (b.cards.length ? 1 : 0), 0);
            effect.damage += 30 * opponentBenched;
        }
        return state;
    }
}
exports.Zoroark = Zoroark;
