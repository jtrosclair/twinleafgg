"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Parasect = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attack_effects_1 = require("../../game/store/prefabs/attack-effects");
const choose_pokemon_prompt_1 = require("../../game/store/prompts/choose-pokemon-prompt");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const energy_card_1 = require("../../game/store/card/energy-card");
class Parasect extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Paras';
        this.cardType = G;
        this.hp = 100;
        this.weakness = [{ type: R }];
        this.retreat = [C, C];
        this.attacks = [
            {
                name: 'Colorful Spores',
                cost: [C],
                damage: 0,
                text: 'Choose 3 of your Pokémon. For each of those Pokémon, search your deck for a different type of basic Energy card and attach it to that Pokémon. Shuffle your deck afterward.'
            },
            {
                name: 'X-Scissor',
                cost: [G, C],
                damage: 30,
                damageCalculation: '+',
                text: 'Flip a coin. If heads, this attack does 30 more damage.'
            }
        ];
        this.set = 'BKT';
        this.setNumber = '2';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Parasect';
        this.fullName = 'Parasect BKT';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Colorful Spores
        // Ref: set-roaring-skies/togekiss-2.ts (energy attachment from deck)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            if (player.deck.cards.length === 0) {
                return state;
            }
            const pokemonCount = 1 + player.bench.filter(b => b.cards.length > 0).length;
            const maxTargets = Math.min(3, pokemonCount);
            // Choose up to 3 of your Pokemon
            store.prompt(state, new choose_pokemon_prompt_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { min: 0, max: maxTargets, allowCancel: true }), targets => {
                if (!targets || targets.length === 0) {
                    (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                    return;
                }
                // For each target, search for a different type of basic Energy
                const usedTypes = [];
                const attachToTarget = (index) => {
                    if (index >= targets.length) {
                        (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                        return;
                    }
                    const target = targets[index];
                    // Filter deck for basic energy of types not yet used
                    const blocked = [];
                    player.deck.cards.forEach((card, i) => {
                        if (!(card instanceof energy_card_1.EnergyCard) || card.energyType !== card_types_1.EnergyType.BASIC) {
                            blocked.push(i);
                        }
                        else if (usedTypes.some(t => card.provides.includes(t))) {
                            blocked.push(i);
                        }
                    });
                    if (blocked.length === player.deck.cards.length) {
                        // No valid energy available
                        attachToTarget(index + 1);
                        return;
                    }
                    store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_ATTACH, player.deck, { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC }, { min: 0, max: 1, allowCancel: true, blocked }), (selected) => {
                        if (selected && selected.length > 0) {
                            const energyCard = selected[0];
                            energyCard.provides.forEach(t => {
                                if (t !== card_types_1.CardType.COLORLESS) {
                                    usedTypes.push(t);
                                }
                            });
                            player.deck.moveCardTo(energyCard, target);
                        }
                        attachToTarget(index + 1);
                    });
                };
                attachToTarget(0);
            });
        }
        // Attack 2: X-Scissor
        // Ref: AGENTS-patterns.md (coin flip more damage)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            (0, attack_effects_1.FLIP_A_COIN_IF_HEADS_DEAL_MORE_DAMAGE)(store, state, effect, 30);
        }
        return state;
    }
}
exports.Parasect = Parasect;
