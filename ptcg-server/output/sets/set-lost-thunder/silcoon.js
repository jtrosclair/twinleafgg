"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Silcoon = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Silcoon extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Wurmple';
        this.cardType = G;
        this.hp = 80;
        this.weakness = [{ type: R }];
        this.retreat = [C, C, C];
        this.attacks = [
            {
                name: 'Cocoon Collector',
                cost: [C],
                damage: 0,
                text: 'Search your deck for up to 4 in any combination of Silcoon and Cascoon and put them onto your Bench. Then, shuffle your deck.'
            },
            {
                name: 'Rolling Tackle',
                cost: [G],
                damage: 20,
                text: ''
            }
        ];
        this.set = 'LOT';
        this.setNumber = '25';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Silcoon';
        this.fullName = 'Silcoon LOT';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Cocoon Collector
        // Ref: set-forbidden-light/scatterbug.ts (Abnormal Outbreak - multi-name search with blocked indices)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const slots = (0, prefabs_1.GET_PLAYER_BENCH_SLOTS)(player);
            if (player.deck.cards.length === 0 || slots.length === 0) {
                return state;
            }
            const maxSelect = Math.min(4, slots.length);
            const blocked = [];
            player.deck.cards.forEach((c, index) => {
                if (!(c instanceof pokemon_card_1.PokemonCard && (c.name === 'Silcoon' || c.name === 'Cascoon'))) {
                    blocked.push(index);
                }
            });
            return store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_PUT_ONTO_BENCH, player.deck, {}, { min: 0, max: maxSelect, allowCancel: true, blocked }), (selected) => {
                const cards = selected || [];
                cards.forEach((card, index) => {
                    if (index < slots.length) {
                        player.deck.moveCardTo(card, slots[index]);
                        slots[index].pokemonPlayedTurn = state.turn;
                    }
                });
                (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            });
        }
        return state;
    }
}
exports.Silcoon = Silcoon;
