"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.VirizionGx = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class VirizionGx extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_GX];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = G;
        this.hp = 170;
        this.weakness = [{ type: R }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Double Draw',
                cost: [C],
                damage: 0,
                text: 'Draw 2 cards.'
            },
            {
                name: 'Sensitive Blade',
                cost: [G, G],
                damage: 50,
                damageCalculation: '+',
                text: 'If you played a Supporter card from your hand during this turn, this attack does 80 more damage.'
            },
            {
                name: 'Breeze Away-GX',
                cost: [C],
                damage: 0,
                text: 'Put any number of your Pokémon in play and all cards attached to them into your hand. (You can\'t use more than 1 GX attack in a game.)'
            }
        ];
        this.set = 'LOT';
        this.setNumber = '34';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Virizion-GX';
        this.fullName = 'Virizion-GX LOT';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Double Draw
        // Ref: AGENTS-patterns.md (Draw cards)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            (0, prefabs_1.DRAW_CARDS)(effect.player, 2);
        }
        // Attack 2: Sensitive Blade
        // Ref: set-breakthrough/gallade.ts (Sensitive Blade - supporterTurn check)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            if (player.supporterTurn > 0) {
                effect.damage += 80;
            }
        }
        // Attack 3: Breeze Away-GX
        // Ref: set-dragons-majesty/altaria-gx.ts (GX attack pattern)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 2, this)) {
            const player = effect.player;
            (0, prefabs_1.BLOCK_IF_GX_ATTACK_USED)(player);
            player.usedGX = true;
            // Let the player choose any number of their Benched Pokemon
            const hasBench = player.bench.some(b => b.cards.length > 0);
            if (hasBench) {
                store.prompt(state, new game_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.BENCH], { min: 0, max: 5, allowCancel: false }), selected => {
                    if (selected && selected.length > 0) {
                        selected.forEach(target => {
                            // Move tools first
                            const tools = target.tools.slice();
                            tools.forEach(t => { target.moveCardTo(t, player.hand); });
                            // Move all cards (Pokemon + energy) to hand
                            target.moveTo(player.hand);
                            target.clearEffects();
                        });
                    }
                });
            }
        }
        return state;
    }
}
exports.VirizionGx = VirizionGx;
