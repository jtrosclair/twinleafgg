"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Celebi = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Celebi extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.PRISM_STAR];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = G;
        this.hp = 90;
        this.weakness = [{ type: R }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Time Distortion',
                cost: [C],
                damage: 0,
                text: 'Devolve any number of your Benched Pokémon as many times as you like. Put each Evolution card removed this way into your hand.'
            },
            {
                name: 'Leech Seed',
                cost: [G],
                damage: 20,
                text: 'Heal 20 damage from this Pokémon.'
            }
        ];
        this.set = 'LOT';
        this.setNumber = '19';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Celebi \u25c7';
        this.fullName = 'Celebi \u25c7 LOT';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Time Distortion
        // Ref: set-unbroken-bonds/devolution-spray-z.ts (DEVOLVE_POKEMON pattern)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const hasEvolved = player.bench.some(b => b.cards.length > 0 && b.getPokemons().length > 1);
            if (hasEvolved) {
                const devolveLoop = () => {
                    const evolvedBench = player.bench.filter(b => b.cards.length > 0 && b.getPokemons().length > 1);
                    if (evolvedBench.length === 0) {
                        return;
                    }
                    store.prompt(state, new game_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.BENCH], { min: 0, max: 1, allowCancel: true }), selected => {
                        if (selected && selected.length > 0 && selected[0].getPokemons().length > 1) {
                            (0, prefabs_1.DEVOLVE_POKEMON)(store, state, selected[0], player.hand);
                            devolveLoop();
                        }
                    });
                };
                devolveLoop();
            }
        }
        // Attack 2: Leech Seed
        // Ref: set-breakpoint/honedge.ts (heal self after attack)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            (0, prefabs_1.HEAL_X_DAMAGE_FROM_THIS_POKEMON)(effect, store, state, 20);
        }
        return state;
    }
}
exports.Celebi = Celebi;
