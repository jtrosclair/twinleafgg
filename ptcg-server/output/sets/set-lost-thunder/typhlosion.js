"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Typhlosion = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Typhlosion extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_2;
        this.evolvesFrom = 'Quilava';
        this.cardType = R;
        this.hp = 160;
        this.weakness = [{ type: W }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'Blazing Energy',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'Once during your turn (before your attack), you may use this Ability. All Energy attached to your Pokémon are [R] Energy instead of their usual type until the end of your turn. (This includes cards that come into play on this turn.)'
            }];
        this.attacks = [
            {
                name: 'Lost Flame',
                cost: [R, R, R, R],
                damage: 120,
                text: 'Put 2 Energy attached to your opponent\'s Active Pokémon in the Lost Zone.'
            }
        ];
        this.set = 'LOT';
        this.setNumber = '42';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Typhlosion';
        this.fullName = 'Typhlosion LOT';
        this.BLAZING_ENERGY_MARKER = 'TYPHLOSION_LOT_BLAZING_ENERGY_MARKER';
        this.BLAZING_ENERGY_ACTIVE_MARKER = 'TYPHLOSION_LOT_BLAZING_ENERGY_ACTIVE_MARKER';
    }
    reduceEffect(store, state, effect) {
        // Ability: Blazing Energy
        // Ref: set-evolutions/charizard.ts (Energy Burn - energy type override)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            (0, prefabs_1.USE_ABILITY_ONCE_PER_TURN)(player, this.BLAZING_ENERGY_MARKER, this);
            (0, prefabs_1.ABILITY_USED)(player, this);
            (0, prefabs_1.ADD_MARKER)(this.BLAZING_ENERGY_ACTIVE_MARKER, player, this);
        }
        // Intercept energy checks to override types to Fire while marker is active
        if (effect instanceof check_effects_1.CheckProvidedEnergyEffect) {
            let owner = null;
            state.players.forEach(p => {
                p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (cardList.getPokemonCard() === this) {
                        owner = p;
                    }
                });
            });
            if (owner && (0, prefabs_1.HAS_MARKER)(this.BLAZING_ENERGY_ACTIVE_MARKER, owner, this)) {
                // Check if the source belongs to the same player
                let sourceOwner = null;
                state.players.forEach(p => {
                    p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                        if (cardList === effect.source) {
                            sourceOwner = p;
                        }
                    });
                });
                if (sourceOwner === owner) {
                    effect.energyMap.forEach(em => {
                        em.provides = em.provides.map(() => card_types_1.CardType.FIRE);
                    });
                }
            }
        }
        (0, prefabs_1.REMOVE_MARKER_AT_END_OF_TURN)(effect, this.BLAZING_ENERGY_MARKER, this);
        (0, prefabs_1.REMOVE_MARKER_AT_END_OF_TURN)(effect, this.BLAZING_ENERGY_ACTIVE_MARKER, this);
        // Attack 1: Lost Flame
        // Ref: set-lost-origin/bouffalant.ts (Lost Headbutt - energy to Lost Zone)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const energyCount = opponent.active.cards.filter(c => c.superType === game_1.SuperType.ENERGY).length;
            if (energyCount === 0) {
                return state;
            }
            const toRemove = Math.min(2, energyCount);
            store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DISCARD, opponent.active, { superType: game_1.SuperType.ENERGY }, { min: toRemove, max: toRemove, allowCancel: false }), (selected) => {
                const cards = selected || [];
                cards.forEach(card => {
                    opponent.active.moveCardTo(card, opponent.lostzone);
                });
            });
        }
        return state;
    }
}
exports.Typhlosion = Typhlosion;
