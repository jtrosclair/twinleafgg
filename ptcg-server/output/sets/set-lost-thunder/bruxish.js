"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Bruxish = void 0;
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const card_types_1 = require("../../game/store/card/card-types");
const state_utils_1 = require("../../game/store/state-utils");
const choose_pokemon_prompt_1 = require("../../game/store/prompts/choose-pokemon-prompt");
const game_message_1 = require("../../game/game-message");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const game_1 = require("../../game");
class Bruxish extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = W;
        this.hp = 90;
        this.weakness = [{ type: G }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Mind Bend',
                cost: [W],
                damage: 20,
                text: 'Your opponent\'s Active Pokémon is now Confused.'
            },
            {
                name: 'Follow the Wound',
                cost: [W, C],
                damage: 0,
                text: 'This attack does 60 damage to 1 of your opponent\'s Benched Pokémon that has any damage counters on it. (Don\'t apply Weakness and Resistance for Benched Pokémon.)'
            }
        ];
        this.set = 'LOT';
        this.setNumber = '70';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Bruxish';
        this.fullName = 'Bruxish LOT';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Mind Bend
        // Ref: set-celestial-storm/deoxys-2.ts (Confused status)
        if ((0, prefabs_1.AFTER_ATTACK)(effect, 0, this)) {
            (0, prefabs_1.ADD_CONFUSION_TO_PLAYER_ACTIVE)(store, state, effect.opponent, this);
        }
        // Attack 2: Follow the Wound
        // Ref: set-steam-siege/bastiodon.ts (bench targeting with condition)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = state_utils_1.StateUtils.getOpponent(state, player);
            const hasDamagedBench = opponent.bench.some(b => b.cards.length > 0 && b.damage > 0);
            if (!hasDamagedBench) {
                return state;
            }
            const blocked = [];
            blocked.push({ player: game_1.PlayerType.TOP_PLAYER, slot: game_1.SlotType.ACTIVE, index: 0 });
            opponent.bench.forEach((b, i) => {
                if (b.cards.length === 0 || b.damage === 0) {
                    blocked.push({ player: game_1.PlayerType.TOP_PLAYER, slot: game_1.SlotType.BENCH, index: i });
                }
            });
            store.prompt(state, new choose_pokemon_prompt_1.ChoosePokemonPrompt(player.id, game_message_1.GameMessage.CHOOSE_POKEMON, game_1.PlayerType.TOP_PLAYER, [game_1.SlotType.BENCH], { min: 1, max: 1, allowCancel: false, blocked }), selected => {
                if (selected && selected.length > 0) {
                    const damage = new attack_effects_1.PutDamageEffect(effect, 60);
                    damage.target = selected[0];
                    store.reduceEffect(state, damage);
                }
            });
        }
        return state;
    }
}
exports.Bruxish = Bruxish;
