"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.FairyCharmGrass = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const check_effects_1 = require("../../game/store/effects/check-effects");
class FairyCharmGrass extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.TOOL;
        this.set = 'LOT';
        this.setNumber = '174';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Fairy Charm Grass';
        this.fullName = 'Fairy Charm Grass LOT';
        this.text = 'Prevent all damage done to the Fairy Pok\u00e9mon this card is attached to by attacks from your opponent\'s Grass Pok\u00e9mon-GX and Grass Pok\u00e9mon-EX.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-lost-origin/panic-mask.ts (tool-based damage prevention)
        if (effect instanceof attack_effects_1.DealDamageEffect && effect.target.tools.includes(this)) {
            const player = effect.player;
            const targetPlayer = game_1.StateUtils.findOwner(state, effect.target);
            // Try to reduce ToolEffect, to check if something is blocking the tool from working
            try {
                const stub = new play_card_effects_1.ToolEffect(effect.player, this);
                store.reduceEffect(state, stub);
            }
            catch (_a) {
                return state;
            }
            if (effect.damage <= 0 || player === targetPlayer) {
                return state;
            }
            // Check if the attached Pokemon is a Fairy type
            const checkTargetType = new check_effects_1.CheckPokemonTypeEffect(effect.target);
            store.reduceEffect(state, checkTargetType);
            if (!checkTargetType.cardTypes.includes(card_types_1.CardType.FAIRY)) {
                return state;
            }
            // Check if the attacking Pokemon is Grass GX/EX
            const attackingPokemon = player.active.getPokemonCard();
            if (attackingPokemon) {
                const checkAttackerType = new check_effects_1.CheckPokemonTypeEffect(player.active);
                store.reduceEffect(state, checkAttackerType);
                const isGrass = checkAttackerType.cardTypes.includes(card_types_1.CardType.GRASS);
                const isGxOrEx = attackingPokemon.tags.includes(card_types_1.CardTag.POKEMON_GX) ||
                    attackingPokemon.tags.includes(card_types_1.CardTag.POKEMON_EX);
                if (isGrass && isGxOrEx) {
                    effect.damage = 0;
                }
            }
        }
        return state;
    }
}
exports.FairyCharmGrass = FairyCharmGrass;
