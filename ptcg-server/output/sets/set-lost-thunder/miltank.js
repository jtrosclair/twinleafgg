"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Miltank = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const trainer_card_1 = require("../../game/store/card/trainer-card");
const game_1 = require("../../game");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const show_cards_prompt_1 = require("../../game/store/prompts/show-cards-prompt");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Miltank extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = C;
        this.hp = 130;
        this.weakness = [{ type: F }];
        this.retreat = [C, C, C];
        this.attacks = [
            {
                name: 'Milk Cannon',
                cost: [C, C, C],
                damage: 60,
                damageCalculation: 'x',
                text: 'Reveal any number of Moomoo Milk cards in your hand. This attack does 60 damage for each card you revealed in this way.'
            }
        ];
        this.set = 'LOT';
        this.setNumber = '158';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Miltank';
        this.fullName = 'Miltank LOT';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Milk Cannon
        // Ref: AGENTS-patterns.md (reveal cards from hand for damage)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const moomooMilkCount = player.hand.cards.filter(c => c instanceof trainer_card_1.TrainerCard && c.name === 'Moomoo Milk').length;
            if (moomooMilkCount === 0) {
                effect.damage = 0;
                return state;
            }
            // Create blocked list for cards that are NOT Moomoo Milk
            const blocked = [];
            player.hand.cards.forEach((c, index) => {
                if (!(c instanceof trainer_card_1.TrainerCard && c.name === 'Moomoo Milk')) {
                    blocked.push(index);
                }
            });
            store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_HAND, player.hand, {}, { min: 0, max: moomooMilkCount, allowCancel: false, blocked }), (selected) => {
                const cards = selected || [];
                effect.damage = 60 * cards.length;
                if (cards.length > 0) {
                    store.prompt(state, new show_cards_prompt_1.ShowCardsPrompt(opponent.id, game_1.GameMessage.CARDS_SHOWED_BY_THE_OPPONENT, cards), () => { });
                }
            });
        }
        return state;
    }
}
exports.Miltank = Miltank;
