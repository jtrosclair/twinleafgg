"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Delibird = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const trainer_card_1 = require("../../game/store/card/trainer-card");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const choose_pokemon_prompt_1 = require("../../game/store/prompts/choose-pokemon-prompt");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Delibird extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = W;
        this.hp = 90;
        this.weakness = [{ type: M }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Happy Delivery',
                cost: [C],
                damage: 0,
                text: 'Choose any number of your Benched Pokémon that don\'t already have a Pokémon Tool attached to them. For each of those Pokémon, search your deck for a Pokémon Tool card and attach it to that Pokémon. Then, shuffle your deck.'
            },
            {
                name: 'Flap',
                cost: [C, C],
                damage: 30,
                text: ''
            }
        ];
        this.set = 'LOT';
        this.setNumber = '57';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Delibird';
        this.fullName = 'Delibird LOT';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Happy Delivery
        // Ref: set-rising-rivals/pokemon-contest-hall.ts (search deck for tool and attach)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            // Check bench for Pokemon without tools
            const benchWithoutTools = player.bench.filter(b => b.cards.length > 0 && b.tools.length === 0);
            const hasToolsInDeck = player.deck.cards.some(c => c instanceof trainer_card_1.TrainerCard && c.trainerType === card_types_1.TrainerType.TOOL);
            if (benchWithoutTools.length === 0 || !hasToolsInDeck) {
                return (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            }
            // Choose benched Pokemon without tools (up to as many as have no tool)
            store.prompt(state, new choose_pokemon_prompt_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON_TO_ATTACH_CARDS, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.BENCH], { min: 0, max: benchWithoutTools.length, allowCancel: true }), selected => {
                if (!selected || selected.length === 0) {
                    (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                    return;
                }
                // For each selected Pokemon, search deck for a tool and attach
                const attachNext = (index) => {
                    if (index >= selected.length) {
                        (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                        return;
                    }
                    const target = selected[index];
                    store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_ATTACH, player.deck, { superType: card_types_1.SuperType.TRAINER, trainerType: card_types_1.TrainerType.TOOL }, { min: 0, max: 1, allowCancel: true }), (cards) => {
                        cards = cards || [];
                        if (cards.length > 0) {
                            const tool = cards[0];
                            (0, prefabs_1.MOVE_CARD_TO)(state, tool, target);
                            const idx = target.cards.indexOf(tool);
                            if (idx !== -1) {
                                target.cards.splice(idx, 1);
                            }
                            target.tools.push(tool);
                        }
                        attachNext(index + 1);
                    });
                };
                attachNext(0);
            });
        }
        return state;
    }
}
exports.Delibird = Delibird;
