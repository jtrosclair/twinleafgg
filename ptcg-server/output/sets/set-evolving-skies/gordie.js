"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Gordie = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
// Ref: set-silver-tempest/candice.ts (Look at top 7, reveal matching cards to hand, show to opponent, shuffle rest)
function* playCard(next, store, state, self, effect) {
    const player = effect.player;
    const opponent = game_1.StateUtils.getOpponent(state, player);
    player.hand.moveCardTo(effect.trainerCard, player.supporter);
    effect.preventDefault = true;
    if (player.deck.cards.length === 0) {
        return state;
    }
    // Take top 7 cards into a temp list
    const topCards = new game_1.CardList();
    const count = Math.min(7, player.deck.cards.length);
    player.deck.moveTo(topCards, count);
    // Block non-Energy cards so player can only select Energy
    const blocked = [];
    topCards.cards.forEach((c, index) => {
        if (c.superType !== card_types_1.SuperType.ENERGY) {
            blocked.push(index);
        }
    });
    let cards = [];
    yield store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_HAND, topCards, { superType: card_types_1.SuperType.ENERGY }, { min: 0, max: count, allowCancel: false, blocked }), selected => {
        cards = selected || [];
        next();
    });
    // Move selected energy cards to hand, shuffle rest back into deck
    topCards.moveCardsTo(cards, player.hand);
    topCards.moveTo(player.deck);
    // Reveal selected energy cards to opponent (card text says "reveal")
    if (cards.length > 0) {
        yield store.prompt(state, new game_1.ShowCardsPrompt(opponent.id, game_1.GameMessage.CARDS_SHOWED_BY_THE_OPPONENT, cards), () => next());
    }
    return (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
}
class Gordie extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.regulationMark = 'E';
        this.set = 'EVS';
        this.setNumber = '149';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Gordie';
        this.fullName = 'Gordie EVS';
        this.text = 'Look at the top 7 cards of your deck. You may reveal any number of Energy cards you find there and put them into your hand. Shuffle the other cards back into your deck. You may play only 1 Supporter card during your turn.';
    }
    reduceEffect(store, state, effect) {
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const generator = playCard(() => generator.next(), store, state, this, effect);
            return generator.next().value;
        }
        return state;
    }
}
exports.Gordie = Gordie;
