"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.ToyCatcher = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const check_effects_1 = require("../../game/store/effects/check-effects");
class ToyCatcher extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.regulationMark = 'E';
        this.set = 'EVS';
        this.setNumber = '163';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Toy Catcher';
        this.fullName = 'Toy Catcher EVS';
        this.text = 'Switch 1 of your opponent\'s Benched Pokémon that has 50 HP or less remaining with your opponent\'s Active Pokémon. You may play any number of Item cards during your turn. Attach a Pokémon Tool to 1 of your Pokémon that doesn\'t already have a Pokémon Tool attached.';
    }
    reduceEffect(store, state, effect) {
        // Switch 1 of your opponent's Benched Pokémon that has 50 HP or less remaining
        // with your opponent's Active Pokémon.
        // Ref: set-fates-collide/mega-catcher.ts (blocked CardTarget pattern for filtered bench selection)
        // Ref: set-perfect-order/yveltal-ex.ts (CheckHpEffect for remaining HP)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Check if any benched Pokemon has 50 HP or less remaining
            let hasValidTarget = false;
            opponent.bench.forEach(b => {
                if (b.cards.length > 0) {
                    const checkHp = new check_effects_1.CheckHpEffect(opponent, b);
                    store.reduceEffect(state, checkHp);
                    const remainingHp = checkHp.hp - b.damage;
                    if (remainingHp <= 50) {
                        hasValidTarget = true;
                    }
                }
            });
            if (!hasValidTarget) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            // Build blocked array - block bench slots where remaining HP > 50 or is empty
            const blocked = [];
            opponent.forEachPokemon(game_1.PlayerType.TOP_PLAYER, (cardList, card, target) => {
                if (target.slot !== game_1.SlotType.BENCH) {
                    return;
                }
                if (cardList.cards.length === 0) {
                    blocked.push(target);
                    return;
                }
                const checkHp = new check_effects_1.CheckHpEffect(opponent, cardList);
                store.reduceEffect(state, checkHp);
                const remainingHp = checkHp.hp - cardList.damage;
                if (remainingHp > 50) {
                    blocked.push(target);
                }
            });
            return store.prompt(state, new game_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON, game_1.PlayerType.TOP_PLAYER, [game_1.SlotType.BENCH], { min: 1, max: 1, allowCancel: false, blocked }), selected => {
                if (selected && selected.length > 0) {
                    opponent.switchPokemon(selected[0]);
                }
            });
        }
        return state;
    }
}
exports.ToyCatcher = ToyCatcher;
