"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Lisia = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Lisia extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.set = 'CES';
        this.setNumber = '137';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Lisia';
        this.fullName = 'Lisia CES';
        this.text = 'Search your deck for up to 2 ◇ (Prism Star) cards, reveal them, and put them into your hand. Then, shuffle your deck. You may play only 1 Supporter card during your turn (before your attack).';
    }
    // Ref: set-celestial-storm/beast-ball.ts (blocked filter by CardTag for ChooseCardsPrompt)
    reduceEffect(store, state, effect) {
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Check if the deck has any Prism Star cards
            const hasPrismStar = player.deck.cards.some(c => c.tags && c.tags.includes(card_types_1.CardTag.PRISM_STAR));
            if (player.deck.cards.length === 0 || !hasPrismStar) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            // Block non-Prism Star cards
            const blocked = [];
            player.deck.cards.forEach((card, index) => {
                if (!card.tags || !card.tags.includes(card_types_1.CardTag.PRISM_STAR)) {
                    blocked.push(index);
                }
            });
            store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_HAND, player.deck, {}, { min: 1, max: 2, allowCancel: false, blocked }), selected => {
                const cards = selected || [];
                if (cards.length > 0) {
                    // Reveal chosen cards to opponent
                    store.prompt(state, new game_1.ShowCardsPrompt(opponent.id, game_1.GameMessage.CARDS_SHOWED_BY_THE_OPPONENT, cards), () => { });
                    // Move chosen cards to hand
                    cards.forEach(card => {
                        player.deck.moveCardTo(card, player.hand);
                    });
                }
                (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            });
        }
        return state;
    }
}
exports.Lisia = Lisia;
