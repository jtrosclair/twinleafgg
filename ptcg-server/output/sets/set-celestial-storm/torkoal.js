"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Torkoal = void 0;
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const card_types_1 = require("../../game/store/card/card-types");
const game_message_1 = require("../../game/game-message");
const state_utils_1 = require("../../game/store/state-utils");
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const game_1 = require("../../game");
class Torkoal extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = R;
        this.hp = 120;
        this.weakness = [{ type: W }];
        this.retreat = [C, C, C];
        this.attacks = [
            {
                name: 'Flaming Honk',
                cost: [C, C],
                damage: 30,
                text: 'Discard the top 4 cards of your deck. If any of those cards are [R] Energy cards, attach them to your Pokémon in any way you like.'
            },
            {
                name: 'Searing Flame',
                cost: [R, R, C],
                damage: 80,
                text: 'Your opponent\'s Active Pokémon is now Burned.'
            }
        ];
        this.set = 'CES';
        this.setNumber = '29';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Torkoal';
        this.fullName = 'Torkoal CES';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Flaming Honk
        // Ref: set-emerging-powers/gigalith.ts (Shear - discard top cards, attach energy)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const cardsToProcess = player.deck.cards.slice(0, 4);
            const fireEnergy = [];
            const nonFireCards = [];
            cardsToProcess.forEach(card => {
                if (card.superType === card_types_1.SuperType.ENERGY && card.energyType === card_types_1.EnergyType.BASIC && card.name === 'Fire Energy') {
                    fireEnergy.push(card);
                }
                else {
                    nonFireCards.push(card);
                }
            });
            // Discard non-fire cards
            nonFireCards.forEach(card => {
                player.deck.moveCardTo(card, player.discard);
            });
            // Attach fire energy to Pokemon in any way you like
            if (fireEnergy.length > 0) {
                fireEnergy.forEach(card => {
                    player.deck.moveCardTo(card, player.discard);
                });
                store.prompt(state, new game_1.AttachEnergyPrompt(player.id, game_message_1.GameMessage.ATTACH_ENERGY_CARDS, player.discard, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC, name: 'Fire Energy' }, { allowCancel: false, min: 0, max: fireEnergy.length }), transfers => {
                    transfers = transfers || [];
                    for (const transfer of transfers) {
                        const target = state_utils_1.StateUtils.getTarget(state, player, transfer.to);
                        player.discard.moveCardTo(transfer.card, target);
                    }
                });
            }
        }
        // Attack 2: Searing Flame
        // Ref: set-ultra-prism/salazzle.ts (Panic Poison)
        if ((0, prefabs_1.AFTER_ATTACK)(effect, 1, this)) {
            (0, prefabs_1.ADD_BURN_TO_PLAYER_ACTIVE)(store, state, effect.opponent, this);
        }
        return state;
    }
}
exports.Torkoal = Torkoal;
