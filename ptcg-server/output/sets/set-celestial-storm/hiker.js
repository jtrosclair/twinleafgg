"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Hiker = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
function* playCard(next, store, state, self, effect) {
    const player = effect.player;
    const opponent = game_1.StateUtils.getOpponent(state, player);
    // Move to supporter zone, prevent default discard
    effect.preventDefault = true;
    player.hand.moveCardTo(effect.trainerCard, player.supporter);
    // Choose which player's deck to look at
    const options = [
        game_1.GameMessage.ORDER_YOUR_DECK,
        game_1.GameMessage.ORDER_OPPONENT_DECK
    ];
    let chosenPlayerIndex = 0;
    yield store.prompt(state, new game_1.SelectPrompt(player.id, game_1.GameMessage.CHOOSE_OPTION, options, { allowCancel: false }), choice => {
        chosenPlayerIndex = choice;
        next();
    });
    const targetPlayer = chosenPlayerIndex === 0 ? player : opponent;
    if (targetPlayer.deck.cards.length === 0) {
        return state;
    }
    // Look at top 5 cards
    const topCards = new game_1.CardList();
    targetPlayer.deck.moveTo(topCards, Math.min(5, targetPlayer.deck.cards.length));
    // Choose 1 card
    let chosenCards = [];
    yield store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_HAND, topCards, {}, { min: 1, max: 1, allowCancel: false }), selected => {
        chosenCards = selected || [];
        next();
    });
    // Put chosen card aside, shuffle rest back into deck
    const chosenCard = chosenCards[0];
    // Move unchosen cards back to deck
    const remainingCards = topCards.cards.filter(c => c !== chosenCard);
    remainingCards.forEach(card => {
        topCards.moveCardTo(card, targetPlayer.deck);
    });
    // Shuffle the deck
    yield store.prompt(state, new game_1.ShuffleDeckPrompt(targetPlayer.id), order => {
        targetPlayer.deck.applyOrder(order);
        next();
    });
    // Put the chosen card on top of the deck
    topCards.moveCardTo(chosenCard, targetPlayer.deck);
    // unshift to put on top (cards[0] = top)
    const idx = targetPlayer.deck.cards.indexOf(chosenCard);
    if (idx > 0) {
        targetPlayer.deck.cards.splice(idx, 1);
        targetPlayer.deck.cards.unshift(chosenCard);
    }
    return state;
}
class Hiker extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.set = 'CES';
        this.setNumber = '133';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Hiker';
        this.fullName = 'Hiker CES';
        this.text = 'Look at the top 5 cards of either player\'s deck and choose 1 of them. That player shuffles the other cards back into their deck. Then, put the card you chose on top of that deck. You may play only 1 Supporter card during your turn (before your attack).';
    }
    // Ref: set-roaring-skies/xatu.ts (Future Sight - choose either player's deck)
    reduceEffect(store, state, effect) {
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const generator = playCard(() => generator.next(), store, state, this, effect);
            return generator.next().value;
        }
        return state;
    }
}
exports.Hiker = Hiker;
