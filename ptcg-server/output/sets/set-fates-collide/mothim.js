"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Mothim = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Mothim extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Burmy';
        this.cardType = G;
        this.hp = 90;
        this.weakness = [{ type: L }];
        this.resistance = [{ type: F, value: -20 }];
        this.retreat = [];
        this.powers = [{
                name: 'Wormadam First',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'As often as you like during your turn (before your attack), you may move 1 damage counter from 1 of your Wormadam to another of your Pokémon.'
            }];
        this.attacks = [
            {
                name: 'Gust',
                cost: [C, C, C],
                damage: 60,
                text: ''
            }
        ];
        this.set = 'FCO';
        this.setNumber = '4';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Mothim';
        this.fullName = 'Mothim FCO';
    }
    reduceEffect(store, state, effect) {
        // Ability: Wormadam First
        // Ref: set-flashfire/dusknoir.ts (Shadow Void - move damage counter between Pokemon)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            // Check if any Wormadam has damage
            let hasDamagedWormadam = false;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                const pokemonCard = cardList.getPokemonCard();
                if (pokemonCard && pokemonCard.name === 'Wormadam' && cardList.damage > 0) {
                    hasDamagedWormadam = true;
                }
            });
            if (!hasDamagedWormadam) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_USE_POWER);
            }
            (0, prefabs_1.ABILITY_USED)(player, this);
            // Choose a Wormadam with damage
            return store.prompt(state, new game_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { allowCancel: true }), sourceTargets => {
                if (!sourceTargets || sourceTargets.length === 0) {
                    return;
                }
                const source = sourceTargets[0];
                const sourceCard = source.getPokemonCard();
                if (!sourceCard || sourceCard.name !== 'Wormadam' || source.damage <= 0) {
                    return;
                }
                // Choose destination Pokemon
                store.prompt(state, new game_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { allowCancel: true }), destTargets => {
                    if (!destTargets || destTargets.length === 0) {
                        return;
                    }
                    const dest = destTargets[0];
                    if (dest === source) {
                        return;
                    }
                    source.damage -= 10;
                    dest.damage += 10;
                });
            });
        }
        return state;
    }
}
exports.Mothim = Mothim;
