"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.EnergyReset = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const discard_energy_prompt_1 = require("../../game/store/prompts/discard-energy-prompt");
function* playCard(next, store, state, effect) {
    const player = effect.player;
    // Check if any Pokemon has energy attached
    let hasEnergy = false;
    player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
        if (cardList.cards.some(c => c.superType === card_types_1.SuperType.ENERGY)) {
            hasEnergy = true;
        }
    });
    if (!hasEnergy) {
        throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
    }
    // Do not discard the card yet
    effect.preventDefault = true;
    player.hand.moveCardTo(effect.trainerCard, player.supporter);
    let transfers = [];
    yield store.prompt(state, new discard_energy_prompt_1.DiscardEnergyPrompt(player.id, game_1.GameMessage.CHOOSE_ENERGIES_TO_DISCARD, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY }, { allowCancel: false, min: 1, max: undefined }), result => {
        transfers = result || [];
        next();
    });
    // Move selected energy cards to hand instead of discard
    transfers.forEach(transfer => {
        const source = game_1.StateUtils.getTarget(state, player, transfer.from);
        source.moveCardTo(transfer.card, player.hand);
    });
    return state;
}
class EnergyReset extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.set = 'FCO';
        this.setNumber = '98';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Energy Reset';
        this.fullName = 'Energy Reset FCO';
        this.text = 'Put as many Energy attached to your Pokémon as you like into your hand.';
    }
    // Ref: set-phantom-forces/heliolisk.ts (DiscardEnergyPrompt for multi-Pokemon energy selection)
    reduceEffect(store, state, effect) {
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const generator = playCard(() => generator.next(), store, state, effect);
            return generator.next().value;
        }
        return state;
    }
}
exports.EnergyReset = EnergyReset;
