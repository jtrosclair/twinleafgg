"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.DiancieEx = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class DiancieEx extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_EX];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = Y;
        this.hp = 150;
        this.weakness = [{ type: M }];
        this.resistance = [{ type: D, value: -20 }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'Sparkle Veil',
                powerType: game_1.PowerType.ABILITY,
                text: 'As long as this Pokémon is your Active Pokémon, any damage done to your Pokémon by an opponent\'s attack is reduced by 30 (after applying Weakness and Resistance).'
            }];
        this.attacks = [
            {
                name: 'Wonder Stage',
                cost: [Y, C, C],
                damage: 60,
                damageCalculation: '+',
                text: 'If there is any Stadium card in play, this attack does 50 more damage.'
            }
        ];
        this.set = 'FCO';
        this.setNumber = '72';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Diancie-EX';
        this.fullName = 'Diancie-EX FCO';
    }
    reduceEffect(store, state, effect) {
        // Ability: Sparkle Veil
        // Ref: set-primal-clash/rhyperior-2.ts (Rock Wall - reduce damage to your Pokemon)
        if (effect instanceof attack_effects_1.DealDamageEffect || effect instanceof attack_effects_1.PutDamageEffect) {
            const targetCard = effect.target.getPokemonCard();
            if (!targetCard) {
                return state;
            }
            // Find the owner of the target
            let targetOwner = null;
            state.players.forEach(p => {
                p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (cardList === effect.target) {
                        targetOwner = p;
                    }
                });
            });
            if (!targetOwner) {
                return state;
            }
            // Check if Diancie-EX is the active Pokemon for the target owner
            if (targetOwner.active.getPokemonCard() !== this) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, targetOwner, this)) {
                return state;
            }
            // Only reduce damage from opponent's attacks
            const sourceOwner = effect.player;
            if (sourceOwner === targetOwner) {
                return state;
            }
            effect.damage = Math.max(0, effect.damage - 30);
        }
        // Attack 1: Wonder Stage
        // Ref: set-breakthrough/cresselia.ts (getStadiumCard check for bonus damage)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const stadiumCard = game_1.StateUtils.getStadiumCard(state);
            if (stadiumCard !== undefined) {
                effect.damage += 50;
            }
        }
        return state;
    }
}
exports.DiancieEx = DiancieEx;
