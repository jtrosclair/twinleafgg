"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Carbink = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const energy_card_1 = require("../../game/store/card/energy-card");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attack_effects_2 = require("../../game/store/prefabs/attack-effects");
class Carbink extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = F;
        this.hp = 80;
        this.weakness = [{ type: G }];
        this.retreat = [C];
        this.powers = [{
                name: 'Energy Keeper',
                powerType: game_1.PowerType.ABILITY,
                text: 'Basic Energy attached to your Basic Pokémon can\'t be discarded by effects of your opponent\'s attacks, Abilities, or Trainer cards.'
            }];
        this.attacks = [
            {
                name: 'Stone Edge',
                cost: [F, F, C],
                damage: 60,
                damageCalculation: '+',
                text: 'Flip a coin. If heads, this attack does 30 more damage.'
            }
        ];
        this.set = 'FCO';
        this.setNumber = '49';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Carbink';
        this.fullName = 'Carbink FCO';
    }
    reduceEffect(store, state, effect) {
        // Ability: Energy Keeper
        // Ref: set-primal-clash/rhyperior-2.ts (Rock Wall - passive ability checking in-play ownership)
        if (effect instanceof attack_effects_1.DiscardCardsEffect) {
            const targetCard = effect.target.getPokemonCard();
            if (!targetCard) {
                return state;
            }
            // Only protect Basic Pokemon
            if (targetCard.stage !== card_types_1.Stage.BASIC) {
                return state;
            }
            // Find the owner of the target
            let targetOwner = null;
            state.players.forEach(p => {
                p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (cardList === effect.target) {
                        targetOwner = p;
                    }
                });
            });
            if (!targetOwner) {
                return state;
            }
            // Check if Carbink is in play for the target owner
            let isInPlay = false;
            targetOwner.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card) => {
                if (card === this) {
                    isInPlay = true;
                }
            });
            if (!isInPlay) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, targetOwner, this)) {
                return state;
            }
            // Only protect from opponent's effects
            const sourceOwner = effect.player;
            if (sourceOwner === targetOwner) {
                return state;
            }
            // Filter out basic energy cards from being discarded
            const basicEnergyCards = effect.cards.filter(c => c instanceof energy_card_1.EnergyCard && c.energyType === card_types_1.EnergyType.BASIC);
            if (basicEnergyCards.length > 0) {
                // Remove basic energy from the discard list
                effect.cards = effect.cards.filter(c => !(c instanceof energy_card_1.EnergyCard && c.energyType === card_types_1.EnergyType.BASIC));
                // If no cards left to discard, prevent the effect entirely
                if (effect.cards.length === 0) {
                    effect.preventDefault = true;
                }
            }
        }
        // Attack 1: Stone Edge
        // Ref: set-phantom-forces/pachirisu.ts (Pachi)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            (0, attack_effects_2.FLIP_A_COIN_IF_HEADS_DEAL_MORE_DAMAGE)(store, state, effect, 30);
        }
        return state;
    }
}
exports.Carbink = Carbink;
