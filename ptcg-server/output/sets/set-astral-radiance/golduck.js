"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Golduck = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Golduck extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Psyduck';
        this.cardType = W;
        this.hp = 100;
        this.weakness = [{ type: L }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Aqua Edge',
                cost: [W, C],
                damage: 50,
                text: ''
            },
            {
                name: 'Entangled Dive',
                cost: [W, W, C],
                damage: 0,
                text: 'Discard each player\'s Active Pokémon and all attached cards. (You choose a new Active Pokémon first.)'
            }
        ];
        this.regulationMark = 'F';
        this.set = 'ASR';
        this.setNumber = '29';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Golduck';
        this.fullName = 'Golduck ASR 29';
    }
    reduceEffect(store, state, effect) {
        // Attack 2: Entangled Dive
        // Ref: set-pokemon-151/ditto.ts (moveTo player.discard to discard active + attached)
        // Ref: prefabs.ts SWITCH_ACTIVE_WITH_BENCHED (player chooses new active first)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Capture references to CURRENT actives BEFORE any switching
            const playerOriginalActive = player.active;
            const opponentOriginalActive = opponent.active;
            // Player chooses new Active Pokemon first (switch with bench)
            // Only switch if the player has a benched Pokemon to switch with
            if (player.bench.some(b => b.cards.length > 0)) {
                (0, prefabs_1.SWITCH_ACTIVE_WITH_BENCHED)(store, state, player);
            }
            // Discard the ORIGINAL active Pokemon and all attached cards
            // Tools must be moved first (moveTo does not move tools)
            playerOriginalActive.tools.slice().forEach(t => { playerOriginalActive.moveCardTo(t, player.discard); });
            playerOriginalActive.moveTo(player.discard);
            opponentOriginalActive.tools.slice().forEach(t => { opponentOriginalActive.moveCardTo(t, opponent.discard); });
            opponentOriginalActive.moveTo(opponent.discard);
        }
        return state;
    }
}
exports.Golduck = Golduck;
