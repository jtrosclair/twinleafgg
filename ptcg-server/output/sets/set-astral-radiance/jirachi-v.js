"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.JirachiV = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attach_energy_prompt_1 = require("../../game/store/prompts/attach-energy-prompt");
const play_card_action_1 = require("../../game/store/actions/play-card-action");
const game_effects_1 = require("../../game/store/effects/game-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attack_effects_1 = require("../../game/store/prefabs/attack-effects");
const pokemon_card_list_1 = require("../../game/store/state/pokemon-card-list");
class JirachiV extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_V];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = P;
        this.hp = 180;
        this.weakness = [{ type: D }];
        this.resistance = [{ type: F, value: -30 }];
        this.retreat = [C];
        this.powers = [{
                name: 'Wish Connector',
                powerType: game_1.PowerType.ABILITY,
                text: 'When 1 of your Basic Pokémon V is Knocked Out by damage from an attack from your opponent\'s Pokémon, you may move a basic Energy card from that Pokémon to another of your Pokémon.'
            }];
        this.attacks = [
            {
                name: 'Hypnostrike',
                cost: [P, C],
                damage: 60,
                text: 'Both Active Pokémon are now Asleep.'
            }
        ];
        this.regulationMark = 'F';
        this.set = 'ASR';
        this.setNumber = '170';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Jirachi V';
        this.fullName = 'Jirachi V ASR 170';
        this.WISH_CONNECTOR_MARKER = 'JIRACHI_V_ASR_WISH_CONNECTOR_MARKER';
    }
    reduceEffect(store, state, effect) {
        // Ability: Wish Connector
        // Ref: set-scarlet-and-violet/exp-share.ts (KnockOutEffect - move energy from KO'd Pokemon to another Pokemon)
        if (effect instanceof game_effects_1.KnockOutEffect) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const target = effect.target;
            // Only activate during opponent's attack phase
            if (state.phase !== game_1.GamePhase.ATTACK || state.players[state.activePlayer] !== opponent) {
                return state;
            }
            // The KO'd Pokemon must be a Basic Pokemon V
            const pokemonCard = target.getPokemonCard();
            if (!pokemonCard || !pokemonCard.tags.includes(card_types_1.CardTag.POKEMON_V) || pokemonCard.stage !== card_types_1.Stage.BASIC) {
                return state;
            }
            // Jirachi V must be on the player's bench (and not the one being KO'd)
            const isJirachiBenched = player.bench.some(b => b.getPokemonCard() === this && b !== target);
            // Also check if Jirachi V is in the active (if it's not the one being KO'd)
            const isJirachiActive = player.active.getPokemonCard() === this && player.active !== target;
            if (!isJirachiBenched && !isJirachiActive) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // Avoid double-triggering
            if (target.marker.hasMarker(this.WISH_CONNECTOR_MARKER, this)) {
                return state;
            }
            // Check if there are basic energy on the KO'd Pokemon
            const hasBasicEnergy = target.cards.some(c => c.superType === card_types_1.SuperType.ENERGY && c.energyType === card_types_1.EnergyType.BASIC);
            if (!hasBasicEnergy) {
                return state;
            }
            target.marker.addMarker(this.WISH_CONNECTOR_MARKER, this);
            // Make a copy of the KO'd Pokemon's card list for the prompt
            // (cards will be in discard when the prompt resolves)
            const targetCopy = new pokemon_card_list_1.PokemonCardList();
            targetCopy.cards = target.cards.slice();
            // Build blocked list - exclude the KO'd slot itself
            const blockedTo = [];
            player.forEachPokemon(play_card_action_1.PlayerType.BOTTOM_PLAYER, (cardList, card, t) => {
                if (cardList === target) {
                    blockedTo.push(t);
                }
            });
            state = store.prompt(state, new attach_energy_prompt_1.AttachEnergyPrompt(player.id, game_1.GameMessage.ATTACH_ENERGY_TO_BENCH, targetCopy, play_card_action_1.PlayerType.BOTTOM_PLAYER, [play_card_action_1.SlotType.ACTIVE, play_card_action_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC }, { allowCancel: true, min: 0, max: 1, differentTargets: false, blockedTo }), transfers => {
                transfers = transfers || [];
                target.marker.removeMarker(this.WISH_CONNECTOR_MARKER, this);
                for (const transfer of transfers) {
                    const t = game_1.StateUtils.getTarget(state, player, transfer.to);
                    player.discard.moveCardTo(transfer.card, t);
                }
            });
        }
        // Attack 1: Hypnostrike
        // Ref: set-phantom-forces/musharna.ts (Dream Dance - Both Active Pokémon are now Asleep)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            (0, attack_effects_1.YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_ASLEEP)(store, state, effect);
            (0, prefabs_1.ADD_SLEEP_TO_PLAYER_ACTIVE)(store, state, player, this);
        }
        return state;
    }
}
exports.JirachiV = JirachiV;
