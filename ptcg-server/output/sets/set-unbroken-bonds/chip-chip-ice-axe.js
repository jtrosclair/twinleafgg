"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.ChipChipIceAxe = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const trainer_prefabs_1 = require("../../game/store/prefabs/trainer-prefabs");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class ChipChipIceAxe extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.set = 'UNB';
        this.setNumber = '165';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Chip-Chip Ice Axe';
        this.fullName = 'Chip-Chip Ice Axe UNB';
        this.text = 'Look at the top 3 cards of your opponent\'s deck and choose 1 of them. Your opponent shuffles the other cards back into their deck. Then, put the card you chose on top of their deck.';
    }
    // Ref: set-guardians-rising/watchog.ts (Scrutinize - look at top cards, choose, rearrange)
    reduceEffect(store, state, effect) {
        if ((0, trainer_prefabs_1.WAS_TRAINER_USED)(effect, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if (opponent.deck.cards.length === 0) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            const topCards = new game_1.CardList();
            opponent.deck.moveTo(topCards, Math.min(3, opponent.deck.cards.length));
            return store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DECK, topCards, {}, { min: 1, max: 1, allowCancel: false }), (selected) => {
                const chosenCard = selected[0];
                // Shuffle the other cards back into opponent's deck
                const otherCards = topCards.cards.filter(c => c !== chosenCard);
                otherCards.forEach(c => {
                    topCards.moveCardTo(c, opponent.deck);
                });
                (0, prefabs_1.SHUFFLE_DECK)(store, state, opponent);
                // Put the chosen card on top of opponent's deck
                topCards.moveCardTo(chosenCard, opponent.deck);
                // moveCardTo puts it at bottom; move it to top
                const idx = opponent.deck.cards.indexOf(chosenCard);
                if (idx !== -1) {
                    opponent.deck.cards.splice(idx, 1);
                    opponent.deck.cards.unshift(chosenCard);
                }
            });
        }
        return state;
    }
}
exports.ChipChipIceAxe = ChipChipIceAxe;
