"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.AlolanDugtrio = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class AlolanDugtrio extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Alolan Diglett';
        this.cardType = M;
        this.hp = 80;
        this.weakness = [{ type: R }];
        this.resistance = [{ type: P, value: -20 }];
        this.retreat = [C];
        this.powers = [{
                name: 'Hair Wall',
                powerType: game_1.PowerType.ABILITY,
                text: 'Your Metal Pokémon take 10 less damage from your opponent\'s attacks (after applying Weakness and Resistance).'
            }];
        this.attacks = [
            {
                name: 'Hammer In',
                cost: [M, C],
                damage: 40,
                text: ''
            }
        ];
        this.set = 'UNB';
        this.setNumber = '122';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Alolan Dugtrio';
        this.fullName = 'Alolan Dugtrio UNB';
    }
    reduceEffect(store, state, effect) {
        // Ability: Hair Wall (passive - reduce damage to Metal Pokemon)
        // Ref: set-team-up/aegislash.ts (Royal Guard - passive damage reduction ability)
        if (effect instanceof attack_effects_1.DealDamageEffect && effect.target.cards.includes(this)) {
            // This card doesn't need to be the target - it reduces damage to all Metal Pokemon
        }
        if (effect instanceof attack_effects_1.DealDamageEffect) {
            const targetCard = effect.target.getPokemonCard();
            if (targetCard === undefined) {
                return state;
            }
            // Check if target is a Metal Pokemon
            const checkType = new check_effects_1.CheckPokemonTypeEffect(effect.target);
            store.reduceEffect(state, checkType);
            if (!checkType.cardTypes.includes(card_types_1.CardType.METAL)) {
                return state;
            }
            // Check if this Alolan Dugtrio is in play and belongs to the defending player
            const targetPlayer = game_1.StateUtils.findOwner(state, effect.target);
            // Check if this card is in play for the target player
            try {
                const myPlayer = game_1.StateUtils.findOwner(state, game_1.StateUtils.findCardList(state, this));
                if (myPlayer !== targetPlayer) {
                    return state;
                }
            }
            catch (_a) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, targetPlayer, this)) {
                return state;
            }
            effect.damage = Math.max(0, effect.damage - 10);
        }
        return state;
    }
}
exports.AlolanDugtrio = AlolanDugtrio;
