"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Gligar = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Gligar extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = F;
        this.hp = 60;
        this.weakness = [{ type: G }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'Free Flight',
                powerType: game_1.PowerType.ABILITY,
                text: 'If this Pokémon has no Energy attached to it, it has no Retreat Cost.'
            }];
        this.attacks = [
            {
                name: 'Shinobi Strike',
                cost: [C, C, C],
                damage: 30,
                damageCalculation: '+',
                text: 'If you played Janine from your hand during this turn, this attack does 90 more damage.'
            }
        ];
        this.set = 'UNB';
        this.setNumber = '98';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Gligar';
        this.fullName = 'Gligar UNB';
        this.JANINE_MARKER = 'GLIGAR_JANINE_MARKER';
    }
    reduceEffect(store, state, effect) {
        // Ability: Free Flight (passive - modify retreat cost)
        // Ref: set-paradox-rift/morpeko.ts (In A Hungry Hurry - no energy = no retreat cost)
        if (effect instanceof check_effects_1.CheckRetreatCostEffect && effect.player.active.cards.includes(this)) {
            const player = effect.player;
            const pokemonCard = player.active.getPokemonCard();
            if (pokemonCard !== this) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            const checkProvidedEnergy = new check_effects_1.CheckProvidedEnergyEffect(player);
            state = store.reduceEffect(state, checkProvidedEnergy);
            if (checkProvidedEnergy.energyMap.length === 0) {
                effect.cost = [];
            }
        }
        // Track if Janine was played this turn
        // Ref: set-ultra-prism/garchomp.ts (Royal Blades - tracking supporter played this turn)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard.name === 'Janine') {
            const player = effect.player;
            (0, prefabs_1.ADD_MARKER)(this.JANINE_MARKER, player, this);
        }
        if (effect instanceof game_phase_effects_1.EndTurnEffect && effect.player.marker.hasMarker(this.JANINE_MARKER, this)) {
            effect.player.marker.removeMarker(this.JANINE_MARKER, this);
        }
        // Attack 1: Shinobi Strike
        // Ref: set-ultra-prism/garchomp.ts (Royal Blades - supporter bonus damage)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            if ((0, prefabs_1.HAS_MARKER)(this.JANINE_MARKER, player, this)) {
                effect.damage += 90;
            }
        }
        return state;
    }
}
exports.Gligar = Gligar;
