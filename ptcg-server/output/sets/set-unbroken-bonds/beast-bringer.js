"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.BeastBringer = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_effects_1 = require("../../game/store/effects/game-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class BeastBringer extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.TOOL;
        this.set = 'UNB';
        this.setNumber = '164';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Beast Bringer';
        this.fullName = 'Beast Bringer UNB';
        this.text = 'If you have exactly 6 Prize cards remaining, and if your opponent\'s Active Pokémon-GX or Pokémon-EX is Knocked Out by damage from an attack of the Ultra Beast this card is attached to, take 1 more Prize card.';
    }
    // Ref: set-team-up/black-market.ts (Black Market - KnockOutEffect prize modification)
    reduceEffect(store, state, effect) {
        if (effect instanceof game_effects_1.KnockOutEffect) {
            // Find which Pokemon this tool is attached to
            const attachedTo = game_1.StateUtils.findCardList(state, this);
            if (!attachedTo || !attachedTo.tools || !attachedTo.tools.includes(this)) {
                return state;
            }
            const toolOwner = game_1.StateUtils.findOwner(state, attachedTo);
            if ((0, prefabs_1.IS_TOOL_BLOCKED)(store, state, toolOwner, this)) {
                return state;
            }
            // Check if tool owner has exactly 6 prize cards remaining
            const prizeCount = toolOwner.prizes.filter(p => p.cards.length > 0).length;
            if (prizeCount !== 6) {
                return state;
            }
            // Check if the attached Pokemon is an Ultra Beast
            const attachedPokemon = attachedTo.getPokemonCard();
            if (!attachedPokemon || !attachedPokemon.tags.includes(card_types_1.CardTag.ULTRA_BEAST)) {
                return state;
            }
            // Check if the KO target is opponent's active GX or EX
            const targetCard = effect.target.getPokemonCard();
            if (!targetCard) {
                return state;
            }
            if (!targetCard.tags.includes(card_types_1.CardTag.POKEMON_GX) && !targetCard.tags.includes(card_types_1.CardTag.POKEMON_EX)) {
                return state;
            }
            // Check that the KO target belongs to the opponent
            const targetOwner = game_1.StateUtils.findOwner(state, effect.target);
            if (targetOwner === toolOwner) {
                return state;
            }
            // Check that the target is the active Pokemon
            if (effect.target !== targetOwner.active) {
                return state;
            }
            effect.prizeCount += 1;
        }
        return state;
    }
}
exports.BeastBringer = BeastBringer;
