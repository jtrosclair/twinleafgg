"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Electrode = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const choose_pokemon_prompt_1 = require("../../game/store/prompts/choose-pokemon-prompt");
const check_effect_1 = require("../../game/store/effect-reducers/check-effect");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Electrode extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Voltorb';
        this.cardType = L;
        this.hp = 80;
        this.weakness = [{ type: F }];
        this.resistance = [{ type: M, value: -20 }];
        this.retreat = [C];
        this.powers = [{
                name: 'Buzzap Thunder',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'Once during your turn (before your attack), you may Knock Out this Pokémon and attach it to one of your Lightning Pokémon as a Special Energy card. This card provides 2 [L] Energy only while this card is attached to a Pokémon.'
            }];
        this.attacks = [
            {
                name: 'Head Bolt',
                cost: [L, L, C],
                damage: 70,
                text: ''
            }
        ];
        this.set = 'EVO';
        this.setNumber = '40';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Electrode';
        this.fullName = 'Electrode EVO';
        // EnergyCard interface properties
        this.provides = [card_types_1.CardType.LIGHTNING, card_types_1.CardType.LIGHTNING];
        this.energyType = card_types_1.EnergyType.SPECIAL;
        this.text = '';
        this.isBlocked = false;
        this.blendedEnergies = [];
        this.blendedEnergyCount = 1;
        this.energyEffect = undefined;
    }
    reduceEffect(store, state, effect) {
        // Ability: Buzzap Thunder
        // Ref: set-base-set/electrode.ts (Buzzap)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            // Check if there's at least one Lightning Pokemon to attach to
            let hasLightningTarget = false;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                var _a;
                if (cardList.getPokemonCard() !== this && ((_a = cardList.getPokemonCard()) === null || _a === void 0 ? void 0 : _a.cardType) === card_types_1.CardType.LIGHTNING) {
                    hasLightningTarget = true;
                }
            });
            if (!hasLightningTarget) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_USE_POWER);
            }
            // KO this Pokemon
            const cardList = game_1.StateUtils.findCardList(state, this);
            cardList.damage = 999;
            state = (0, check_effect_1.checkState)(store, state);
            if (store.hasPrompts()) {
                state = store.waitPrompt(state, () => { });
            }
            // Build blocked list for non-Lightning Pokemon
            const blocked = [];
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card, target) => {
                if (card.cardType !== card_types_1.CardType.LIGHTNING) {
                    blocked.push(target);
                }
            });
            // Choose a Lightning Pokemon to attach to
            return store.prompt(state, new choose_pokemon_prompt_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON_TO_ATTACH_CARDS, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { allowCancel: false, blocked }), targets => {
                if (!targets || targets.length === 0) {
                    return;
                }
                // Move Electrode to the target Pokemon as energy
                effect.preventDefault = true;
                const sourceList = game_1.StateUtils.findCardList(state, this);
                if (sourceList) {
                    sourceList.moveCardTo(this, targets[0]);
                    if (!targets[0].energies.cards.includes(this)) {
                        targets[0].energies.cards.push(this);
                    }
                }
            });
        }
        // Provide energy when attached as energy
        if (effect instanceof check_effects_1.CheckProvidedEnergyEffect && effect.source.energies.cards.includes(this)) {
            effect.energyMap.push({ card: this, provides: this.provides.slice() });
        }
        return state;
    }
}
exports.Electrode = Electrode;
