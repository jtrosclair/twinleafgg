"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Rhyperior2 = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Rhyperior2 extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_2;
        this.evolvesFrom = 'Rhydon';
        this.cardType = F;
        this.hp = 160;
        this.weakness = [{ type: G }];
        this.retreat = [C, C, C, C];
        this.powers = [{
                name: 'Rock Wall',
                powerType: game_1.PowerType.ABILITY,
                text: 'Any damage done to your Pokémon by an opponent\'s attack is reduced by 10 (after applying Weakness and Resistance).'
            }];
        this.attacks = [
            {
                name: 'Hammer Arm',
                cost: [F, F, C, C],
                damage: 100,
                text: 'Discard the top card of your opponent\'s deck.'
            }
        ];
        this.set = 'PRC';
        this.setNumber = '77';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Rhyperior';
        this.fullName = 'Rhyperior PRC 77';
    }
    reduceEffect(store, state, effect) {
        // Ability: Rock Wall
        // Ref: set-ex-dragon-frontiers/kingdra-ex.ts (Extra Smoke - reduce damage to your Pokemon)
        if (effect instanceof attack_effects_1.DealDamageEffect || effect instanceof attack_effects_1.PutDamageEffect) {
            const targetCard = effect.target.getPokemonCard();
            if (!targetCard) {
                return state;
            }
            // Find the owner of the target
            let targetOwner = null;
            state.players.forEach(p => {
                p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (cardList === effect.target) {
                        targetOwner = p;
                    }
                });
            });
            if (!targetOwner) {
                return state;
            }
            // Check if Rhyperior is in play for the target owner
            let isInPlay = false;
            targetOwner.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card) => {
                if (card === this) {
                    isInPlay = true;
                }
            });
            if (!isInPlay) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, targetOwner, this)) {
                return state;
            }
            // Only reduce damage from opponent's attacks
            const sourceOwner = effect.player;
            if (sourceOwner === targetOwner) {
                return state;
            }
            effect.damage = Math.max(0, effect.damage - 10);
        }
        // Attack 1: Hammer Arm
        // Ref: set-dragons-exalted/giratina-ex.ts (Dragon Pulse - discard top cards)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            (0, prefabs_1.MOVE_CARDS)(store, state, opponent.deck, opponent.discard, { count: 1, sourceCard: this, sourceEffect: effect });
        }
        return state;
    }
}
exports.Rhyperior2 = Rhyperior2;
