"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.ProfessorBirchsObservations = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const shuffle_prompt_1 = require("../../game/store/prompts/shuffle-prompt");
function* playCard(next, store, state, self, effect) {
    const player = effect.player;
    const cards = player.hand.cards.filter(c => c !== self);
    // Shuffle hand into deck (except for this card, which will be discarded by default)
    if (cards.length > 0) {
        player.hand.moveCardsTo(cards, player.deck);
    }
    // Shuffle deck
    yield store.prompt(state, new shuffle_prompt_1.ShuffleDeckPrompt(player.id), order => {
        player.deck.applyOrder(order);
        next();
    });
    // Flip coin: heads = draw 7, tails = draw 4
    yield store.prompt(state, [
        new game_1.CoinFlipPrompt(player.id, game_1.GameMessage.COIN_FLIP)
    ], result => {
        player.deck.moveTo(player.hand, result ? 7 : 4);
        next();
    });
    return state;
}
class ProfessorBirchsObservations extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.set = 'PRC';
        this.setNumber = '134';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Professor Birch\'s Observations';
        this.fullName = 'Professor Birch\'s Observations PRC';
        this.text = 'Shuffle your hand into your deck and flip a coin. If heads, draw 7 cards. If tails, draw 4 cards. You may play only 1 Supporter card during your turn (before your attack).';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-surging-sparks/drasna.ts (shuffle hand into deck + coin flip draw)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const generator = playCard(() => generator.next(), store, state, this, effect);
            return generator.next().value;
        }
        return state;
    }
}
exports.ProfessorBirchsObservations = ProfessorBirchsObservations;
