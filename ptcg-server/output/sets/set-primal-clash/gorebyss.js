"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Gorebyss = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Gorebyss extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Clamperl';
        this.cardType = W;
        this.hp = 80;
        this.weakness = [{ type: G }];
        this.retreat = [];
        this.usedCaptivate = false;
        this.attacks = [
            {
                name: 'Captivate',
                cost: [C],
                damage: 0,
                text: 'Switch 1 of your opponent\'s Benched Pokémon with his or her Active Pokémon.'
            },
            {
                name: 'Psychic',
                cost: [W, C],
                damage: 30,
                damageCalculation: '+',
                text: 'This attack does 10 more damage for each Energy attached to your opponent\'s Active Pokémon.'
            }
        ];
        this.set = 'PRC';
        this.setNumber = '51';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Gorebyss';
        this.fullName = 'Gorebyss PRC';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Captivate
        // Ref: set-phantom-forces/galvantula.ts (Cobweb Trip - post-damage switch opponent)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const opponent = game_1.StateUtils.getOpponent(state, effect.player);
            if (opponent.bench.some(b => b.cards.length > 0)) {
                this.usedCaptivate = true;
            }
        }
        if (effect instanceof game_phase_effects_1.AfterAttackEffect && this.usedCaptivate) {
            this.usedCaptivate = false;
            const opponent = game_1.StateUtils.getOpponent(state, effect.player);
            if (opponent.bench.some(b => b.cards.length > 0)) {
                (0, prefabs_1.SWITCH_ACTIVE_WITH_BENCHED)(store, state, opponent);
            }
        }
        if (effect instanceof game_phase_effects_1.EndTurnEffect && this.usedCaptivate) {
            this.usedCaptivate = false;
        }
        // Attack 2: Psychic
        // Ref: set-phantom-forces/jellicent.ts (Ensnaring Spray)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const checkEnergy = new check_effects_1.CheckProvidedEnergyEffect(opponent, opponent.active);
            store.reduceEffect(state, checkEnergy);
            let energyCount = 0;
            checkEnergy.energyMap.forEach(em => {
                energyCount += em.provides.length;
            });
            effect.damage += 10 * energyCount;
        }
        return state;
    }
}
exports.Gorebyss = Gorebyss;
