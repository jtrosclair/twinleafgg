"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.DittoV = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class DittoV extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_V];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = C;
        this.hp = 170;
        this.weakness = [{ type: F }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'V Transformation',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'Once during your turn, you may choose a Basic Pokémon V from your discard pile and switch it with this Pokémon. Any attached cards, damage counters, Special Conditions, turns in play, and any other effects remain on the new Pokémon.'
            }];
        this.attacks = [
            {
                name: 'Stick On',
                cost: [C],
                damage: 0,
                text: 'Attach a basic Energy card from your discard pile to this Pokémon.'
            }
        ];
        this.regulationMark = 'D';
        this.set = 'SHF';
        this.setNumber = 'SV118';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Ditto V';
        this.fullName = 'Ditto V SHF';
        this.V_TRANSFORMATION_MARKER = 'DITTO_V_SHF_V_TRANSFORMATION_MARKER';
    }
    reduceEffect(store, state, effect) {
        // Ability: V Transformation
        // Ref: set-evolving-skies/zoroark.ts (Phantom Transformation - choose from discard, swap with this Pokemon)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            // Check if there's a Basic V in discard (not Ditto V itself)
            const hasBasicVInDiscard = player.discard.cards.some(c => {
                return c instanceof pokemon_card_1.PokemonCard
                    && c.stage === card_types_1.Stage.BASIC
                    && c.tags.includes(card_types_1.CardTag.POKEMON_V)
                    && c !== this;
            });
            if (!hasBasicVInDiscard) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_USE_POWER);
            }
            (0, prefabs_1.USE_ABILITY_ONCE_PER_TURN)(player, this.V_TRANSFORMATION_MARKER, this);
            (0, prefabs_1.ABILITY_USED)(player, this);
            // Find where this Ditto V is (active or bench)
            const getBenchIndex = (card) => {
                for (let i = 0; i < player.bench.length; i++) {
                    if (player.bench[i].cards.includes(card)) {
                        return i;
                    }
                }
                return -1;
            };
            const benchIndex = getBenchIndex(this);
            // Blocked indices: non-Basic-V cards in discard
            const blocked = [];
            player.discard.cards.forEach((card, index) => {
                if (!(card instanceof pokemon_card_1.PokemonCard)
                    || card.stage !== card_types_1.Stage.BASIC
                    || !card.tags.includes(card_types_1.CardTag.POKEMON_V)
                    || card === this) {
                    blocked.push(index);
                }
            });
            return store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_POKEMON_TO_DISCARD, player.discard, {}, { min: 1, max: 1, allowCancel: false, blocked }), selected => {
                const cards = selected || [];
                if (cards.length === 0)
                    return;
                const replacement = cards[0];
                if (benchIndex >= 0) {
                    player.bench[benchIndex].moveCardTo(this, player.discard);
                    player.discard.moveCardTo(replacement, player.bench[benchIndex]);
                }
                else {
                    player.active.moveCardTo(this, player.discard);
                    player.discard.moveCardTo(replacement, player.active);
                }
            });
        }
        (0, prefabs_1.REMOVE_MARKER_AT_END_OF_TURN)(effect, this.V_TRANSFORMATION_MARKER, this);
        // Attack 1: Stick On
        // Ref: set-vivid-voltage/swellow.ts (ATTACH_X_TYPE_ENERGY_FROM_DISCARD_TO_1_OF_YOUR_POKEMON)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            (0, prefabs_1.ATTACH_X_TYPE_ENERGY_FROM_DISCARD_TO_1_OF_YOUR_POKEMON)(store, state, player, 1, undefined, {
                destinationSlots: [game_1.SlotType.ACTIVE],
                energyFilter: { energyType: card_types_1.EnergyType.BASIC },
                min: 0
            });
        }
        return state;
    }
}
exports.DittoV = DittoV;
