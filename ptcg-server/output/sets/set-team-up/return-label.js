"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.ReturnLabel = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
class ReturnLabel extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.set = 'TEU';
        this.setNumber = '153';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Return Label';
        this.fullName = 'Return Label TEU';
        this.text = 'Put a card from your opponent\'s discard pile on the bottom of their deck.';
    }
    // Ref: set-lost-thunder/morty.ts (choose from opponent's zone pattern)
    reduceEffect(store, state, effect) {
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if (opponent.discard.cards.length === 0) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DECK, opponent.discard, {}, { min: 1, max: 1, allowCancel: false }), selected => {
                const cards = selected || [];
                if (cards.length > 0) {
                    // Move to deck (moveCardTo puts at top by default, we need bottom)
                    opponent.discard.moveCardTo(cards[0], opponent.deck);
                    // Move from top to bottom: card was placed at beginning, move to end
                    const card = opponent.deck.cards.shift();
                    if (card) {
                        opponent.deck.cards.push(card);
                    }
                }
            });
        }
        return state;
    }
}
exports.ReturnLabel = ReturnLabel;
