"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.LatiasAndLatiosGx = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const check_effects_1 = require("../../game/store/effects/check-effects");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const costs_1 = require("../../game/store/prefabs/costs");
class LatiasAndLatiosGx extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.TAG_TEAM, card_types_1.CardTag.POKEMON_GX];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = N;
        this.hp = 250;
        this.weakness = [{ type: Y }];
        this.retreat = [C];
        this.AERO_UNIT_MARKER = 'LATIAS_LATIOS_GX_AERO_UNIT_MARKER';
        this.CLEAR_AERO_UNIT_MARKER = 'LATIAS_LATIOS_GX_CLEAR_AERO_UNIT_MARKER';
        this.attacks = [
            {
                name: 'Buster Purge',
                cost: [W, P, P, C],
                damage: 240,
                text: 'Discard 3 Energy from this Pokémon.'
            },
            {
                name: 'Aero Unit-GX',
                cost: [P],
                damage: 0,
                text: 'Attach 5 basic Energy cards from your discard pile to your Pokémon in any way you like. If this Pokémon has at least 1 extra Energy attached to it (in addition to this attack\'s cost), prevent all effects of attacks, including damage, done to it during your opponent\'s next turn. (You can\'t use more than 1 GX attack in a game.)'
            }
        ];
        this.set = 'TEU';
        this.setNumber = '113';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Latias & Latios-GX';
        this.fullName = 'Latias & Latios-GX TEU';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Buster Purge
        // Ref: set-lost-thunder/stunfisk.ts (DISCARD_X_ENERGY_FROM_THIS_POKEMON)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            (0, costs_1.DISCARD_X_ENERGY_FROM_THIS_POKEMON)(store, state, effect, 3);
        }
        // Attack 2: Aero Unit-GX
        // Refs: set-lost-thunder/sceptile-gx.ts (GX attack), set-ultra-prism/purugly.ts (prevent all effects marker)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            (0, prefabs_1.BLOCK_IF_GX_ATTACK_USED)(player);
            player.usedGX = true;
            // Check if this Pokemon has extra energy beyond the 1 [P] cost
            const checkEnergy = new check_effects_1.CheckProvidedEnergyEffect(player, player.active);
            store.reduceEffect(state, checkEnergy);
            const totalEnergy = checkEnergy.energyMap.reduce((sum, em) => sum + em.provides.length, 0);
            const hasExtraEnergy = totalEnergy >= 2;
            // Attach up to 5 basic Energy from discard
            const basicEnergyInDiscard = player.discard.cards.filter(c => c instanceof game_1.EnergyCard && c.energyType === card_types_1.EnergyType.BASIC);
            if (basicEnergyInDiscard.length > 0) {
                const maxAttach = Math.min(5, basicEnergyInDiscard.length);
                state = store.prompt(state, new game_1.AttachEnergyPrompt(player.id, game_1.GameMessage.ATTACH_ENERGY_CARDS, player.discard, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC }, { allowCancel: false, min: maxAttach, max: 5 }), transfers => {
                    transfers = transfers || [];
                    for (const transfer of transfers) {
                        const target = game_1.StateUtils.getTarget(state, player, transfer.to);
                        player.discard.moveCardTo(transfer.card, target);
                    }
                });
            }
            // If extra energy, set prevention markers
            if (hasExtraEnergy) {
                player.active.marker.addMarker(this.AERO_UNIT_MARKER, this);
                opponent.marker.addMarker(this.CLEAR_AERO_UNIT_MARKER, this);
            }
        }
        // Prevent all effects of attacks including damage
        if (effect instanceof attack_effects_1.AbstractAttackEffect && effect.target.cards.includes(this)) {
            const pokemonCard = effect.target.getPokemonCard();
            if (pokemonCard !== this) {
                return state;
            }
            if (!effect.target.marker.hasMarker(this.AERO_UNIT_MARKER, this)) {
                return state;
            }
            const player = game_1.StateUtils.findOwner(state, effect.target);
            const attacker = game_1.StateUtils.findOwner(state, effect.source);
            if (player === attacker) {
                return state;
            }
            effect.preventDefault = true;
        }
        // Cleanup at end of opponent's turn
        if (effect instanceof game_phase_effects_1.EndTurnEffect
            && effect.player.marker.hasMarker(this.CLEAR_AERO_UNIT_MARKER, this)) {
            effect.player.marker.removeMarker(this.CLEAR_AERO_UNIT_MARKER, this);
            const opponent = game_1.StateUtils.getOpponent(state, effect.player);
            opponent.forEachPokemon(game_1.PlayerType.TOP_PLAYER, cardList => {
                cardList.marker.removeMarker(this.AERO_UNIT_MARKER, this);
            });
        }
        return state;
    }
}
exports.LatiasAndLatiosGx = LatiasAndLatiosGx;
