"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Electrocharger = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Electrocharger extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.set = 'TEU';
        this.setNumber = '139';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Electrocharger';
        this.fullName = 'Electrocharger TEU';
        this.text = 'Flip 2 coins. For each heads, shuffle an Electropower card from your discard pile into your deck.';
    }
    // Ref: set-unified-minds/beheeyem.ts (trainer effect pattern), AGENTS-patterns.md (coin flips)
    reduceEffect(store, state, effect) {
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            // Check if there are any Electropower cards in the discard pile
            const electropowerCards = player.discard.cards.filter(c => c instanceof trainer_card_1.TrainerCard && c.name === 'Electropower');
            if (electropowerCards.length === 0) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            (0, prefabs_1.MULTIPLE_COIN_FLIPS_PROMPT)(store, state, player, 2, results => {
                const heads = results.filter(r => r).length;
                if (heads === 0) {
                    return;
                }
                const availableCards = player.discard.cards.filter(c => c instanceof trainer_card_1.TrainerCard && c.name === 'Electropower');
                const maxToShuffle = Math.min(heads, availableCards.length);
                if (maxToShuffle === 0) {
                    return;
                }
                store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DECK, player.discard, { name: 'Electropower' }, { min: maxToShuffle, max: maxToShuffle, allowCancel: false }), (selected) => {
                    const cards = selected || [];
                    cards.forEach(c => {
                        player.discard.moveCardTo(c, player.deck);
                    });
                    if (cards.length > 0) {
                        (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                    }
                });
            });
        }
        return state;
    }
}
exports.Electrocharger = Electrocharger;
