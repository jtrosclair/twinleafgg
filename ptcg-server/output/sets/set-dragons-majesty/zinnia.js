"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Zinnia = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const attach_energy_prompt_1 = require("../../game/store/prompts/attach-energy-prompt");
const check_effects_1 = require("../../game/store/effects/check-effects");
const game_effects_1 = require("../../game/store/effects/game-effects");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const state_1 = require("../../game/store/state/state");
function* playCard(next, store, state, self, effect) {
    const player = effect.player;
    // Check if KO marker is set
    if (!player.marker.hasMarker(self.ZINNIA_MARKER)) {
        throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
    }
    // Check if there are basic energy in hand (other than this card)
    const hasBasicEnergy = player.hand.cards.some(c => c instanceof game_1.EnergyCard && c.energyType === card_types_1.EnergyType.BASIC && c !== self);
    // Check if there is a Dragon Pokemon in play
    let hasDragon = false;
    player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
        const checkType = new check_effects_1.CheckPokemonTypeEffect(cardList);
        store.reduceEffect(state, checkType);
        if (checkType.cardTypes.includes(card_types_1.CardType.DRAGON)) {
            hasDragon = true;
        }
    });
    if (!hasBasicEnergy || !hasDragon) {
        throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
    }
    // Move to supporter zone
    player.hand.moveCardTo(effect.trainerCard, player.supporter);
    effect.preventDefault = true;
    // Filter slots that don't have Dragon Pokemon
    const blockedTo = [];
    player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card, target) => {
        const checkType = new check_effects_1.CheckPokemonTypeEffect(cardList);
        store.reduceEffect(state, checkType);
        if (!checkType.cardTypes.includes(card_types_1.CardType.DRAGON)) {
            blockedTo.push(target);
        }
    });
    yield store.prompt(state, new attach_energy_prompt_1.AttachEnergyPrompt(player.id, game_1.GameMessage.ATTACH_ENERGY_CARDS, player.hand, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC }, { allowCancel: false, min: 0, max: 2, sameTarget: true, blockedTo }), transfers => {
        transfers = transfers || [];
        for (const transfer of transfers) {
            const target = game_1.StateUtils.getTarget(state, player, transfer.to);
            player.hand.moveCardTo(transfer.card, target);
        }
        next();
    });
    return state;
}
class Zinnia extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.set = 'DRM';
        this.setNumber = '64';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Zinnia';
        this.fullName = 'Zinnia DRM';
        this.text = 'You can play this card only if 1 of your Pokémon was Knocked Out during your opponent\'s last turn.\n\nAttach up to 2 basic Energy cards from your hand to 1 of your Dragon Pokémon.';
        this.ZINNIA_MARKER = 'ZINNIA_MARKER';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-twilight-masquerade/hassel.ts (KnockOutEffect marker + Supporter pattern)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const generator = playCard(() => generator.next(), store, state, this, effect);
            return generator.next().value;
        }
        if (effect instanceof game_effects_1.KnockOutEffect) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const duringTurn = [state_1.GamePhase.PLAYER_TURN, state_1.GamePhase.ATTACK].includes(state.phase);
            // Do not activate between turns, or when it's not opponent's turn.
            if (!duringTurn || state.players[state.activePlayer] !== opponent) {
                return state;
            }
            const cardList = game_1.StateUtils.findCardList(state, this);
            const owner = game_1.StateUtils.findOwner(state, cardList);
            if (owner === player) {
                effect.player.marker.addMarker(this.ZINNIA_MARKER, this);
            }
            return state;
        }
        if (effect instanceof game_phase_effects_1.EndTurnEffect && effect.player.marker.hasMarker(this.ZINNIA_MARKER, this)) {
            effect.player.marker.removeMarker(this.ZINNIA_MARKER, this);
        }
        return state;
    }
}
exports.Zinnia = Zinnia;
