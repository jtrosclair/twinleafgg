"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Relicanth = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
class Relicanth extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = W;
        this.hp = 90;
        this.weakness = [{ type: L }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Fossil Search',
                cost: [C],
                damage: 0,
                text: 'Search your deck for up to 2 Rare Fossil cards and put them onto your Bench. Then, shuffle your deck.'
            },
            {
                name: 'Surf',
                cost: [W, C],
                damage: 40,
                text: ''
            }
        ];
        this.regulationMark = 'D';
        this.set = 'DAA';
        this.setNumber = '40';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Relicanth';
        this.fullName = 'Relicanth DAA';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Fossil Search
        // Ref: set-furious-fists/fossil-researcher.ts (search deck for fossils, put on bench via PlayPokemonEffect)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            if (player.deck.cards.length === 0) {
                return state;
            }
            const slots = player.bench.filter(b => b.cards.length === 0);
            if (slots.length === 0) {
                return state;
            }
            // Block all cards that are not Rare Fossil
            const blocked = [];
            player.deck.cards.forEach((card, index) => {
                if (card.name !== 'Rare Fossil') {
                    blocked.push(index);
                }
            });
            const max = Math.min(2, slots.length);
            store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_PUT_ONTO_BENCH, player.deck, {}, { min: 0, max, allowCancel: false, blocked }), selected => {
                const cards = selected || [];
                cards.forEach((card, index) => {
                    if (index < slots.length) {
                        const playPokemonEffect = new play_card_effects_1.PlayPokemonEffect(player, card, slots[index]);
                        store.reduceEffect(state, playPokemonEffect);
                    }
                });
                (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            });
        }
        return state;
    }
}
exports.Relicanth = Relicanth;
