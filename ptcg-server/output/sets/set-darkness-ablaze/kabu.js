"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Kabu = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const shuffle_prompt_1 = require("../../game/store/prompts/shuffle-prompt");
class Kabu extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.regulationMark = 'D';
        this.set = 'DAA';
        this.setNumber = '163';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Kabu';
        this.fullName = 'Kabu DAA';
        this.text = 'Shuffle your hand into your deck. Then, draw 4 cards. If your Active Pokémon is your only Pokémon in play, draw 8 cards instead. You may play only 1 Supporter card during your turn.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-ultra-prism/cynthia.ts (shuffle hand into deck and draw)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            if (player.supporterTurn > 0) {
                throw new game_1.GameError(game_1.GameMessage.SUPPORTER_ALREADY_PLAYED);
            }
            player.hand.moveCardTo(this, player.supporter);
            effect.preventDefault = true;
            // Check if Active is only Pokemon in play (no benched Pokemon)
            const hasBenched = player.bench.some(b => b.cards.length > 0);
            const drawCount = hasBenched ? 4 : 8;
            const cards = player.hand.cards.filter(c => c !== this);
            if (cards.length > 0) {
                player.hand.moveCardsTo(cards, player.deck);
            }
            return store.prompt(state, new shuffle_prompt_1.ShuffleDeckPrompt(player.id), order => {
                player.deck.applyOrder(order);
                player.deck.moveTo(player.hand, Math.min(drawCount, player.deck.cards.length));
                player.supporter.moveCardTo(this, player.discard);
            });
        }
        return state;
    }
}
exports.Kabu = Kabu;
