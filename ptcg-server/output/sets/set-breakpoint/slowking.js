"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Slowking = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Slowking extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Slowpoke';
        this.cardType = W;
        this.hp = 100;
        this.weakness = [{ type: G }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'Royal Flash',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'Once during your turn (before your attack), you may flip a coin. If heads, move an Energy from your opponent\'s Active Pokémon to 1 of his or her Benched Pokémon.'
            }];
        this.attacks = [
            {
                name: 'Psych Up',
                cost: [W, C],
                damage: 40,
                text: 'During your next turn, this Pokémon\'s Psych Up attack does 40 more damage (before applying Weakness and Resistance).'
            }
        ];
        this.set = 'BKP';
        this.setNumber = '21';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Slowking';
        this.fullName = 'Slowking BKP';
        this.ROYAL_FLASH_MARKER = 'SLOWKING_BKP_ROYAL_FLASH_MARKER';
        this.PSYCH_UP_MARKER = 'SLOWKING_BKP_PSYCH_UP_MARKER';
        this.PSYCH_UP_CLEAR_MARKER = 'SLOWKING_BKP_PSYCH_UP_CLEAR_MARKER';
    }
    reduceEffect(store, state, effect) {
        // Ability: Royal Flash
        // Refs: set-furious-fists/sylveon.ts (Curly Ribbon - move energy from opponent's active to bench), AGENTS-patterns.md (once per turn ability)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            // Check opponent has energy on active and benched Pokemon
            const hasEnergy = opponent.active.cards.some(c => c instanceof game_1.EnergyCard);
            const hasBenched = opponent.bench.some(b => b.cards.length > 0);
            if (!hasEnergy || !hasBenched) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_USE_POWER);
            }
            (0, prefabs_1.USE_ABILITY_ONCE_PER_TURN)(player, this.ROYAL_FLASH_MARKER, this);
            (0, prefabs_1.ABILITY_USED)(player, this);
            (0, prefabs_1.COIN_FLIP_PROMPT)(store, state, player, result => {
                if (result) {
                    // Choose an energy from opponent's active
                    store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_ATTACH, opponent.active, { superType: card_types_1.SuperType.ENERGY }, { min: 1, max: 1, allowCancel: false }), selected => {
                        const cards = selected || [];
                        if (cards.length === 0) {
                            return;
                        }
                        // Choose a benched Pokemon to move the energy to
                        store.prompt(state, new game_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON_TO_ATTACH_CARDS, game_1.PlayerType.TOP_PLAYER, [game_1.SlotType.BENCH], { min: 1, max: 1, allowCancel: false }), targets => {
                            if (targets && targets.length > 0) {
                                opponent.active.moveCardTo(cards[0], targets[0]);
                            }
                        });
                    });
                }
            });
        }
        (0, prefabs_1.REMOVE_MARKER_AT_END_OF_TURN)(effect, this.ROYAL_FLASH_MARKER, this);
        // Attack 1: Psych Up
        // Ref: set-breakthrough/swoobat.ts (Wave Amplification - NEXT_TURN_ATTACK_BONUS)
        (0, prefabs_1.NEXT_TURN_ATTACK_BONUS)(effect, {
            attack: this.attacks[0],
            source: this,
            bonusDamage: 40,
            bonusMarker: this.PSYCH_UP_MARKER,
            clearMarker: this.PSYCH_UP_CLEAR_MARKER
        });
        return state;
    }
}
exports.Slowking = Slowking;
