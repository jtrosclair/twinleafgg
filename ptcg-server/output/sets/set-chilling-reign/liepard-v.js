"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.LiepardV = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attack_effects_1 = require("../../game/store/prefabs/attack-effects");
class LiepardV extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_V];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = D;
        this.hp = 190;
        this.weakness = [{ type: G }];
        this.retreat = [C];
        this.powers = [{
                name: 'Hidden Claw',
                powerType: game_1.PowerType.ABILITY,
                text: 'When you play this Pokémon from your hand onto your Bench during your turn, you may discard a Pokémon Tool from a Pokémon (yours or your opponent\'s).'
            }];
        this.attacks = [
            {
                name: 'Shadow Ripper',
                cost: [D, C, C],
                damage: 110,
                text: 'You may put this Pokémon and all attached cards into your hand.'
            }
        ];
        this.regulationMark = 'E';
        this.set = 'CRE';
        this.setNumber = '104';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Liepard V';
        this.fullName = 'Liepard V CRE';
        this.wantsToShadowRipper = false;
    }
    reduceEffect(store, state, effect) {
        // Ability: Hidden Claw
        // Ref: set-rebel-clash/eldegoss-v.ts (PlayPokemonEffect trigger when played from hand to bench)
        if (effect instanceof play_card_effects_1.PlayPokemonEffect && effect.pokemonCard === this) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // Collect all Pokemon with tools from both sides
            const hasPokemonWithTools = (() => {
                let found = false;
                player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (cardList.tools.length > 0)
                        found = true;
                });
                opponent.forEachPokemon(game_1.PlayerType.TOP_PLAYER, (cardList) => {
                    if (cardList.tools.length > 0)
                        found = true;
                });
                return found;
            })();
            if (!hasPokemonWithTools) {
                return state;
            }
            (0, prefabs_1.CONFIRMATION_PROMPT)(store, state, player, result => {
                if (!result)
                    return;
                // Block Pokemon without tools
                const blocked = [];
                player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, _card, target) => {
                    if (cardList.tools.length === 0)
                        blocked.push(target);
                });
                opponent.forEachPokemon(game_1.PlayerType.TOP_PLAYER, (cardList, _card, target) => {
                    if (cardList.tools.length === 0)
                        blocked.push(target);
                });
                store.prompt(state, new game_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON_TO_DISCARD_CARDS, game_1.PlayerType.ANY, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { allowCancel: false, min: 1, max: 1, blocked }), (selected) => {
                    const targets = selected || [];
                    if (targets.length === 0)
                        return;
                    const target = targets[0];
                    if (target.tools.length > 0) {
                        // Find which player owns this target to discard tool to correct discard pile
                        const targetOwner = game_1.StateUtils.findOwner(state, target);
                        target.moveCardTo(target.tools[0], targetOwner.discard);
                    }
                });
            });
        }
        // Attack 1: Shadow Ripper
        // Ref: set-chilling-reign/shaymin.ts (Sky Return - PUT_THIS_POKEMON_AND_ALL_ATTACHED_CARDS_INTO_YOUR_HAND with AfterAttackEffect)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            this.wantsToShadowRipper = true;
        }
        if (effect instanceof game_phase_effects_1.AfterAttackEffect && this.wantsToShadowRipper) {
            this.wantsToShadowRipper = false;
            (0, prefabs_1.CONFIRMATION_PROMPT)(store, state, effect.player, result => {
                if (result) {
                    (0, attack_effects_1.PUT_THIS_POKEMON_AND_ALL_ATTACHED_CARDS_INTO_YOUR_HAND)(store, state, effect);
                }
            });
        }
        if (effect instanceof game_phase_effects_1.EndTurnEffect) {
            this.wantsToShadowRipper = false;
        }
        return state;
    }
}
exports.LiepardV = LiepardV;
