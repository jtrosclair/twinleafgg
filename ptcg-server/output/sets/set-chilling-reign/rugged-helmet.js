"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.RuggedHelmet = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const state_utils_1 = require("../../game/store/state-utils");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class RuggedHelmet extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.TOOL;
        this.regulationMark = 'E';
        this.set = 'CRE';
        this.setNumber = '152';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Rugged Helmet';
        this.fullName = 'Rugged Helmet CRE';
        this.text = 'Attach a Pokémon Tool to 1 of your Pokémon that doesn\'t already have a Pokémon Tool attached. If the Pokémon this card is attached to is in the Active Spot and is damaged by an attack from your opponent\'s Pokémon (even if it is Knocked Out), put an Energy attached to the Attacking Pokémon into your opponent\'s hand. You may play any number of Item cards during your turn.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-scarlet-and-violet/rocky-helmet.ts (AfterDamageEffect tool pattern),
        //      set-plasma-freeze/rock-guard.ts (tool attached to target + attacker retaliation)
        if (effect instanceof attack_effects_1.AfterDamageEffect && effect.target.tools.includes(this)) {
            const player = effect.player;
            const targetPlayer = state_utils_1.StateUtils.findOwner(state, effect.target);
            if (effect.damage <= 0 || player === targetPlayer || targetPlayer.active !== effect.target) {
                return state;
            }
            // Try to reduce ToolEffect to check if something is blocking the tool
            try {
                const stub = new play_card_effects_1.ToolEffect(effect.player, this);
                store.reduceEffect(state, stub);
            }
            catch (_a) {
                return state;
            }
            if (state.phase === game_1.GamePhase.ATTACK) {
                // Get energy attached to the attacking Pokemon
                const attackerEnergyCards = effect.source.cards.filter(c => c.superType === card_types_1.SuperType.ENERGY);
                if (attackerEnergyCards.length === 0) {
                    return state;
                }
                // If only one energy, take it automatically; otherwise let target player choose
                if (attackerEnergyCards.length === 1) {
                    (0, prefabs_1.MOVE_CARDS)(store, state, effect.source, targetPlayer.hand, { cards: [attackerEnergyCards[0]], sourceCard: this });
                }
                else {
                    return store.prompt(state, new game_1.ChooseCardsPrompt(targetPlayer, game_1.GameMessage.MOVE_ENERGY_CARDS, effect.source, { superType: card_types_1.SuperType.ENERGY }, { min: 1, max: 1, allowCancel: false }), selected => {
                        if (selected && selected.length > 0) {
                            (0, prefabs_1.MOVE_CARDS)(store, state, effect.source, targetPlayer.hand, { cards: selected, sourceCard: this });
                        }
                    });
                }
            }
        }
        return state;
    }
}
exports.RuggedHelmet = RuggedHelmet;
