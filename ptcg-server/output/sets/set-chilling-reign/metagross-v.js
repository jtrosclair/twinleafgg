"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.MetagrossV = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class MetagrossV extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_V, card_types_1.CardTag.RAPID_STRIKE];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = M;
        this.hp = 220;
        this.weakness = [{ type: R }];
        this.resistance = [{ type: G, value: -30 }];
        this.retreat = [C, C, C];
        this.attacks = [
            {
                name: 'Bullet Punch',
                cost: [M],
                damage: 20,
                damageCalculation: '+',
                text: 'Flip 2 coins. This attack does 20 more damage for each heads.'
            },
            {
                name: 'Synchro Hammer',
                cost: [M, C],
                damage: 60,
                damageCalculation: '+',
                text: 'If this Pokémon and your opponent\'s Active Pokémon have the same amount of Energy attached, this attack does 90 more damage.'
            }
        ];
        this.regulationMark = 'E';
        this.set = 'CRE';
        this.setNumber = '112';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Metagross V';
        this.fullName = 'Metagross V CRE';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Bullet Punch
        // Ref: set-champions-path/malamar.ts (MULTIPLE_COIN_FLIPS_PROMPT for damage per heads)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            (0, prefabs_1.MULTIPLE_COIN_FLIPS_PROMPT)(store, state, player, 2, results => {
                const heads = results.filter(r => r).length;
                effect.damage += 20 * heads;
            });
        }
        // Attack 2: Synchro Hammer
        // Ref: set-chilling-reign/galarian-rapidash-v.ts (CheckProvidedEnergyEffect to count energy)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const playerEnergyCheck = new check_effects_1.CheckProvidedEnergyEffect(player, player.active);
            store.reduceEffect(state, playerEnergyCheck);
            const playerEnergyCount = playerEnergyCheck.energyMap.reduce((sum, em) => sum + em.provides.length, 0);
            const opponentEnergyCheck = new check_effects_1.CheckProvidedEnergyEffect(opponent, opponent.active);
            store.reduceEffect(state, opponentEnergyCheck);
            const opponentEnergyCount = opponentEnergyCheck.energyMap.reduce((sum, em) => sum + em.provides.length, 0);
            if (playerEnergyCount === opponentEnergyCount) {
                effect.damage += 90;
            }
        }
        return state;
    }
}
exports.MetagrossV = MetagrossV;
