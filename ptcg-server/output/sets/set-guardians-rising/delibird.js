"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Delibird = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Delibird extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = W;
        this.hp = 90;
        this.weakness = [{ type: M }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'All the Presents',
                cost: [C],
                damage: 0,
                text: 'Flip a coin until you get tails. For each heads, you may search your deck for a card and put it into your hand. Then, shuffle your deck.'
            },
            {
                name: 'Surprise Attack',
                cost: [W],
                damage: 40,
                text: 'Flip a coin. If tails, this attack does nothing.'
            }
        ];
        this.set = 'GRI';
        this.setNumber = '26';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Delibird';
        this.fullName = 'Delibird GRI';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: All the Presents
        // Refs: set-sun-and-moon/pyukumuku.ts (flip until tails), set-x-and-y/zorua.ts (search deck for cards)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            if (player.deck.cards.length === 0) {
                return state;
            }
            return (0, prefabs_1.FLIP_UNTIL_TAILS_AND_COUNT_HEADS)(store, state, player, heads => {
                if (heads > 0 && player.deck.cards.length > 0) {
                    const maxCards = Math.min(heads, player.deck.cards.length);
                    (0, prefabs_1.SEARCH_DECK_FOR_CARDS_TO_HAND)(store, state, player, this, {}, { min: 0, max: maxCards, allowCancel: true });
                }
            });
        }
        // Attack 2: Surprise Attack
        // Ref: set-sun-and-moon/pelipper.ts (Fly - coin flip, tails does nothing)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            (0, prefabs_1.COIN_FLIP_PROMPT)(store, state, player, result => {
                if (!result) {
                    effect.damage = 0;
                }
            });
        }
        return state;
    }
}
exports.Delibird = Delibird;
