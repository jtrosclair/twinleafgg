"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Watchog = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Watchog extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Patrat';
        this.cardType = C;
        this.hp = 90;
        this.weakness = [{ type: F }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Scrutinize',
                cost: [C],
                damage: 0,
                text: 'Look at the top 2 cards of your opponent\'s deck, discard 1 of them, and put the other card back.'
            },
            {
                name: 'Slam',
                cost: [C, C, C],
                damage: 60,
                damageCalculation: 'x',
                text: 'Flip 2 coins. This attack does 60 damage for each heads.'
            }
        ];
        this.set = 'GRI';
        this.setNumber = '108';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Watchog';
        this.fullName = 'Watchog GRI';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Scrutinize
        // Ref: set-x-and-y/diglett.ts (Mine - look at top cards pattern)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if (opponent.deck.cards.length === 0) {
                return state;
            }
            const topCards = new game_1.CardList();
            opponent.deck.moveTo(topCards, Math.min(2, opponent.deck.cards.length));
            return store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DISCARD, topCards, {}, { min: 1, max: 1, allowCancel: false }), (selected) => {
                const cards = selected || [];
                if (cards.length > 0) {
                    topCards.moveCardsTo(cards, opponent.discard);
                }
                // Put remaining card(s) back on top
                topCards.moveToTopOfDestination(opponent.deck);
            });
        }
        // Attack 2: Slam
        // Ref: set-sun-and-moon/parasect.ts (Fury Cutter - multiple coin flips)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            (0, prefabs_1.MULTIPLE_COIN_FLIPS_PROMPT)(store, state, effect.player, 2, results => {
                const heads = results.filter(r => r).length;
                effect.damage = 60 * heads;
            });
        }
        return state;
    }
}
exports.Watchog = Watchog;
