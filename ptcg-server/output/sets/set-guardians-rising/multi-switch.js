"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.MultiSwitch = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
class MultiSwitch extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.set = 'GRI';
        this.setNumber = '129';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Multi Switch';
        this.fullName = 'Multi Switch GRI';
        this.text = 'Move an Energy from 1 of your Benched Pokémon to your Active Pokémon.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-black-bolt-white-flare/ns-plan.ts (N's Plan - move energy from bench to active)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            // Check if any benched Pokemon has energy
            let hasEnergy = false;
            player.bench.forEach(b => {
                if (b.cards.length > 0 && b.cards.some(c => c.superType === card_types_1.SuperType.ENERGY)) {
                    hasEnergy = true;
                }
            });
            if (!hasEnergy) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            // Block all energy on Active (can't move from Active)
            const blockedMap = [];
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card, target) => {
                if (target.slot === game_1.SlotType.ACTIVE) {
                    blockedMap.push({ source: target, blocked: Array.from({ length: cardList.cards.length }, (_, i) => i) });
                }
            });
            // Block bench as target (only Active can receive)
            const blockedTo = [];
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (list, card, target) => {
                if (target.slot !== game_1.SlotType.ACTIVE) {
                    blockedTo.push(target);
                }
            });
            return store.prompt(state, new game_1.MoveEnergyPrompt(player.id, game_1.GameMessage.MOVE_ENERGY_CARDS, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.BENCH, game_1.SlotType.ACTIVE], { superType: card_types_1.SuperType.ENERGY }, { allowCancel: false, min: 1, max: 1, blockedMap, blockedTo }), transfers => {
                if (transfers && transfers.length > 0) {
                    for (const transfer of transfers) {
                        const source = game_1.StateUtils.getTarget(state, player, transfer.from);
                        source.moveCardTo(transfer.card, player.active);
                    }
                }
            });
        }
        return state;
    }
}
exports.MultiSwitch = MultiSwitch;
