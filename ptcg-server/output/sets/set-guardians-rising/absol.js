"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Absol = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const energy_card_1 = require("../../game/store/card/energy-card");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Absol extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = D;
        this.hp = 100;
        this.weakness = [{ type: F }];
        this.resistance = [{ type: P, value: -20 }];
        this.retreat = [C];
        this.DOOM_NEWS_MARKER = 'ABSOL_DOOM_NEWS_MARKER';
        this.CLEAR_DOOM_NEWS_MARKER = 'ABSOL_CLEAR_DOOM_NEWS_MARKER';
        this.attacks = [
            {
                name: 'Future Sight',
                cost: [D],
                damage: 0,
                text: 'Look at the top 4 cards of either player\'s deck and put them back in any order.'
            },
            {
                name: 'Doom News',
                cost: [D, C],
                damage: 0,
                text: 'Put all Energy attached to this Pokémon into your hand. At the end of your opponent\'s next turn, the Defending Pokémon will be Knocked Out.'
            }
        ];
        this.set = 'GRI';
        this.setNumber = '81';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Absol';
        this.fullName = 'Absol GRI';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Future Sight
        // Ref: set-vivid-voltage/duskull.ts (Future Sight)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const options = [
                {
                    message: game_1.GameMessage.ORDER_YOUR_DECK,
                    action: () => {
                        const deckTop = new game_1.CardList();
                        player.deck.moveTo(deckTop, Math.min(4, player.deck.cards.length));
                        return store.prompt(state, new game_1.OrderCardsPrompt(player.id, game_1.GameMessage.CHOOSE_CARDS_ORDER, deckTop, { allowCancel: false }), order => {
                            if (order === null) {
                                return state;
                            }
                            deckTop.applyOrder(order);
                            deckTop.moveToTopOfDestination(player.deck);
                        });
                    }
                },
                {
                    message: game_1.GameMessage.ORDER_OPPONENT_DECK,
                    action: () => {
                        const opponentDeckTop = new game_1.CardList();
                        opponent.deck.moveTo(opponentDeckTop, Math.min(4, opponent.deck.cards.length));
                        return store.prompt(state, new game_1.OrderCardsPrompt(player.id, game_1.GameMessage.CHOOSE_CARDS_ORDER, opponentDeckTop, { allowCancel: false }), order => {
                            if (order === null) {
                                return state;
                            }
                            opponentDeckTop.applyOrder(order);
                            opponentDeckTop.moveToTopOfDestination(opponent.deck);
                        });
                    }
                }
            ];
            return store.prompt(state, new game_1.SelectPrompt(player.id, game_1.GameMessage.CHOOSE_OPTION, options.map(opt => opt.message), { allowCancel: false }), choice => {
                const option = options[choice];
                option.action();
            });
        }
        // Attack 2: Doom News
        // Ref: set-lost-origin/hisuian-zoroark.ts (Doom Curse)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Put all Energy attached to this Pokemon into your hand
            const energyCards = player.active.cards.filter(c => c instanceof energy_card_1.EnergyCard);
            energyCards.forEach(c => {
                player.active.moveCardTo(c, player.hand);
            });
            // Set KO marker on defending Pokemon
            opponent.active.marker.addMarker(this.DOOM_NEWS_MARKER, this);
            opponent.marker.addMarker(this.CLEAR_DOOM_NEWS_MARKER, this);
        }
        // At end of opponent's turn, KO the defending Pokemon
        // Ref: set-roaring-skies/jirachi.ts (Doom Desire)
        if (effect instanceof game_phase_effects_1.EndTurnEffect && effect.player.marker.hasMarker(this.CLEAR_DOOM_NEWS_MARKER, this)) {
            effect.player.marker.removeMarker(this.CLEAR_DOOM_NEWS_MARKER, this);
            effect.player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                if (cardList.marker.hasMarker(this.DOOM_NEWS_MARKER, this)) {
                    cardList.marker.removeMarker(this.DOOM_NEWS_MARKER, this);
                    const checkHp = new check_effects_1.CheckHpEffect(effect.player, cardList);
                    store.reduceEffect(state, checkHp);
                    cardList.damage = checkHp.hp;
                }
            });
        }
        return state;
    }
}
exports.Absol = Absol;
