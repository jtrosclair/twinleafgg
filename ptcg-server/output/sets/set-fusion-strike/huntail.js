"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Huntail = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Huntail extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.FUSION_STRIKE];
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Clamperl';
        this.cardType = W;
        this.hp = 110;
        this.weakness = [{ type: L }];
        this.retreat = [C];
        this.powers = [{
                name: 'Single Strike Jammer',
                powerType: game_1.PowerType.ABILITY,
                text: 'Your opponent\'s Single Strike Pokémon\'s attacks cost Colorless more.'
            }];
        this.attacks = [
            {
                name: 'Cavernous Chomp',
                cost: [W, C, C],
                damage: 80,
                text: ''
            }
        ];
        this.regulationMark = 'E';
        this.set = 'FST';
        this.setNumber = '66';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Huntail';
        this.fullName = 'Huntail FST 66';
    }
    reduceEffect(store, state, effect) {
        // Ability: Single Strike Jammer
        // Ref: set-unbroken-bonds/persian.ts (CheckAttackCostEffect + IS_ABILITY_BLOCKED + forEachPokemon)
        if (effect instanceof check_effects_1.CheckAttackCostEffect) {
            const player = effect.player;
            // Check if this Huntail is in play on the opponent's side
            // effect.player is the one attacking (whose cost we're checking)
            // This ability affects the opponent's Pokemon, so we check on the opposite player's side
            const attackingPokemon = player.active.getPokemonCard();
            if (!attackingPokemon || !attackingPokemon.tags.includes(card_types_1.CardTag.SINGLE_STRIKE)) {
                return state;
            }
            // Check if Huntail is in play on the other side
            let huntailInPlay = false;
            // We need to find if this huntail is in play for the opponent of the attacker
            // The attacker is effect.player, so we iterate over state.players to find the opponent
            state.players.forEach(p => {
                if (p !== player) {
                    p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card) => {
                        if (card === this) {
                            huntailInPlay = true;
                        }
                    });
                }
            });
            if (!huntailInPlay) {
                return state;
            }
            // Find the owner of this Huntail (opponent of the attacking player)
            let huntailOwner = state.players.find(p => p !== player);
            if (!huntailOwner) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, huntailOwner, this)) {
                return state;
            }
            effect.cost.push(card_types_1.CardType.COLORLESS);
        }
        return state;
    }
}
exports.Huntail = Huntail;
