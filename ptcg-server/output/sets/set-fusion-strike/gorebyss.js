"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Gorebyss = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_effects_1 = require("../../game/store/effects/game-effects");
const check_effects_1 = require("../../game/store/effects/check-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attack_effects_1 = require("../../game/store/prefabs/attack-effects");
const game_2 = require("../../game");
class Gorebyss extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.FUSION_STRIKE];
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Clamperl';
        this.cardType = W;
        this.hp = 110;
        this.weakness = [{ type: L }];
        this.retreat = [C];
        this.powers = [{
                name: 'Rapid Strike Canceler',
                powerType: game_1.PowerType.ABILITY,
                text: 'Your opponent\'s Rapid Strike Pokémon in play have no Abilities.'
            }];
        this.attacks = [
            {
                name: 'Draining Kiss',
                cost: [W, C],
                damage: 50,
                text: 'Heal 30 damage from this Pokémon.'
            }
        ];
        this.regulationMark = 'E';
        this.set = 'FST';
        this.setNumber = '67';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Gorebyss';
        this.fullName = 'Gorebyss FST 67';
    }
    reduceEffect(store, state, effect) {
        // Ability: Rapid Strike Canceler
        // Opponent's Rapid Strike Pokemon have no Abilities
        // Ref: set-chilling-reign/path-to-the-peak.ts (ability blocking via CheckPokemonPowersEffect and PowerEffect)
        if (effect instanceof check_effects_1.CheckPokemonPowersEffect) {
            const targetPokemon = effect.target;
            if (!targetPokemon || !targetPokemon.tags || !targetPokemon.tags.includes(card_types_1.CardTag.RAPID_STRIKE)) {
                return state;
            }
            // Check if this Gorebyss is in play (opponent of the target)
            const targetOwner = game_1.StateUtils.findCardList(state, targetPokemon);
            if (!(targetOwner instanceof game_1.PokemonCardList)) {
                return state;
            }
            // Find Gorebyss in play
            let goreByssOwner = null;
            state.players.forEach(p => {
                p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (cardList.getPokemonCard() === this) {
                        goreByssOwner = p;
                    }
                });
            });
            if (!goreByssOwner) {
                return state;
            }
            // Determine if Gorebyss belongs to the opponent of the target
            let targetPlayer = null;
            state.players.forEach(p => {
                p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (cardList === targetOwner) {
                        targetPlayer = p;
                    }
                });
            });
            if (!targetPlayer || targetPlayer === goreByssOwner) {
                return state;
            }
            // Check if Gorebyss's ability is blocked
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, goreByssOwner, this)) {
                return state;
            }
            // Remove all abilities from the Rapid Strike target
            effect.powers = effect.powers.filter(power => power.powerType !== game_1.PowerType.ABILITY);
        }
        if (effect instanceof game_effects_1.PowerEffect) {
            const pokemonCard = effect.card;
            if (!pokemonCard.tags || !pokemonCard.tags.includes(card_types_1.CardTag.RAPID_STRIKE)) {
                return state;
            }
            if (effect.power.powerType !== game_1.PowerType.ABILITY) {
                return state;
            }
            if (effect.power.exemptFromAbilityLock) {
                return state;
            }
            // Find Gorebyss in play and check if it's the opponent of the Pokemon using the ability
            let gorebyss = null;
            state.players.forEach(p => {
                p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (cardList.getPokemonCard() === this) {
                        gorebyss = { owner: p };
                    }
                });
            });
            if (!gorebyss) {
                return state;
            }
            // The ability user is effect.player; if gorebyss.owner is opponent, block it
            if (gorebyss.owner === effect.player) {
                return state;
            }
            if (effect.power.useFromDiscard || effect.power.useFromHand) {
                return state;
            }
            // Check if Gorebyss's ability is blocked
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, gorebyss.owner, this)) {
                return state;
            }
            throw new game_2.GameError(game_2.GameMessage.CANNOT_USE_POWER);
        }
        // Attack 1: Draining Kiss
        // Ref: set-burning-shadows/gyarados.ts (Splashing Around - heal 30)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            (0, attack_effects_1.HEAL_X_DAMAGE_FROM_THIS_POKEMON)(30, effect, store, state);
        }
        return state;
    }
}
exports.Gorebyss = Gorebyss;
