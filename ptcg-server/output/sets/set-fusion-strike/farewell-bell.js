"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.FarewellBell = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_effects_1 = require("../../game/store/effects/game-effects");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class FarewellBell extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.regulationMark = 'E';
        this.set = 'FST';
        this.setNumber = '234';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Farewell Bell';
        this.fullName = 'Farewell Bell FST 234';
        this.text = 'If the Pokémon VMAX this card is attached to is Knocked Out by damage from an attack from your opponent\'s Pokémon, search your deck for a card and put it into your hand. Then, shuffle your deck. You may play any number of Item cards during your turn. Attach a Pokémon Tool to 1 of your Pokémon that doesn\'t already have a Pokémon Tool attached.';
        this.damageDealt = false;
    }
    reduceEffect(store, state, effect) {
        // Track if damage was dealt to the attached Pokemon from opponent's attack
        // Ref: set-plasma-blast/reversal-trigger.ts (KO trigger tool pattern)
        if ((effect instanceof attack_effects_1.DealDamageEffect || effect instanceof attack_effects_1.PutDamageEffect) &&
            effect.target.tools.includes(this)) {
            const player = game_1.StateUtils.findOwner(state, effect.target);
            if ((0, prefabs_1.IS_TOOL_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // Only works on VMAX Pokemon
            const pokemon = effect.target.getPokemonCard();
            if (pokemon && pokemon.tags.includes(card_types_1.CardTag.POKEMON_VMAX)) {
                this.damageDealt = true;
            }
        }
        // When VMAX KO'd by damage from attack, search deck for any card
        if (effect instanceof game_effects_1.KnockOutEffect && effect.target.tools.includes(this)
            && this.damageDealt) {
            const toolOwner = game_1.StateUtils.findOwner(state, effect.target);
            // Only works on VMAX Pokemon
            const pokemon = effect.target.getPokemonCard();
            if (pokemon && pokemon.tags.includes(card_types_1.CardTag.POKEMON_VMAX)) {
                this.damageDealt = false;
                if (toolOwner.deck.cards.length > 0) {
                    (0, prefabs_1.SEARCH_DECK_FOR_CARDS_TO_HAND)(store, state, toolOwner, this, {}, { min: 1, max: 1, allowCancel: false });
                }
            }
        }
        return state;
    }
}
exports.FarewellBell = FarewellBell;
