"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Sidney = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const energy_card_1 = require("../../game/store/card/energy-card");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
class Sidney extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.regulationMark = 'E';
        this.set = 'FST';
        this.setNumber = '241';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Sidney';
        this.fullName = 'Sidney FST 241';
        this.text = 'Your opponent reveals their hand. Discard up to 2 in any combination of Pokémon Tool cards, Special Energy cards, and Stadium cards from it. You may play only 1 Supporter card during your turn.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-unbroken-bonds/honchkrow-gx.ts (discard from opponent's hand via ChooseCardsPrompt)
        // Ref: set-chilling-reign/skwovet.ts (blocked array for filtering specific card types)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if (opponent.hand.cards.length === 0) {
                return state;
            }
            // Filter: only Tool cards, Special Energy cards, and Stadium cards are eligible
            var eligibleCount = 0;
            const blocked = [];
            opponent.hand.cards.forEach((card, index) => {
                const isTool = card instanceof trainer_card_1.TrainerCard && card.trainerType === card_types_1.TrainerType.TOOL;
                const isSpecialEnergy = card instanceof energy_card_1.EnergyCard && card.energyType === card_types_1.EnergyType.SPECIAL;
                const isStadium = card instanceof trainer_card_1.TrainerCard && card.trainerType === card_types_1.TrainerType.STADIUM;
                if (isTool || isSpecialEnergy || isStadium) {
                    eligibleCount++;
                }
                else {
                    blocked.push(index);
                }
            });
            const max = Math.min(2, eligibleCount);
            store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DISCARD, opponent.hand, {}, { min: 0, max, allowCancel: false, blocked }), (selected) => {
                const cards = selected || [];
                opponent.hand.moveCardsTo(cards, opponent.discard);
            });
        }
        return state;
    }
}
exports.Sidney = Sidney;
