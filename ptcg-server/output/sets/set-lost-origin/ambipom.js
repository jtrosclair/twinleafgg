"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Ambipom = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Ambipom extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Aipom';
        this.cardType = C;
        this.hp = 90;
        this.weakness = [{ type: F }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'All-You-Can-Grab',
                cost: [C],
                damage: 0,
                text: 'Flip a coin until you get tails. Search your deck for a number of cards up to the number of heads and put them into your hand. Then, shuffle your deck.'
            },
            {
                name: 'Knock Off',
                cost: [C, C],
                damage: 50,
                text: 'Discard a random card from your opponent\'s hand.'
            }
        ];
        this.regulationMark = 'F';
        this.set = 'LOR';
        this.setNumber = '145';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Ambipom';
        this.fullName = 'Ambipom LOR 145';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: All-You-Can-Grab
        // Ref: set-phantom-forces/feraligatr.ts (FLIP_UNTIL_TAILS_AND_COUNT_HEADS)
        // Ref: set-team-up/jirachi.ts (deckTop pattern, search deck for specific count)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            (0, prefabs_1.FLIP_UNTIL_TAILS_AND_COUNT_HEADS)(store, state, player, (heads) => {
                if (heads > 0 && player.deck.cards.length > 0) {
                    const max = Math.min(heads, player.deck.cards.length);
                    (0, prefabs_1.SEARCH_DECK_FOR_CARDS_TO_HAND)(store, state, player, this, {}, { min: 0, max, allowCancel: false });
                }
                else {
                    (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
                }
            });
        }
        // Attack 2: Knock Off
        // Ref: set-unbroken-bonds/murkrow.ts (ChooseCardsPrompt isSecret:true to pick random from opponent's hand)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if (opponent.hand.cards.length === 0) {
                return state;
            }
            // Pick a random card from opponent's hand
            const randomIndex = Math.floor(Math.random() * opponent.hand.cards.length);
            const randomCard = opponent.hand.cards[randomIndex];
            opponent.hand.moveCardTo(randomCard, opponent.discard);
        }
        return state;
    }
}
exports.Ambipom = Ambipom;
