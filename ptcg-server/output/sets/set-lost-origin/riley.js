"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Riley = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
class Riley extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.regulationMark = 'F';
        this.set = 'LOR';
        this.setNumber = '166';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Riley';
        this.fullName = 'Riley LOR 166';
        this.text = 'Reveal the top 5 cards of your deck and have your opponent choose 2 of them. Discard the chosen cards and put the remaining cards into your hand. You may play only 1 Supporter card during your turn.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-lost-origin/colresss-experiment.ts (deckTop pattern, CardList temp)
        // Ref: set-stormfront/marleys-request.ts (opponent ChooseCardsPrompt pattern)
        // Ref: set-astral-radiance/energy-loto.ts (reveal + shuffle back)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if (player.deck.cards.length === 0) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_PLAY_THIS_CARD);
            }
            const supporterTurn = player.supporterTurn;
            if (supporterTurn > 0) {
                throw new game_1.GameError(game_1.GameMessage.SUPPORTER_ALREADY_PLAYED);
            }
            effect.preventDefault = true;
            player.hand.moveCardTo(this, player.supporter);
            // Draw top 5 cards
            const deckTop = new game_1.CardList();
            const count = Math.min(5, player.deck.cards.length);
            player.deck.moveTo(deckTop, count);
            // Show the top 5 cards to opponent
            state = store.prompt(state, new game_1.ShowCardsPrompt(opponent.id, game_1.GameMessage.CARDS_SHOWED_BY_THE_OPPONENT, deckTop.cards), () => {
                // Opponent chooses 2 to discard
                const chooseCount = Math.min(2, deckTop.cards.length);
                if (chooseCount === 0) {
                    // All cards go to hand
                    deckTop.moveTo(player.hand);
                    player.supporter.moveCardTo(this, player.discard);
                    return;
                }
                state = store.prompt(state, new game_1.ChooseCardsPrompt(opponent, game_1.GameMessage.CHOOSE_CARD_TO_DISCARD, deckTop, {}, { min: chooseCount, max: chooseCount, allowCancel: false }), selected => {
                    const discarded = selected || [];
                    // Discard chosen cards
                    deckTop.moveCardsTo(discarded, player.discard);
                    // Rest go to hand
                    deckTop.moveTo(player.hand);
                    player.supporter.moveCardTo(this, player.discard);
                });
            });
        }
        return state;
    }
}
exports.Riley = Riley;
