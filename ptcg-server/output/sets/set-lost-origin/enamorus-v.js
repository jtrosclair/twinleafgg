"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.EnamorusV = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const check_effects_1 = require("../../game/store/effects/check-effects");
const game_effects_1 = require("../../game/store/effects/game-effects");
const game_message_1 = require("../../game/game-message");
class EnamorusV extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_V];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = P;
        this.hp = 210;
        this.weakness = [{ type: M }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'Guardian of Love',
                powerType: game_1.PowerType.ABILITY,
                text: 'Prevent all effects of your opponent\'s Pokémon\'s Abilities done to each of your Pokémon that has any [P] Energy attached, except any Enamorus V.'
            }];
        this.attacks = [
            {
                name: 'Blossom Tail',
                cost: [P, C, C],
                damage: 100,
                text: 'Attach up to 2 basic Energy cards from your discard pile to your Benched Pokémon in any way you like.'
            }
        ];
        this.regulationMark = 'F';
        this.set = 'LOR';
        this.setNumber = '82';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Enamorus V';
        this.fullName = 'Enamorus V LOR 82';
    }
    reduceEffect(store, state, effect) {
        // Ability: Guardian of Love (passive)
        // Prevent all effects of your opponent's Pokemon's Abilities done to each of your Pokemon
        // that has any [P] Energy attached, except any Enamorus V.
        // Ref: set-unbroken-bonds/stealthy-hood.ts (EffectOfAbilityEffect pattern),
        //      set-unbroken-bonds/vileplume.ts (AddSpecialConditionsPowerEffect pattern),
        //      set-astral-radiance/bastiodon.ts (passive ability isPokemonInPlay check)
        // Helper: check whether the guardian protection applies to a given target PokemonCardList
        const shouldProtectTarget = (targetList, abilityOwnerPlayer) => {
            const targetPokemon = targetList.getPokemonCard();
            if (!targetPokemon) {
                return false;
            }
            if (targetPokemon.name === 'Enamorus V') {
                return false;
            }
            const checkEnergy = new check_effects_1.CheckProvidedEnergyEffect(abilityOwnerPlayer, targetList);
            store.reduceEffect(state, checkEnergy);
            return checkEnergy.energyMap.some(em => em.provides.includes(card_types_1.CardType.PSYCHIC) || em.provides.includes(card_types_1.CardType.ANY));
        };
        // Intercept EffectOfAbilityEffect (e.g. Garbodor-style, Kecleon-style ability targeting)
        if (effect instanceof game_effects_1.EffectOfAbilityEffect && effect.target) {
            let enamorusPlayer = null;
            for (const p of state.players) {
                if (game_1.StateUtils.isPokemonInPlay(p, this)) {
                    enamorusPlayer = p;
                    break;
                }
            }
            if (!enamorusPlayer) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, enamorusPlayer, this)) {
                return state;
            }
            try {
                const targetPlayer = game_1.StateUtils.findOwner(state, effect.target);
                if (targetPlayer !== enamorusPlayer) {
                    return state;
                }
                if (effect.player === enamorusPlayer) {
                    return state;
                }
                if (shouldProtectTarget(effect.target, enamorusPlayer)) {
                    effect.target = undefined;
                }
            }
            catch (_a) {
                return state;
            }
        }
        // Intercept ability-sourced special conditions (AddSpecialConditionsPowerEffect)
        if (effect instanceof check_effects_1.AddSpecialConditionsPowerEffect) {
            let enamorusPlayer = null;
            for (const p of state.players) {
                if (game_1.StateUtils.isPokemonInPlay(p, this)) {
                    enamorusPlayer = p;
                    break;
                }
            }
            if (!enamorusPlayer) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, enamorusPlayer, this)) {
                return state;
            }
            try {
                const targetPlayer = game_1.StateUtils.findOwner(state, effect.target);
                if (!targetPlayer || targetPlayer !== enamorusPlayer) {
                    return state;
                }
                if (effect.player === enamorusPlayer) {
                    return state;
                }
                if (shouldProtectTarget(effect.target, enamorusPlayer)) {
                    effect.preventDefault = true;
                }
            }
            catch (_b) {
                return state;
            }
        }
        // Attack 1: Blossom Tail
        // Attach up to 2 basic Energy cards from your discard pile to your Benched Pokemon
        // Ref: set-astral-radiance/regigigas.ts (AttachEnergyPrompt from discard to bench)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const hasBasicEnergyInDiscard = player.discard.cards.some(c => c instanceof game_1.EnergyCard && c.energyType === card_types_1.EnergyType.BASIC);
            if (!hasBasicEnergyInDiscard) {
                return state;
            }
            return store.prompt(state, new game_1.AttachEnergyPrompt(player.id, game_message_1.GameMessage.ATTACH_ENERGY_TO_BENCH, player.discard, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC }, { allowCancel: true, min: 0, max: 2 }), transfers => {
                transfers = transfers || [];
                for (const transfer of transfers) {
                    const target = game_1.StateUtils.getTarget(state, player, transfer.to);
                    (0, prefabs_1.MOVE_CARDS)(store, state, player.discard, target, { cards: [transfer.card], sourceCard: this });
                }
            });
        }
        return state;
    }
}
exports.EnamorusV = EnamorusV;
