"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Serperior = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const check_effects_1 = require("../../game/store/effects/check-effects");
class Serperior extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_2;
        this.evolvesFrom = 'Servine';
        this.cardType = G;
        this.hp = 140;
        this.weakness = [{ type: R }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'Royal Garden',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'If this Pokémon has any [G] Energy attached to it, this Pokémon has no Retreat Cost.'
            }];
        this.attacks = [
            {
                name: 'Solar Beam',
                cost: [G, C, C],
                damage: 80,
                text: ''
            }
        ];
        this.set = 'LTR';
        this.setNumber = 'RC3';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Serperior';
        this.fullName = 'Serperior LTR';
    }
    reduceEffect(store, state, effect) {
        // Ability: Royal Garden
        // Ref: set-dark-explorers/darkrai-ex.ts (Dark Cloak)
        if (effect instanceof check_effects_1.CheckRetreatCostEffect) {
            const player = effect.player;
            // Only applies to this Serperior itself
            if (effect.player.active.getPokemonCard() !== this) {
                return state;
            }
            // Check if this Serperior is in play
            let isInPlay = false;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card) => {
                if (card === this) {
                    isInPlay = true;
                }
            });
            if (!isInPlay) {
                return state;
            }
            // Check if ability is blocked
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // Check if this Pokemon has Grass energy attached
            const checkProvidedEnergyEffect = new check_effects_1.CheckProvidedEnergyEffect(player);
            store.reduceEffect(state, checkProvidedEnergyEffect);
            const energyMap = checkProvidedEnergyEffect.energyMap;
            const hasGrassEnergy = game_1.StateUtils.checkEnoughEnergy(energyMap, [card_types_1.CardType.GRASS]);
            if (hasGrassEnergy) {
                effect.cost = [];
            }
        }
        return state;
    }
}
exports.Serperior = Serperior;
