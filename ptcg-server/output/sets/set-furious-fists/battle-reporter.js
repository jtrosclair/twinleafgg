"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.BattleReporter = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class BattleReporter extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.set = 'FFI';
        this.setNumber = '88';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Battle Reporter';
        this.fullName = 'Battle Reporter FFI';
        this.text = 'Draw cards until you have the same number of cards in your hand as your opponent. You may play only 1 Supporter card during your turn (before your attack).';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-great-encounters/baltoy.ts (Psychic Balance - draw until equal to opponent)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const supporterTurn = player.supporterTurn;
            if (supporterTurn > 0) {
                throw new game_1.GameError(game_1.GameMessage.SUPPORTER_ALREADY_PLAYED);
            }
            // Count opponent's hand size
            const opponentHandSize = opponent.hand.cards.length;
            // Current hand size (this card is already moved out of hand by the time we count,
            // so we count current hand)
            const currentHandSize = player.hand.cards.length;
            if (currentHandSize < opponentHandSize && player.deck.cards.length > 0) {
                const cardsToDraw = opponentHandSize - currentHandSize;
                (0, prefabs_1.DRAW_CARDS)(player, cardsToDraw);
            }
        }
        return state;
    }
}
exports.BattleReporter = BattleReporter;
