"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.DragoniteEx = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_effects_1 = require("../../game/store/effects/game-effects");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const costs_1 = require("../../game/store/prefabs/costs");
class DragoniteEx extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_EX];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = N;
        this.hp = 180;
        this.weakness = [{ type: Y }];
        this.retreat = [C, C, C];
        this.powers = [{
                name: 'Bust In',
                powerType: game_1.PowerType.ABILITY,
                exemptFromInitialize: true,
                text: 'When you play this Pokémon from your hand onto your Bench, you may move any number of basic Energy attached to your Pokémon to this Pokémon. If you do, switch this Pokémon with your Active Pokémon.'
            }];
        this.attacks = [
            {
                name: 'Jet Sonic',
                cost: [G, G, L],
                damage: 80,
                damageCalculation: '+',
                text: 'You may discard an Energy attached to this Pokémon. If you do, this attack does 40 more damage.'
            }
        ];
        this.set = 'FFI';
        this.setNumber = '74';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Dragonite-EX';
        this.fullName = 'Dragonite-EX FFI';
    }
    reduceEffect(store, state, effect) {
        // Ability: Bust In
        // Ref: set-temporal-forces/iron-leaves-ex.ts (Rapid Vernier - play from hand + switch + move energy)
        if (effect instanceof play_card_effects_1.PlayPokemonEffect && effect.pokemonCard === this) {
            const player = effect.player;
            state = store.prompt(state, new game_1.ConfirmPrompt(player.id, game_1.GameMessage.WANT_TO_USE_ABILITY), wantToUse => {
                if (wantToUse) {
                    // Check if ability is blocked
                    try {
                        const stub = new game_effects_1.PowerEffect(player, {
                            name: 'test',
                            powerType: game_1.PowerType.ABILITY,
                            text: ''
                        }, this);
                        store.reduceEffect(state, stub);
                    }
                    catch (_a) {
                        return state;
                    }
                    // Find this Pokemon's card list on bench
                    const cardList = game_1.StateUtils.findCardList(state, this);
                    const benchIndex = player.bench.indexOf(cardList);
                    if (benchIndex === -1) {
                        return state;
                    }
                    // Check if there is any basic energy on other Pokemon
                    let hasEnergyOnOtherPokemon = false;
                    player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (pokemonCardList) => {
                        if (pokemonCardList === player.bench[benchIndex]) {
                            return;
                        }
                        if (pokemonCardList.cards.some(c => c instanceof game_1.EnergyCard && c.energyType === card_types_1.EnergyType.BASIC)) {
                            hasEnergyOnOtherPokemon = true;
                        }
                    });
                    if (hasEnergyOnOtherPokemon) {
                        // Move any number of basic Energy from other Pokemon to this Pokemon
                        const blockedTo = [];
                        const blockedFrom = [];
                        player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (pokemonCardList, card, target) => {
                            if (pokemonCardList === player.bench[benchIndex]) {
                                blockedFrom.push(target);
                                return;
                            }
                            blockedTo.push(target);
                        });
                        store.prompt(state, new game_1.MoveEnergyPrompt(player.id, game_1.GameMessage.MOVE_ENERGY_CARDS, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC }, { allowCancel: true, blockedFrom, blockedTo }), transfers => {
                            if (transfers && transfers.length > 0) {
                                for (const transfer of transfers) {
                                    const source = game_1.StateUtils.getTarget(state, player, transfer.from);
                                    source.moveCardTo(transfer.card, player.bench[benchIndex]);
                                }
                                // Switch this Pokemon with Active
                                player.switchPokemon(player.bench[benchIndex]);
                            }
                        });
                    }
                }
            });
        }
        // Attack 1: Jet Sonic
        // Ref: set-boundaries-crossed/swanna.ts (Defog - optional discard for more damage)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const hasEnergy = player.active.cards.some(c => c instanceof game_1.EnergyCard);
            if (hasEnergy) {
                state = store.prompt(state, new game_1.ConfirmPrompt(player.id, game_1.GameMessage.WANT_TO_USE_ABILITY), wantToDiscard => {
                    if (wantToDiscard) {
                        (0, costs_1.DISCARD_X_ENERGY_FROM_THIS_POKEMON)(store, state, effect, 1);
                        effect.damage += 40;
                    }
                });
            }
        }
        return state;
    }
}
exports.DragoniteEx = DragoniteEx;
