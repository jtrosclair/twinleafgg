"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Manaphy = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attach_energy_prompt_1 = require("../../game/store/prompts/attach-energy-prompt");
const game_effects_1 = require("../../game/store/effects/game-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
class Manaphy extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = W;
        this.hp = 70;
        this.weakness = [{ type: L }];
        this.retreat = [C];
        this.powers = [{
                name: 'Final Wish',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'When this Pokémon is Knocked Out by damage from an opponent\'s attack, search your deck for a card and put it into your hand. Shuffle your deck afterward.'
            }];
        this.attacks = [
            {
                name: 'Seafaring',
                cost: [W],
                damage: 0,
                text: 'Flip 3 coins. For each heads, attach a [W] Energy card from your discard pile to your Benched Pokémon in any way you like.'
            }
        ];
        this.set = 'PLS';
        this.setNumber = '34';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Manaphy';
        this.fullName = 'Manaphy PLS';
    }
    reduceEffect(store, state, effect) {
        // Ability: Final Wish (passive - on KO)
        // Ref: set-roaring-skies/articuno.ts (KnockOutEffect pattern)
        if (effect instanceof game_effects_1.KnockOutEffect && effect.target.cards.includes(this)) {
            const player = game_1.StateUtils.findOwner(state, effect.target);
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if (state.phase !== game_1.GamePhase.ATTACK) {
                return state;
            }
            if (state.players[state.activePlayer] !== opponent) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            if (player.deck.cards.length === 0) {
                return state;
            }
            // Search deck for any card
            store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_HAND, player.deck, {}, { min: 1, max: 1, allowCancel: false }), selected => {
                if (selected && selected.length > 0) {
                    player.deck.moveCardsTo(selected, player.hand);
                }
                (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            });
        }
        // Attack 1: Seafaring
        // Ref: set-plasma-storm/victini-ex.ts (Turbo Energize attach from discard)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            (0, prefabs_1.MULTIPLE_COIN_FLIPS_PROMPT)(store, state, player, 3, results => {
                const heads = results.filter(r => r).length;
                if (heads === 0) {
                    return;
                }
                // Count available water energy in discard
                const waterEnergy = player.discard.cards.filter(c => c.superType === card_types_1.SuperType.ENERGY && c.energyType === card_types_1.EnergyType.BASIC &&
                    c.provides.includes(card_types_1.CardType.WATER));
                const hasBench = player.bench.some(b => b.cards.length > 0);
                if (waterEnergy.length === 0 || !hasBench) {
                    return;
                }
                const count = Math.min(heads, waterEnergy.length);
                store.prompt(state, new attach_energy_prompt_1.AttachEnergyPrompt(player.id, game_1.GameMessage.ATTACH_ENERGY_TO_BENCH, player.discard, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC, cardType: card_types_1.CardType.WATER }, { allowCancel: false, min: count, max: count }), transfers => {
                    transfers = transfers || [];
                    for (const transfer of transfers) {
                        const target = game_1.StateUtils.getTarget(state, player, transfer.to);
                        player.discard.moveCardTo(transfer.card, target);
                    }
                });
            });
        }
        return state;
    }
}
exports.Manaphy = Manaphy;
