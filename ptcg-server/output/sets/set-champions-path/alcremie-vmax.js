"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.AlcremieVmax = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const energy_card_1 = require("../../game/store/card/energy-card");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const discard_energy_prompt_1 = require("../../game/store/prompts/discard-energy-prompt");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class AlcremieVmax extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_VMAX];
        this.stage = card_types_1.Stage.VMAX;
        this.evolvesFrom = 'Alcremie V';
        this.cardType = P;
        this.hp = 310;
        this.weakness = [{ type: M }];
        this.retreat = [C, C, C];
        this.attacks = [
            {
                name: 'Adornment',
                cost: [C],
                damage: 0,
                text: 'For each of your Benched Pokémon, search your deck for a [P] Energy card and attach it to that Pokémon. Then, shuffle your deck.'
            },
            {
                name: 'G-Max Whisk',
                cost: [P, P],
                damage: 60,
                damageCalculation: 'x',
                text: 'Discard any amount of Energy from your Pokémon. This attack does 60 damage for each card you discarded in this way.'
            }
        ];
        this.regulationMark = 'D';
        this.set = 'CPA';
        this.setNumber = '23';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Alcremie VMAX';
        this.fullName = 'Alcremie VMAX CPA';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Adornment
        // Ref: set-ultra-prism/pachirisu.ts (Snuggly Generator - search deck for energy and attach to each bench)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            // Collect all benched Pokemon targets
            const benchTargets = [];
            player.bench.forEach(benchSlot => {
                if (benchSlot.cards.length > 0) {
                    benchTargets.push(benchSlot);
                }
            });
            if (benchTargets.length === 0) {
                return state;
            }
            // Count Psychic Energy available in deck
            const psychicInDeck = player.deck.cards.filter(c => c instanceof energy_card_1.EnergyCard && c.energyType === card_types_1.EnergyType.BASIC && c.provides.includes(card_types_1.CardType.PSYCHIC)).length;
            if (psychicInDeck === 0) {
                return (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            }
            const maxAttach = Math.min(benchTargets.length, psychicInDeck);
            // Build blocked list for deck search
            const blocked = [];
            player.deck.cards.forEach((card, index) => {
                if (!(card instanceof energy_card_1.EnergyCard) || card.energyType !== card_types_1.EnergyType.BASIC || !card.provides.includes(card_types_1.CardType.PSYCHIC)) {
                    blocked.push(index);
                }
            });
            return store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_ATTACH, player.deck, { superType: card_types_1.SuperType.ENERGY }, { min: 0, max: maxAttach, allowCancel: false, blocked }), selected => {
                const cards = selected || [];
                // Attach one energy to each bench Pokemon, in order
                for (let i = 0; i < cards.length && i < benchTargets.length; i++) {
                    player.deck.moveCardTo(cards[i], benchTargets[i]);
                }
                return (0, prefabs_1.SHUFFLE_DECK)(store, state, player);
            });
        }
        // Attack 2: G-Max Whisk
        // Ref: set-dragons-majesty/turtonator.ts (Explosive Jet - DiscardEnergyPrompt + damage per discarded)
        // Ref: set-paldea-evolved/chien-pao-ex.ts (Hail Blade - DISCARD_UP_TO_X_TYPE_ENERGY_FROM_YOUR_POKEMON)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            // Count all energy across all Pokemon
            let totalEnergy = 0;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                totalEnergy += cardList.cards.filter(c => c.superType === card_types_1.SuperType.ENERGY).length;
            });
            if (totalEnergy === 0) {
                effect.damage = 0;
                return state;
            }
            effect.damage = 0;
            return store.prompt(state, new discard_energy_prompt_1.DiscardEnergyPrompt(player.id, game_1.GameMessage.CHOOSE_ENERGIES_TO_DISCARD, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY }, { allowCancel: false, min: 0, max: totalEnergy }), transfers => {
                if (transfers === null || transfers.length === 0) {
                    return state;
                }
                // Discard the selected energy cards
                transfers.forEach(transfer => {
                    const source = game_1.StateUtils.getTarget(state, player, transfer.from);
                    const discardEffect = new attack_effects_1.DiscardCardsEffect(effect, [transfer.card]);
                    discardEffect.target = source;
                    store.reduceEffect(state, discardEffect);
                });
                effect.damage = transfers.length * 60;
            });
        }
        return state;
    }
}
exports.AlcremieVmax = AlcremieVmax;
