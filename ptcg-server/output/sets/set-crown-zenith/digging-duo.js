"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.DiggingDuo = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const card_list_1 = require("../../game/store/state/card-list");
// Ref: set-battle-styles/sordward-and-shielbert.ts (generator pattern with effect.preventDefault)
// Ref: set-plasma-blast/root-fossil-lileep.ts (bottom deck lookup pattern)
function* playCard(next, store, state, effect) {
    const player = effect.player;
    if (player.deck.cards.length === 0) {
        return state;
    }
    // Flip a coin to determine how many bottom cards to look at
    let isHeads = false;
    yield store.prompt(state, new game_1.CoinFlipPrompt(player.id, game_1.GameMessage.COIN_FLIP), result => {
        isHeads = result;
        next();
    });
    // Heads: look at bottom 8; Tails: look at bottom 3
    const lookCount = isHeads ? 8 : 3;
    const deckLength = player.deck.cards.length;
    const start = Math.max(0, deckLength - lookCount);
    const end = deckLength;
    const deckBottom = new card_list_1.CardList();
    deckBottom.cards = player.deck.cards.slice(start, end);
    if (deckBottom.cards.length === 0) {
        return state;
    }
    // Choose 1 card from the bottom cards to put into hand
    let chosen = [];
    yield store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_HAND, deckBottom, {}, { min: 1, max: 1, allowCancel: false }), selected => {
        chosen = selected || [];
        next();
    });
    // Move chosen card to hand; the rest stay in deck
    chosen.forEach(card => {
        player.deck.moveCardTo(card, player.hand);
    });
    // Shuffle the remaining deck cards
    return store.prompt(state, new game_1.ShuffleDeckPrompt(player.id), order => {
        player.deck.applyOrder(order);
    });
}
class DiggingDuo extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.regulationMark = 'F';
        this.set = 'CRZ';
        this.setNumber = '126';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Digging Duo';
        this.fullName = 'Digging Duo CRZ 126';
        this.text = 'Flip a coin. If heads, look at the bottom 8 cards of your deck and put 1 of them into your hand. If tails, look at the bottom 3 cards of your deck and put 1 of them into your hand. Shuffle the other cards back into your deck. You may play only 1 Supporter card during your turn.';
    }
    reduceEffect(store, state, effect) {
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            if (player.supporterTurn > 0) {
                throw new game_1.GameError(game_1.GameMessage.SUPPORTER_ALREADY_PLAYED);
            }
            player.hand.moveCardTo(effect.trainerCard, player.supporter);
            effect.preventDefault = true;
            const generator = playCard(() => generator.next(), store, state, effect);
            return generator.next().value;
        }
        return state;
    }
}
exports.DiggingDuo = DiggingDuo;
