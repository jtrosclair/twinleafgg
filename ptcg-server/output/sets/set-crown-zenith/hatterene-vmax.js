"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.HattereneVmax = void 0;
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const check_effects_1 = require("../../game/store/effects/check-effects");
const play_card_action_1 = require("../../game/store/actions/play-card-action");
const move_damage_prompt_1 = require("../../game/store/prompts/move-damage-prompt");
const card_types_1 = require("../../game/store/card/card-types");
const state_utils_1 = require("../../game/store/state-utils");
const game_error_1 = require("../../game/game-error");
const game_message_1 = require("../../game/game-message");
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const game_1 = require("../../game");
class HattereneVmax extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_VMAX];
        this.stage = card_types_1.Stage.VMAX;
        this.evolvesFrom = 'Hatterene V';
        this.cardType = P;
        this.hp = 320;
        this.weakness = [{ type: D }];
        this.resistance = [{ type: F, value: -30 }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'Witch\'s Domain',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'Once during your turn, you may move up to 2 damage counters from your Pokémon to your opponent\'s Active Pokémon.'
            }];
        this.attacks = [
            {
                name: 'G-Max Smite',
                cost: [P, C, C],
                damage: 150,
                text: 'Your opponent\'s Active Pokémon is now Confused.'
            }
        ];
        this.regulationMark = 'F';
        this.set = 'CRZ';
        this.setNumber = '66';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Hatterene VMAX';
        this.fullName = 'Hatterene VMAX CRZ 66';
        this.WITCHS_DOMAIN_MARKER = 'HATTERENE_VMAX_WITCHS_DOMAIN_MARKER';
    }
    reduceEffect(store, state, effect) {
        // Ability: Witch's Domain
        // Ref: set-battle-styles/meowstic.ts (Ear Moves - once per turn, MoveDamagePrompt cross-player)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = state_utils_1.StateUtils.getOpponent(state, player);
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_error_1.GameError(game_message_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            // Check that player has at least one Pokemon with damage
            const damagedOwnPokemon = [
                ...(player.active.damage > 0 ? [player.active] : []),
                ...player.bench.filter(b => b.cards.length > 0 && b.damage > 0)
            ];
            if (damagedOwnPokemon.length === 0) {
                throw new game_error_1.GameError(game_message_1.GameMessage.CANNOT_USE_POWER);
            }
            (0, prefabs_1.USE_ABILITY_ONCE_PER_TURN)(player, this.WITCHS_DOMAIN_MARKER, this);
            // Build max allowed damage for opponent's active only (the "to" target)
            const maxAllowedDamage = [];
            const checkHpEffect = new check_effects_1.CheckHpEffect(opponent, opponent.active);
            store.reduceEffect(state, checkHpEffect);
            opponent.forEachPokemon(play_card_action_1.PlayerType.TOP_PLAYER, (cardList, card, target) => {
                if (cardList === opponent.active) {
                    maxAllowedDamage.push({ target, damage: checkHpEffect.hp });
                }
            });
            // Block all "to" targets except opponent's active
            const blockedTo = [];
            // Block player's own Pokemon as "to" targets
            player.forEachPokemon(play_card_action_1.PlayerType.BOTTOM_PLAYER, (cardList, card, target) => {
                blockedTo.push(target);
            });
            // Block opponent's bench as "to" targets
            opponent.forEachPokemon(play_card_action_1.PlayerType.TOP_PLAYER, (cardList, card, target) => {
                if (cardList !== opponent.active) {
                    blockedTo.push(target);
                }
            });
            return store.prompt(state, new move_damage_prompt_1.MoveDamagePrompt(player.id, game_message_1.GameMessage.MOVE_DAMAGE, play_card_action_1.PlayerType.BOTTOM_PLAYER, [play_card_action_1.SlotType.ACTIVE, play_card_action_1.SlotType.BENCH], maxAllowedDamage, { min: 1, max: 2, allowCancel: true, blockedTo }), transfers => {
                if (!transfers || transfers.length === 0) {
                    return;
                }
                for (const transfer of transfers) {
                    const source = state_utils_1.StateUtils.getTarget(state, player, transfer.from);
                    const target = state_utils_1.StateUtils.getTarget(state, player, transfer.to);
                    if (source.damage >= 10) {
                        source.damage -= 10;
                        target.damage += 10;
                    }
                }
                player.forEachPokemon(play_card_action_1.PlayerType.BOTTOM_PLAYER, cardList => {
                    if (cardList.getPokemonCard() === this) {
                        cardList.addBoardEffect(game_1.BoardEffect.ABILITY_USED);
                    }
                });
            });
        }
        (0, prefabs_1.REMOVE_MARKER_AT_END_OF_TURN)(effect, this.WITCHS_DOMAIN_MARKER, this);
        // Attack 1: G-Max Smite
        // Ref: set-crown-zenith/girafarig.ts (YOUR_OPPPONENTS_ACTIVE_POKEMON_IS_NOW_CONFUSED)
        if ((0, prefabs_1.AFTER_ATTACK)(effect, 0, this)) {
            (0, prefabs_1.ADD_CONFUSION_TO_PLAYER_ACTIVE)(store, state, effect.opponent, this);
        }
        return state;
    }
}
exports.HattereneVmax = HattereneVmax;
