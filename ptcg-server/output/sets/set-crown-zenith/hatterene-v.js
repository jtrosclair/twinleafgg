"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.HattereneV = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const energy_card_1 = require("../../game/store/card/energy-card");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const order_cards_prompt_1 = require("../../game/store/prompts/order-cards-prompt");
class HattereneV extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_V];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = P;
        this.hp = 200;
        this.weakness = [{ type: D }];
        this.resistance = [{ type: F, value: -30 }];
        this.retreat = [C, C];
        this.attacks = [
            {
                name: 'Horoscope',
                cost: [P],
                damage: 0,
                text: 'Look at the top 3 cards of your deck. You may attach any number of Energy cards you find there to this Pokémon. Put the other cards back in any order.'
            },
            {
                name: 'Teleportation Burst',
                cost: [P, C, C],
                damage: 80,
                text: 'Switch this Pokémon with 1 of your Benched Pokémon.'
            }
        ];
        this.regulationMark = 'F';
        this.set = 'CRZ';
        this.setNumber = '65';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Hatterene V';
        this.fullName = 'Hatterene V CRZ 65';
        this.usedTeleportationBurst = false;
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Horoscope
        // Ref: set-vivid-voltage/trumbeak.ts (Charging Trumpet - look top 3, attach Energy, shuffle back)
        //      set-furious-fists/gothorita.ts (OrderCardsPrompt - put back in any order)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            if (player.deck.cards.length === 0) {
                return state;
            }
            const topCards = new game_1.CardList();
            const count = Math.min(3, player.deck.cards.length);
            player.deck.moveTo(topCards, count);
            const energyCards = topCards.cards.filter(c => c instanceof energy_card_1.EnergyCard);
            if (energyCards.length === 0) {
                // No energy found - put all cards back in any order
                return store.prompt(state, new order_cards_prompt_1.OrderCardsPrompt(player.id, game_1.GameMessage.CHOOSE_CARDS_ORDER, topCards, { allowCancel: false }), order => {
                    if (order !== null) {
                        topCards.applyOrder(order);
                    }
                    topCards.moveToTopOfDestination(player.deck);
                });
            }
            // Prompt to attach energy, then put remaining back in any order
            return store.prompt(state, new game_1.AttachEnergyPrompt(player.id, game_1.GameMessage.ATTACH_ENERGY_CARDS, topCards, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE], { superType: card_types_1.SuperType.ENERGY }, { allowCancel: false, min: 0, max: energyCards.length }), transfers => {
                transfers = transfers || [];
                for (const transfer of transfers) {
                    const target = game_1.StateUtils.getTarget(state, player, transfer.to);
                    const energyCard = transfer.card;
                    if (energyCard.energyType === card_types_1.EnergyType.SPECIAL) {
                        // For special energy, just move the card
                        topCards.moveCardTo(energyCard, target);
                    }
                    else {
                        const attachEnergyEffect = new play_card_effects_1.AttachEnergyEffect(player, energyCard, target);
                        store.reduceEffect(state, attachEnergyEffect);
                    }
                }
                // Put remaining cards back in any order
                if (topCards.cards.length > 0) {
                    store.prompt(state, new order_cards_prompt_1.OrderCardsPrompt(player.id, game_1.GameMessage.CHOOSE_CARDS_ORDER, topCards, { allowCancel: false }), order => {
                        if (order !== null) {
                            topCards.applyOrder(order);
                        }
                        topCards.moveToTopOfDestination(player.deck);
                    });
                }
            });
        }
        // Attack 2: Teleportation Burst
        // Ref: set-chilling-reign/tapu-fini.ts (Smash Turn - switch after attack in AfterAttackEffect)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const hasBench = player.bench.some(b => b.cards.length > 0);
            if (hasBench) {
                this.usedTeleportationBurst = true;
            }
        }
        if (effect instanceof game_phase_effects_1.AfterAttackEffect && this.usedTeleportationBurst) {
            this.usedTeleportationBurst = false;
            const player = effect.player;
            (0, prefabs_1.SWITCH_ACTIVE_WITH_BENCHED)(store, state, player);
        }
        if (effect instanceof game_phase_effects_1.EndTurnEffect) {
            this.usedTeleportationBurst = false;
        }
        return state;
    }
}
exports.HattereneV = HattereneV;
