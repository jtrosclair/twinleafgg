"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.MistysWaterCommand = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const trainer_prefabs_1 = require("../../game/store/prefabs/trainer-prefabs");
class MistysWaterCommand extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.set = 'HIF';
        this.setNumber = '63';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Misty\'s Water Command';
        this.fullName = 'Misty\'s Water Command HIF';
        this.text = 'Move any number of [W] Energy from your Pokémon to your Psyduck, Horsea, Staryu, Starmie-GX, Magikarp, Gyarados, or Lapras in any way you like. You may play only 1 Supporter card during your turn (before your attack).';
        this.VALID_POKEMON_NAMES = [
            'Psyduck', 'Horsea', 'Staryu', 'Starmie-GX', 'Magikarp', 'Gyarados', 'Lapras'
        ];
    }
    reduceEffect(store, state, effect) {
        // Ref: set-hidden-fates/brocks-training.ts (Brock's Training - named Pokemon targeting), set-team-up/electrode.ts (Electro Shaker - MoveEnergyPrompt)
        if ((0, trainer_prefabs_1.WAS_TRAINER_USED)(effect, this)) {
            const player = effect.player;
            // Check if player has any Water energy on their Pokemon
            let hasWaterEnergy = false;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                if (cardList.cards.some(c => c.superType === card_types_1.SuperType.ENERGY && c.name === 'Water Energy')) {
                    hasWaterEnergy = true;
                }
            });
            if (!hasWaterEnergy) {
                return state;
            }
            // Check if player has any valid target Pokemon in play
            let hasValidTarget = false;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                const pokemon = cardList.getPokemonCard();
                if (pokemon && this.VALID_POKEMON_NAMES.includes(pokemon.name)) {
                    hasValidTarget = true;
                }
            });
            if (!hasValidTarget) {
                return state;
            }
            // Build blocked targets list: only allow specific Pokemon names
            const blockedTo = [];
            const activePokemon = player.active.getPokemonCard();
            if (!activePokemon || !this.VALID_POKEMON_NAMES.includes(activePokemon.name)) {
                blockedTo.push({ player: game_1.PlayerType.BOTTOM_PLAYER, slot: game_1.SlotType.ACTIVE, index: 0 });
            }
            player.bench.forEach((b, index) => {
                const pokemon = b.getPokemonCard();
                if (!pokemon || !this.VALID_POKEMON_NAMES.includes(pokemon.name)) {
                    blockedTo.push({ player: game_1.PlayerType.BOTTOM_PLAYER, slot: game_1.SlotType.BENCH, index });
                }
            });
            store.prompt(state, new game_1.MoveEnergyPrompt(player.id, game_1.GameMessage.MOVE_ENERGY_CARDS, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { superType: card_types_1.SuperType.ENERGY, name: 'Water Energy' }, { allowCancel: true, blockedTo }), transfers => {
                if (!transfers) {
                    return;
                }
                for (const transfer of transfers) {
                    const source = game_1.StateUtils.getTarget(state, player, transfer.from);
                    const target = game_1.StateUtils.getTarget(state, player, transfer.to);
                    source.moveCardTo(transfer.card, target);
                }
            });
        }
        return state;
    }
}
exports.MistysWaterCommand = MistysWaterCommand;
