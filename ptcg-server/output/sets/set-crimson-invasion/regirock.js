"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Regirock = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Regirock extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = F;
        this.hp = 130;
        this.weakness = [{ type: G }];
        this.retreat = [C, C, C];
        this.powers = [{
                name: 'Rock Peak Growl',
                powerType: game_1.PowerType.ABILITY,
                text: 'Your Registeel\'s attacks do 10 more damage to your opponent\'s Active Pokémon (before applying Weakness and Resistance).'
            }];
        this.attacks = [
            {
                name: 'Tough Swing',
                cost: [F, F, C],
                damage: 110,
                text: 'This attack\'s damage isn\'t affected by Resistance.'
            }
        ];
        this.set = 'CIN';
        this.setNumber = '53';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Regirock';
        this.fullName = 'Regirock CIN';
    }
    reduceEffect(store, state, effect) {
        // Ability: Rock Peak Growl (passive - boost Registeel's attacks)
        // Ref: set-ultra-prism/passimian.ts (Power Huddle)
        if (effect instanceof attack_effects_1.DealDamageEffect) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const damageSource = effect.source.getPokemonCard();
            // Check if this Regirock is in play (active or bench)
            if (!player.active.cards.includes(this) && !player.bench.some(b => b.cards.includes(this))) {
                return state;
            }
            // Check if this card is the top Pokemon (not evolved over)
            const regirockSlot = player.active.cards.includes(this) ? player.active : player.bench.find(b => b.cards.includes(this));
            if (!regirockSlot || regirockSlot.getPokemonCard() !== this) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            if (damageSource
                && damageSource.name === 'Registeel'
                && effect.target === opponent.active) {
                effect.damage += 10;
            }
        }
        // Attack 1: Tough Swing
        // Ref: set-ultra-prism/passimian.ts (Rock Hurl)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            effect.ignoreResistance = true;
        }
        return state;
    }
}
exports.Regirock = Regirock;
