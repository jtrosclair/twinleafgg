"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Kingdra = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const game_effects_1 = require("../../game/store/effects/game-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attach_energy_prompt_1 = require("../../game/store/prompts/attach-energy-prompt");
const state_1 = require("../../game/store/state/state");
const card_list_1 = require("../../game/store/state/card-list");
const energy_card_1 = require("../../game/store/card/energy-card");
class Kingdra extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_2;
        this.evolvesFrom = 'Seadra';
        this.cardType = W;
        this.hp = 150;
        this.weakness = [{ type: L }];
        this.retreat = [C];
        this.powers = [{
                name: 'Deep Sea King',
                powerType: game_1.PowerType.ABILITY,
                text: 'When your Active Pokémon is Knocked Out by damage from an attack from your opponent\'s Pokémon, you may move any amount of [W] Energy from that Pokémon to this Pokémon.'
            }];
        this.attacks = [
            {
                name: 'Aqua Burst',
                cost: [W],
                damage: 40,
                damageCalculation: 'x',
                text: 'This attack does 40 damage for each [W] Energy attached to this Pokémon.'
            }
        ];
        this.regulationMark = 'E';
        this.set = 'BST';
        this.setNumber = '33';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Kingdra';
        this.fullName = 'Kingdra BST';
        this.DEEP_SEA_KING_MARKER = 'KINGDRA_BST_DEEP_SEA_KING_MARKER';
    }
    reduceEffect(store, state, effect) {
        // Ability: Deep Sea King (passive - when active KO'd by opponent, move W energy to this Pokemon)
        // Ref: set-scarlet-and-violet/exp-share.ts (KnockOutEffect with energy move), set-burning-shadows/wishful-baton.ts
        if (effect instanceof game_effects_1.KnockOutEffect && effect.target === effect.player.active) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Only fire during attack phase when opponent is the active player
            if (state.phase !== state_1.GamePhase.ATTACK || state.players[state.activePlayer] !== opponent) {
                return state;
            }
            // Find this Kingdra on the bench
            let kingdraCardList = null;
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card) => {
                if (card === this && cardList !== effect.target) {
                    kingdraCardList = cardList;
                }
            });
            if (!kingdraCardList) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // Prevent double-triggering
            if (effect.target.marker.hasMarker(this.DEEP_SEA_KING_MARKER, this)) {
                return state;
            }
            const active = effect.target;
            // Get Water energy cards from the active Pokemon
            const waterEnergyCards = active.cards.filter(c => c instanceof energy_card_1.EnergyCard
                && c.energyType === card_types_1.EnergyType.BASIC
                && c.provides.includes(card_types_1.CardType.WATER));
            if (waterEnergyCards.length === 0) {
                return state;
            }
            active.marker.addMarker(this.DEEP_SEA_KING_MARKER, this);
            // Make a snapshot of the water energy cards (they'll be moved to discard soon)
            const energySnapshot = new card_list_1.CardList();
            energySnapshot.cards = waterEnergyCards.slice();
            // Build blockedTo to only allow moving to Kingdra's slot
            const blockedTo = [];
            player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card, target) => {
                if (cardList !== kingdraCardList) {
                    blockedTo.push(target);
                }
            });
            state = store.prompt(state, new attach_energy_prompt_1.AttachEnergyPrompt(player.id, game_1.GameMessage.ATTACH_ENERGY_TO_BENCH, energySnapshot, game_1.PlayerType.BOTTOM_PLAYER, [game_1.SlotType.BENCH, game_1.SlotType.ACTIVE], { superType: card_types_1.SuperType.ENERGY }, { allowCancel: true, min: 0, max: waterEnergyCards.length, blockedTo }), transfers => {
                transfers = transfers || [];
                active.marker.removeMarker(this.DEEP_SEA_KING_MARKER, this);
                for (const transfer of transfers) {
                    const target = game_1.StateUtils.getTarget(state, player, transfer.to);
                    // Cards have been moved to discard by KO; move from discard to target
                    player.discard.moveCardTo(transfer.card, target);
                }
            });
        }
        // Attack 1: Aqua Burst
        // Ref: AGENTS-patterns.md (count [W] Energy attached) + AGENTS-patterns.md (energyType.BASIC check)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const checkEnergy = new check_effects_1.CheckProvidedEnergyEffect(player, player.active);
            store.reduceEffect(state, checkEnergy);
            const waterEnergyCount = checkEnergy.energyMap.reduce((sum, em) => {
                return sum + em.provides.filter(t => t === card_types_1.CardType.WATER || t === card_types_1.CardType.ANY).length;
            }, 0);
            effect.damage = 40 * waterEnergyCount;
        }
        return state;
    }
}
exports.Kingdra = Kingdra;
