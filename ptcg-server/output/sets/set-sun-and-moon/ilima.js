"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Ilima = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Ilima extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.SUPPORTER;
        this.set = 'SUM';
        this.setNumber = '121';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Ilima';
        this.fullName = 'Ilima SUM';
        this.text = 'Each player shuffles their hand into their deck and flips a coin. If heads, that player draws 6 cards. If tails, they draw 3 cards. You may play only 1 Supporter card during your turn (before your attack).';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-unified-minds/reset-stamp.ts (shuffle hand into deck)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Shuffle player's hand into deck (excluding this card which is in supporter zone)
            const playerCards = player.hand.cards.filter(c => c !== this);
            player.hand.moveCardsTo(playerCards, player.deck);
            store.prompt(state, new game_1.ShuffleDeckPrompt(player.id), order => {
                player.deck.applyOrder(order);
            });
            // Flip coin for player
            (0, prefabs_1.COIN_FLIP_PROMPT)(store, state, player, playerResult => {
                const playerDraw = playerResult ? 6 : 3;
                (0, prefabs_1.DRAW_CARDS)(player, Math.min(playerDraw, player.deck.cards.length));
            });
            // Shuffle opponent's hand into deck
            const opponentCards = opponent.hand.cards.slice();
            opponent.hand.moveCardsTo(opponentCards, opponent.deck);
            store.prompt(state, new game_1.ShuffleDeckPrompt(opponent.id), order => {
                opponent.deck.applyOrder(order);
            });
            // Flip coin for opponent
            (0, prefabs_1.COIN_FLIP_PROMPT)(store, state, opponent, opponentResult => {
                const opponentDraw = opponentResult ? 6 : 3;
                (0, prefabs_1.DRAW_CARDS)(opponent, Math.min(opponentDraw, opponent.deck.cards.length));
            });
        }
        return state;
    }
}
exports.Ilima = Ilima;
