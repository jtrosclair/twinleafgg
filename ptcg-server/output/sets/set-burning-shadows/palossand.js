"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Palossand = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const marker_constants_1 = require("../../game/store/markers/marker-constants");
class Palossand extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Sandygast';
        this.cardType = P;
        this.hp = 130;
        this.weakness = [{ type: D }];
        this.resistance = [{ type: F, value: -20 }];
        this.retreat = [C, C, C, C];
        this.attacks = [
            {
                name: 'Sand Tomb',
                cost: [P, C, C],
                damage: 80,
                text: 'The Defending Pokémon can\'t retreat during your opponent\'s next turn.'
            },
            {
                name: 'Dust Storm',
                cost: [C, C, C, C],
                damage: 100,
                text: 'Discard all cards from both Active Pokémon.'
            }
        ];
        this.set = 'BUS';
        this.setNumber = '62';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Palossand';
        this.fullName = 'Palossand BUS';
        this.usedDustStorm = false;
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Sand Tomb
        // Ref: set-sun-and-moon/bewear.ts (Bear Hug)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            return (0, prefabs_1.BLOCK_RETREAT)(store, state, effect, this);
        }
        (0, prefabs_1.BLOCK_RETREAT_IF_MARKER)(effect, marker_constants_1.MarkerConstants.DEFENDING_POKEMON_CANNOT_RETREAT_MARKER, this);
        (0, prefabs_1.REMOVE_MARKER_FROM_ACTIVE_AT_END_OF_TURN)(effect, marker_constants_1.MarkerConstants.DEFENDING_POKEMON_CANNOT_RETREAT_MARKER, this);
        // Attack 2: Dust Storm
        // Ref: AGENTS-patterns.md (discard all cards from Active Pokemon)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            this.usedDustStorm = true;
        }
        // Discard all cards from both actives after damage
        if (effect instanceof game_phase_effects_1.AfterAttackEffect && this.usedDustStorm) {
            this.usedDustStorm = false;
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Discard all cards (non-Pokemon) from player's active
            const playerPokemons = player.active.getPokemons();
            const playerCards = player.active.cards.filter(c => !playerPokemons.includes(c));
            playerCards.forEach(c => {
                player.active.moveCardTo(c, player.discard);
            });
            // Discard tools from player's active
            const playerTools = player.active.tools.slice();
            playerTools.forEach(c => {
                player.active.moveCardTo(c, player.discard);
            });
            // Discard all cards (non-Pokemon) from opponent's active
            const opponentPokemons = opponent.active.getPokemons();
            const opponentCards = opponent.active.cards.filter(c => !opponentPokemons.includes(c));
            opponentCards.forEach(c => {
                opponent.active.moveCardTo(c, opponent.discard);
            });
            // Discard tools from opponent's active
            const opponentTools = opponent.active.tools.slice();
            opponentTools.forEach(c => {
                opponent.active.moveCardTo(c, opponent.discard);
            });
        }
        if (effect instanceof game_phase_effects_1.EndTurnEffect) {
            this.usedDustStorm = false;
        }
        return state;
    }
}
exports.Palossand = Palossand;
