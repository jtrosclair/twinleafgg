"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.RotomDexPokeFinderMode = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const play_card_effects_1 = require("../../game/store/effects/play-card-effects");
class RotomDexPokeFinderMode extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.ITEM;
        this.set = 'BUS';
        this.setNumber = '122';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Rotom Dex Poké Finder Mode';
        this.fullName = 'Rotom Dex Poké Finder Mode BUS';
        this.text = 'Look at the top 4 cards of your deck and put them back in any order or shuffle them into your deck.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-guardians-rising/absol.ts (Future Sight - look at top cards and reorder)
        if (effect instanceof play_card_effects_1.TrainerEffect && effect.trainerCard === this) {
            const player = effect.player;
            if (player.deck.cards.length === 0) {
                return state;
            }
            const deckTop = new game_1.CardList();
            player.deck.moveTo(deckTop, Math.min(4, player.deck.cards.length));
            // Ask: put back in order or shuffle into deck?
            return store.prompt(state, new game_1.ConfirmPrompt(player.id, game_1.GameMessage.ORDER_YOUR_DECK), wantToOrder => {
                if (wantToOrder) {
                    // Put them back in any order
                    store.prompt(state, new game_1.OrderCardsPrompt(player.id, game_1.GameMessage.CHOOSE_CARDS_ORDER, deckTop, { allowCancel: false }), order => {
                        if (order === null) {
                            return state;
                        }
                        deckTop.applyOrder(order);
                        deckTop.moveToTopOfDestination(player.deck);
                    });
                }
                else {
                    // Shuffle into deck
                    deckTop.moveTo(player.deck);
                    store.prompt(state, new game_1.ShuffleDeckPrompt(player.id), order => {
                        player.deck.applyOrder(order);
                    });
                }
            });
        }
        return state;
    }
}
exports.RotomDexPokeFinderMode = RotomDexPokeFinderMode;
