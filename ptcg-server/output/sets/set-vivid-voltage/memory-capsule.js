"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.MemoryCapsule = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const check_effects_1 = require("../../game/store/effects/check-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class MemoryCapsule extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.TOOL;
        this.regulationMark = 'D';
        this.set = 'VIV';
        this.setNumber = '155';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Memory Capsule';
        this.fullName = 'Memory Capsule VIV';
        this.text = 'Attach a Pokémon Tool to 1 of your Pokémon that doesn\'t already have a Pokémon Tool attached. The Pokémon this card is attached to can use any attack from its previous Evolutions. (You still need the necessary Energy to use each attack.) You may play any number of Item cards during your turn.';
    }
    reduceEffect(store, state, effect) {
        // Memory Capsule: The Pokemon this card is attached to can use attacks from its previous Evolutions.
        // Ref: set-legends-awakened/mewtwo-lv-x.ts (CheckPokemonAttacksEffect + evolution card attacks)
        // Ref: set-plasma-blast/g-scope.ts (CheckPokemonAttacksEffect with tools.includes check)
        if (effect instanceof check_effects_1.CheckPokemonAttacksEffect
            && effect.player.active.tools.includes(this)) {
            const player = effect.player;
            if ((0, prefabs_1.IS_TOOL_BLOCKED)(store, state, player, this)) {
                return state;
            }
            // Find the card list containing the active pokemon
            const cardList = player.active;
            // Add attacks from all previous evolution cards in this card list
            const activePokemon = cardList.getPokemonCard();
            for (const card of cardList.cards) {
                if (card.superType === card_types_1.SuperType.POKEMON && card !== activePokemon) {
                    const pokemonCard = card;
                    if (pokemonCard.attacks) {
                        effect.attacks.push(...pokemonCard.attacks.filter((a) => !effect.attacks.includes(a)));
                    }
                }
            }
        }
        return state;
    }
}
exports.MemoryCapsule = MemoryCapsule;
