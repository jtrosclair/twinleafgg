"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.OrbeetleV = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const check_effects_1 = require("../../game/store/effects/check-effects");
class OrbeetleV extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_V];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = G;
        this.hp = 180;
        this.weakness = [{ type: R }];
        this.retreat = [C];
        this.attacks = [
            {
                name: 'Strafe',
                cost: [G],
                damage: 20,
                text: 'You may switch this Pokémon with 1 of your Benched Pokémon.'
            },
            {
                name: 'Mysterious Wave',
                cost: [G, C],
                damage: 50,
                damageCalculation: '+',
                text: 'This attack does 30 more damage for each Energy attached to your opponent\'s Active Pokémon.'
            }
        ];
        this.regulationMark = 'D';
        this.set = 'VIV';
        this.setNumber = '20';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Orbeetle V';
        this.fullName = 'Orbeetle V VIV';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Strafe
        // Ref: set-darkness-ablaze/manectric.ts (AFTER_ATTACK + ConfirmPrompt + SWITCH_ACTIVE_WITH_BENCHED)
        if ((0, prefabs_1.AFTER_ATTACK)(effect, 0, this)) {
            const player = effect.player;
            if (player.bench.some(b => b.cards.length > 0)) {
                store.prompt(state, new game_1.ConfirmPrompt(player.id, game_1.GameMessage.WANT_TO_SWITCH_POKEMON), wantToSwitch => {
                    if (wantToSwitch) {
                        (0, prefabs_1.SWITCH_ACTIVE_WITH_BENCHED)(store, state, player);
                    }
                });
            }
        }
        // Attack 2: Mysterious Wave
        // Ref: set-vivid-voltage/dhelmise.ts (CheckProvidedEnergyEffect to count energyMap),
        //      set-vivid-voltage/ferrothorn.ts (count total energy attached)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const checkEnergy = new check_effects_1.CheckProvidedEnergyEffect(opponent, opponent.active);
            store.reduceEffect(state, checkEnergy);
            const energyCount = checkEnergy.energyMap.reduce((sum, em) => sum + em.provides.length, 0);
            effect.damage += 30 * energyCount;
        }
        return state;
    }
}
exports.OrbeetleV = OrbeetleV;
