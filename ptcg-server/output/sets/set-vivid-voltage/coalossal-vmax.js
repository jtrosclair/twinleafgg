"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.CoalossalVmax = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const energy_card_1 = require("../../game/store/card/energy-card");
const card_list_1 = require("../../game/store/state/card-list");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class CoalossalVmax extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_VMAX];
        this.stage = card_types_1.Stage.VMAX;
        this.evolvesFrom = 'Coalossal V';
        this.cardType = F;
        this.hp = 330;
        this.weakness = [{ type: G }];
        this.retreat = [C, C, C, C];
        this.attacks = [
            {
                name: 'Eruption Shot',
                cost: [F],
                damage: 40,
                damageCalculation: '+',
                text: 'Discard the top card of your deck. If that card is an Energy card, this attack does 90 more damage, and attach that card to this Pokémon.'
            },
            {
                name: 'G-Max Boulder',
                cost: [F, F, F, C],
                damage: 240,
                text: ''
            }
        ];
        this.regulationMark = 'D';
        this.set = 'VIV';
        this.setNumber = '99';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Coalossal VMAX';
        this.fullName = 'Coalossal VMAX VIV';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Eruption Shot
        // Ref: set-sword-and-shield/torkoal-v.ts (Combustion Pillar - discard top card, check if energy)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            if (player.deck.cards.length === 0) {
                return state;
            }
            const topCard = new card_list_1.CardList();
            player.deck.moveTo(topCard, 1);
            const isEnergyCard = topCard.cards.length > 0 && topCard.cards[0] instanceof energy_card_1.EnergyCard;
            if (isEnergyCard) {
                // Attach the energy card to this Pokemon
                const cardList = game_1.StateUtils.findCardList(state, this);
                if (cardList) {
                    topCard.moveCardTo(topCard.cards[0], cardList);
                }
                else {
                    topCard.moveTo(player.discard);
                }
                effect.damage += 90;
            }
            else {
                topCard.moveTo(player.discard);
            }
        }
        return state;
    }
}
exports.CoalossalVmax = CoalossalVmax;
