"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.WyndonStadium = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_effects_1 = require("../../game/store/effects/game-effects");
class WyndonStadium extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.STADIUM;
        this.regulationMark = 'D';
        this.set = 'VIV';
        this.setNumber = '161';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Wyndon Stadium';
        this.fullName = 'Wyndon Stadium VIV';
        this.text = 'Whenever either player plays a Pokémon VMAX from their hand to evolve a Pokémon V during their turn, heal 100 damage from that Pokémon.';
    }
    reduceEffect(store, state, effect) {
        // Passive effect: heal 100 when any VMAX evolves from a V
        // Ref: set-vivid-voltage/circhester-bath.ts (passive stadium intercept pattern)
        // Ref: set-darkness-ablaze/corviknight.ts (EvolveEffect with pokemonCard.tags check)
        if (effect instanceof game_effects_1.EvolveEffect) {
            const stadiumCard = game_1.StateUtils.getStadiumCard(state);
            if (stadiumCard !== this) {
                return state;
            }
            // Only trigger when a VMAX evolves
            if (!effect.pokemonCard.tags.includes(card_types_1.CardTag.POKEMON_VMAX)) {
                return state;
            }
            // Heal 100 damage from the newly evolved Pokemon
            const target = effect.target;
            if (target.damage > 0) {
                const healAmount = Math.min(100, target.damage);
                const healEffect = new game_effects_1.HealEffect(effect.player, target, healAmount);
                store.reduceEffect(state, healEffect);
            }
        }
        return state;
    }
}
exports.WyndonStadium = WyndonStadium;
