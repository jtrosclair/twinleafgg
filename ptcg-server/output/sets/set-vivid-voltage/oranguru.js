"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Oranguru = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Oranguru extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = C;
        this.hp = 120;
        this.weakness = [{ type: F }];
        this.retreat = [C, C];
        this.PRIMATE_WISDOM_MARKER = 'ORANGURU_VIV_PRIMATE_WISDOM_MARKER';
        this.powers = [{
                name: 'Primate Wisdom',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'Once during your turn, you may switch a card from your hand with the top card of your deck.'
            }];
        this.attacks = [
            {
                name: 'Whap Down',
                cost: [C, C, C],
                damage: 70,
                text: ''
            }
        ];
        this.regulationMark = 'D';
        this.set = 'VIV';
        this.setNumber = '199';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Oranguru';
        this.fullName = 'Oranguru VIV';
    }
    reduceEffect(store, state, effect) {
        // Ability: Primate Wisdom
        // Ref: set-legends-awakened/uxie-lv-x.ts (ADD_MARKER + ChooseCardsPrompt from deckTop),
        //      set-vivid-voltage/clefable.ts (IS_ABILITY_BLOCKED + USE_ABILITY_ONCE_PER_TURN + ABILITY_USED)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            if (player.hand.cards.length === 0) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_USE_POWER);
            }
            if (player.deck.cards.length === 0) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_USE_POWER);
            }
            (0, prefabs_1.USE_ABILITY_ONCE_PER_TURN)(player, this.PRIMATE_WISDOM_MARKER, this);
            (0, prefabs_1.ABILITY_USED)(player, this);
            // Take the top card of the deck into a temp list
            const deckTop = new game_1.CardList();
            player.deck.moveTo(deckTop, 1);
            // Player chooses 1 card from their hand to put on top of deck
            return store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DECK, player.hand, {}, { min: 1, max: 1, allowCancel: false }), selected => {
                const cards = selected || [];
                if (cards.length > 0) {
                    // Move chosen hand card to top of deck
                    player.hand.moveCardsTo(cards, player.deck);
                    // Move the former top card into hand
                    deckTop.moveTo(player.hand);
                }
                else {
                    // Cancelled: move deckTop back
                    deckTop.moveToTopOfDestination(player.deck);
                }
            });
        }
        (0, prefabs_1.REMOVE_MARKER_AT_END_OF_TURN)(effect, this.PRIMATE_WISDOM_MARKER, this);
        return state;
    }
}
exports.Oranguru = Oranguru;
