"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.OrbeetleVmax = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const check_effects_1 = require("../../game/store/effects/check-effects");
class OrbeetleVmax extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_VMAX];
        this.stage = card_types_1.Stage.VMAX;
        this.evolvesFrom = 'Orbeetle V';
        this.cardType = G;
        this.hp = 310;
        this.weakness = [{ type: R }];
        this.retreat = [C];
        this.EERIE_BEAM_MARKER = 'ORBEETLE_VMAX_VIV_EERIE_BEAM_MARKER';
        this.powers = [{
                name: 'Eerie Beam',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'Once during your turn, if this Pokémon is in the Active Spot, you may put 1 damage counter on each of your opponent\'s Pokémon.'
            }];
        this.attacks = [
            {
                name: 'G-Max Wave',
                cost: [G, C],
                damage: 50,
                damageCalculation: '+',
                text: 'This attack does 50 more damage for each Energy attached to your opponent\'s Active Pokémon.'
            }
        ];
        this.regulationMark = 'D';
        this.set = 'VIV';
        this.setNumber = '21';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Orbeetle VMAX';
        this.fullName = 'Orbeetle VMAX VIV';
    }
    reduceEffect(store, state, effect) {
        // Ability: Eerie Beam
        // Ref: set-unbroken-bonds/weezing.ts (put damage counter on each opponent's Pokemon),
        //      set-vivid-voltage/clefable.ts (IS_ABILITY_BLOCKED + USE_ABILITY_ONCE_PER_TURN + ABILITY_USED + active check)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            // Must be in Active Spot
            if (player.active.getPokemonCard() !== this) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_USE_POWER);
            }
            (0, prefabs_1.USE_ABILITY_ONCE_PER_TURN)(player, this.EERIE_BEAM_MARKER, this);
            (0, prefabs_1.ABILITY_USED)(player, this);
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Put 1 damage counter (10 damage) on each of opponent's Pokemon
            opponent.forEachPokemon(game_1.PlayerType.TOP_PLAYER, (cardList) => {
                if (cardList.cards.length > 0) {
                    cardList.damage += 10;
                }
            });
        }
        (0, prefabs_1.REMOVE_MARKER_AT_END_OF_TURN)(effect, this.EERIE_BEAM_MARKER, this);
        // Attack 1: G-Max Wave
        // Ref: set-vivid-voltage/orbeetle-v.ts (CheckProvidedEnergyEffect on opponent's active for energy count)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const checkEnergy = new check_effects_1.CheckProvidedEnergyEffect(opponent, opponent.active);
            store.reduceEffect(state, checkEnergy);
            const energyCount = checkEnergy.energyMap.reduce((sum, em) => sum + em.provides.length, 0);
            effect.damage += 50 * energyCount;
        }
        return state;
    }
}
exports.OrbeetleVmax = OrbeetleVmax;
