"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Eelektrik = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attack_effects_1 = require("../../game/store/effects/attack-effects");
class Eelektrik extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Tynamo';
        this.cardType = L;
        this.hp = 90;
        this.weakness = [{ type: F }];
        this.retreat = [C, C];
        this.attacks = [
            {
                name: 'Shocking Smash',
                cost: [L],
                damage: 0,
                text: 'Flip a coin. If heads, discard an Energy from 1 of your opponent\'s Pokémon.'
            },
            {
                name: 'Head Bolt',
                cost: [L, C, C],
                damage: 60,
                text: ''
            }
        ];
        this.regulationMark = 'D';
        this.set = 'VIV';
        this.setNumber = '58';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Eelektrik';
        this.fullName = 'Eelektrik VIV';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Shocking Smash
        // Refs: set-unbroken-bonds/fearow.ts (coin flip + discard energy), set-unbroken-bonds/honchkrow-gx.ts (ChoosePokemonPrompt from bench+active)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            (0, prefabs_1.COIN_FLIP_PROMPT)(store, state, player, result => {
                if (!result) {
                    return;
                }
                // Check if any opponent Pokemon has energy
                let hasEnergy = false;
                opponent.forEachPokemon(game_1.PlayerType.TOP_PLAYER, (cardList) => {
                    if (cardList.cards.some(c => c.superType === card_types_1.SuperType.ENERGY)) {
                        hasEnergy = true;
                    }
                });
                if (!hasEnergy) {
                    return;
                }
                store.prompt(state, new game_1.ChoosePokemonPrompt(player.id, game_1.GameMessage.CHOOSE_POKEMON_TO_DISCARD_CARDS, game_1.PlayerType.TOP_PLAYER, [game_1.SlotType.ACTIVE, game_1.SlotType.BENCH], { allowCancel: false }), targets => {
                    if (!targets || targets.length === 0) {
                        return;
                    }
                    const target = targets[0];
                    const energyCards = target.cards.filter(c => c.superType === card_types_1.SuperType.ENERGY);
                    if (energyCards.length === 0) {
                        return;
                    }
                    store.prompt(state, new game_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_DISCARD, target, { superType: card_types_1.SuperType.ENERGY }, { min: 1, max: 1, allowCancel: false }), selected => {
                        const cards = selected || [];
                        if (cards.length > 0) {
                            const discardEnergy = new attack_effects_1.DiscardCardsEffect(effect, cards);
                            discardEnergy.target = target;
                            store.reduceEffect(state, discardEnergy);
                        }
                    });
                });
            });
        }
        return state;
    }
}
exports.Eelektrik = Eelektrik;
