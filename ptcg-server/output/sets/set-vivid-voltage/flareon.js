"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Flareon = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_effects_1 = require("../../game/store/effects/game-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class Flareon extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Eevee';
        this.cardType = R;
        this.hp = 110;
        this.weakness = [{ type: W }];
        this.retreat = [C, C];
        this.powers = [{
                name: 'Incandescent Awakening',
                powerType: game_1.PowerType.ABILITY,
                text: 'If this Pokémon has a Memory Capsule attached, Grass Pokémon in play (both yours and your opponent\'s) have no Abilities.'
            }];
        this.attacks = [
            {
                name: 'Fire Mane',
                cost: [R, R, C],
                damage: 100,
                text: ''
            }
        ];
        this.regulationMark = 'D';
        this.set = 'VIV';
        this.setNumber = '26';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Flareon';
        this.fullName = 'Flareon VIV';
    }
    reduceEffect(store, state, effect) {
        // Ability: Incandescent Awakening (passive - block Grass Pokemon abilities if Memory Capsule attached)
        // Ref: set-vivid-voltage/jolteon.ts (same pattern for Water Pokemon)
        if (effect instanceof game_effects_1.PowerEffect && effect.power !== this.powers[0]) {
            const targetCard = effect.card;
            // Only affects Grass-type Pokemon
            if (targetCard.cardType !== card_types_1.CardType.GRASS) {
                return state;
            }
            // Check if this Flareon is in play on either side and has Memory Capsule attached
            let flareonCardList = null;
            let flareonOwner = null;
            for (const player of state.players) {
                player.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (cardList.getPokemonCard() === this) {
                        flareonCardList = cardList;
                        flareonOwner = player;
                    }
                });
            }
            if (!flareonCardList || !flareonOwner) {
                return state;
            }
            // Check if Flareon has Memory Capsule attached
            const hasMemoryCapsule = flareonCardList.tools.some((tool) => tool.name === 'Memory Capsule');
            if (!hasMemoryCapsule) {
                return state;
            }
            if (effect.power.useFromDiscard || effect.power.useFromHand) {
                return state;
            }
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, flareonOwner, this)) {
                return state;
            }
            if (!effect.power.exemptFromAbilityLock) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
        }
        return state;
    }
}
exports.Flareon = Flareon;
