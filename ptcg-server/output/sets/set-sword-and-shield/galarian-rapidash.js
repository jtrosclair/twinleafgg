"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.GalarianRapidash = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const check_effects_1 = require("../../game/store/effects/check-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class GalarianRapidash extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_1;
        this.evolvesFrom = 'Galarian Ponyta';
        this.cardType = P;
        this.hp = 100;
        this.weakness = [{ type: D }];
        this.resistance = [{ type: F, value: -30 }];
        this.retreat = [C];
        this.powers = [{
                name: 'Pastel Veil',
                powerType: game_1.PowerType.ABILITY,
                text: 'Your Pokémon recover from all Special Conditions and can\'t be affected by any Special Conditions.'
            }];
        this.attacks = [
            {
                name: 'Psychic',
                cost: [P, C],
                damage: 30,
                damageCalculation: '+',
                text: 'This attack does 30 more damage for each Energy attached to your opponent\'s Active Pokémon.'
            }
        ];
        this.regulationMark = 'D';
        this.set = 'SSH';
        this.setNumber = '82';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Galarian Rapidash';
        this.fullName = 'Galarian Rapidash SSH';
    }
    reduceEffect(store, state, effect) {
        // Ability: Pastel Veil (passive - all your Pokemon immune to Special Conditions)
        // Ref: set-breakpoint/raticate.ts (PREVENT_AND_CLEAR_SPECIAL_CONDITIONS for owner's Pokemon)
        (0, prefabs_1.PREVENT_AND_CLEAR_SPECIAL_CONDITIONS)(state, effect, {
            shouldApply: (target) => {
                // Find owner of the target Pokemon slot
                let owner = null;
                state.players.forEach(p => {
                    p.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, cardList => {
                        if (cardList === target) {
                            owner = p;
                        }
                    });
                });
                if (!owner) {
                    return false;
                }
                // Check if this Galarian Rapidash is in play for the same owner
                let rapidashInPlay = false;
                owner.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList, card) => {
                    if (card === this) {
                        rapidashInPlay = true;
                    }
                });
                if (!rapidashInPlay) {
                    return false;
                }
                return !(0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, owner, this);
            }
        });
        // Attack 1: Psychic
        // Ref: set-plasma-blast/tropius.ts (Energy Press - CheckProvidedEnergyEffect to count enemy energy)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            const checkProvidedEnergyEffect = new check_effects_1.CheckProvidedEnergyEffect(opponent);
            store.reduceEffect(state, checkProvidedEnergyEffect);
            const energyCount = checkProvidedEnergyEffect.energyMap.reduce((left, p) => left + p.provides.length, 0);
            effect.damage += 30 * energyCount;
        }
        return state;
    }
}
exports.GalarianRapidash = GalarianRapidash;
