"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.SitrusBerry = void 0;
const trainer_card_1 = require("../../game/store/card/trainer-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
class SitrusBerry extends trainer_card_1.TrainerCard {
    constructor() {
        super(...arguments);
        this.trainerType = card_types_1.TrainerType.TOOL;
        this.regulationMark = 'D';
        this.set = 'SSH';
        this.setNumber = '182';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Sitrus Berry';
        this.fullName = 'Sitrus Berry SSH';
        this.text = 'Attach a Pokémon Tool to 1 of your Pokémon that doesn\'t already have a Pokémon Tool attached. At the end of each turn, if the Pokémon this card is attached to has 3 or more damage counters on it, heal 30 damage from it and discard this card. You may play any number of Item cards during your turn.';
    }
    reduceEffect(store, state, effect) {
        // Ref: set-unified-minds/giant-bomb.ts (tool EndTurnEffect pattern - check attachment + discard)
        if (effect instanceof game_phase_effects_1.EndTurnEffect) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            // Check both players' Pokemon for this tool attached
            for (const checkPlayer of [player, opponent]) {
                checkPlayer.forEachPokemon(game_1.PlayerType.BOTTOM_PLAYER, (cardList) => {
                    if (!cardList.tools.includes(this)) {
                        return;
                    }
                    // Check if tool is blocked
                    if ((0, prefabs_1.IS_TOOL_BLOCKED)(store, state, checkPlayer, this)) {
                        return;
                    }
                    // Damage counters: 1 damage counter = 10 damage
                    // 3 or more damage counters = 30+ damage
                    if (cardList.damage >= 30) {
                        // Heal 30 damage
                        cardList.damage = Math.max(0, cardList.damage - 30);
                        // Discard this tool
                        cardList.moveCardTo(this, checkPlayer.discard);
                    }
                });
            }
        }
        return state;
    }
}
exports.SitrusBerry = SitrusBerry;
