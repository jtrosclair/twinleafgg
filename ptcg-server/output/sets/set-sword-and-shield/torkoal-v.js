"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.TorkoalV = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const energy_card_1 = require("../../game/store/card/energy-card");
const card_list_1 = require("../../game/store/state/card-list");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const attack_effects_1 = require("../../game/store/prefabs/attack-effects");
class TorkoalV extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_V];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = R;
        this.hp = 210;
        this.weakness = [{ type: W }];
        this.retreat = [C, C, C, C];
        this.attacks = [
            {
                name: 'Combustion Pillar',
                cost: [R, R, C],
                damage: 90,
                damageCalculation: '+',
                text: 'Discard the top card of your deck. If that card is a [R] Energy card, this attack does 90 more damage.'
            },
            {
                name: 'Steam Crush',
                cost: [R, R, R, C],
                damage: 120,
                text: 'Discard 2 Energy from your opponent\'s Active Pokémon.'
            }
        ];
        this.regulationMark = 'D';
        this.set = 'SSH';
        this.setNumber = '24';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Torkoal V';
        this.fullName = 'Torkoal V SSH';
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Combustion Pillar
        // Ref: set-cosmic-eclipse/ns-resolve.ts (discard top cards, check if energy)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            if (player.deck.cards.length === 0) {
                return state;
            }
            // Take top card into a temporary list
            const topCard = new card_list_1.CardList();
            player.deck.moveTo(topCard, 1);
            // Check if it's a [R] Basic Energy card
            const isFireEnergy = topCard.cards.length > 0 &&
                topCard.cards[0] instanceof energy_card_1.EnergyCard &&
                topCard.cards[0].energyType === card_types_1.EnergyType.BASIC &&
                topCard.cards[0].provides.includes(card_types_1.CardType.FIRE);
            // Move the card to discard
            topCard.moveTo(player.discard);
            if (isFireEnergy) {
                effect.damage += 90;
            }
        }
        // Attack 2: Steam Crush
        // Ref: set-cosmic-eclipse/absol.ts (DiscardCardsEffect), set-cosmic-eclipse/torkoal.ts (Kindle)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const opponent = game_1.StateUtils.getOpponent(state, effect.player);
            const hasEnergy1 = opponent.active.cards.some(c => c.superType === card_types_1.SuperType.ENERGY);
            if (hasEnergy1) {
                (0, attack_effects_1.DISCARD_AN_ENERGY_FROM_OPPONENTS_ACTIVE_POKEMON)(store, state, effect);
            }
            const hasEnergy2 = opponent.active.cards.some(c => c.superType === card_types_1.SuperType.ENERGY);
            if (hasEnergy2) {
                (0, attack_effects_1.DISCARD_AN_ENERGY_FROM_OPPONENTS_ACTIVE_POKEMON)(store, state, effect);
            }
        }
        return state;
    }
}
exports.TorkoalV = TorkoalV;
