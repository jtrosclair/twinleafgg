"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.LaprasV = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const energy_card_1 = require("../../game/store/card/energy-card");
const game_phase_effects_1 = require("../../game/store/effects/game-phase-effects");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const choose_cards_prompt_1 = require("../../game/store/prompts/choose-cards-prompt");
const check_effects_1 = require("../../game/store/effects/check-effects");
class LaprasV extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.tags = [card_types_1.CardTag.POKEMON_V];
        this.stage = card_types_1.Stage.BASIC;
        this.cardType = W;
        this.hp = 210;
        this.weakness = [{ type: L }];
        this.retreat = [C, C];
        this.attacks = [
            {
                name: 'Body Surf',
                cost: [C],
                damage: 0,
                text: 'Attach a [W] Energy card from your hand to this Pokémon. If you do, switch it with 1 of your Benched Pokémon.'
            },
            {
                name: 'Ocean Loop',
                cost: [W, W, W, C],
                damage: 210,
                text: 'Put 2 [W] Energy attached to this Pokémon into your hand.'
            }
        ];
        this.regulationMark = 'D';
        this.set = 'SSH';
        this.setNumber = '49';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Lapras V';
        this.fullName = 'Lapras V SSH';
        this.bodySurfAttached = false;
    }
    reduceEffect(store, state, effect) {
        // Attack 1: Body Surf
        // Ref: set-dragons-majesty/reshiram-gx.ts (ChooseCardsPrompt from hand + attach to self)
        // Ref: AGENTS.md (AfterAttackEffect pattern for switching after energy attach)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const hasWaterInHand = player.hand.cards.some(c => c instanceof energy_card_1.EnergyCard && c.energyType === card_types_1.EnergyType.BASIC && c.provides.includes(card_types_1.CardType.WATER));
            if (!hasWaterInHand) {
                return state;
            }
            store.prompt(state, new choose_cards_prompt_1.ChooseCardsPrompt(player, game_1.GameMessage.CHOOSE_CARD_TO_ATTACH, player.hand, { superType: card_types_1.SuperType.ENERGY, energyType: card_types_1.EnergyType.BASIC, name: 'Water Energy' }, { min: 0, max: 1, allowCancel: false }), (cards) => {
                cards = cards || [];
                if (cards.length > 0) {
                    // Attach energy to Lapras V (this)
                    const cardList = game_1.StateUtils.findCardList(state, this);
                    if (cardList) {
                        player.hand.moveCardsTo(cards, cardList);
                    }
                    this.bodySurfAttached = true;
                }
            });
        }
        if (effect instanceof game_phase_effects_1.AfterAttackEffect && this.bodySurfAttached) {
            this.bodySurfAttached = false;
            const player = effect.player;
            if (player.bench.some(b => b.cards.length > 0)) {
                (0, prefabs_1.SWITCH_ACTIVE_WITH_BENCHED)(store, state, player);
            }
        }
        if (effect instanceof game_phase_effects_1.EndTurnEffect) {
            this.bodySurfAttached = false;
        }
        // Attack 2: Ocean Loop
        // Ref: set-hidden-fates/lycanroc-gx-2.ts (ChooseEnergyPrompt + move energy to hand)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 1, this)) {
            const player = effect.player;
            const checkEnergy = new check_effects_1.CheckProvidedEnergyEffect(player, player.active);
            state = store.reduceEffect(state, checkEnergy);
            const waterEnergyMap = checkEnergy.energyMap.filter(em => em.provides.includes(card_types_1.CardType.WATER) || em.provides.includes(card_types_1.CardType.ANY));
            if (waterEnergyMap.length === 0) {
                return state;
            }
            const count = Math.min(2, waterEnergyMap.length);
            store.prompt(state, new game_1.ChooseEnergyPrompt(player.id, game_1.GameMessage.CHOOSE_ENERGIES_TO_HAND, waterEnergyMap, [card_types_1.CardType.COLORLESS, card_types_1.CardType.COLORLESS], { allowCancel: false }), energy => {
                const cards = (energy || []).slice(0, count).map(e => e.card);
                if (cards.length > 0) {
                    player.active.moveCardsTo(cards, player.hand);
                }
            });
        }
        return state;
    }
}
exports.LaprasV = LaprasV;
