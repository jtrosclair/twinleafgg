"use strict";
// This file was auto-generated by the card stub generator.
// Card effects were implemented by an agent.
// If you have any questions or feedback, reach out to @C4 in the discord.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Orbeetle = void 0;
const pokemon_card_1 = require("../../game/store/card/pokemon-card");
const card_types_1 = require("../../game/store/card/card-types");
const game_1 = require("../../game");
const prefabs_1 = require("../../game/store/prefabs/prefabs");
const check_effects_1 = require("../../game/store/effects/check-effects");
const order_cards_prompt_1 = require("../../game/store/prompts/order-cards-prompt");
class Orbeetle extends pokemon_card_1.PokemonCard {
    constructor() {
        super(...arguments);
        this.stage = card_types_1.Stage.STAGE_2;
        this.evolvesFrom = 'Dottler';
        this.cardType = G;
        this.hp = 130;
        this.weakness = [{ type: R }];
        this.retreat = [C];
        this.powers = [{
                name: 'Bug\'s Radar',
                useWhenInPlay: true,
                powerType: game_1.PowerType.ABILITY,
                text: 'Once during your turn, you may look at the top 3 cards of your opponent\'s deck and put them back in any order.'
            }];
        this.attacks = [
            {
                name: 'Brainwave',
                cost: [G, C],
                damage: 90,
                damageCalculation: '+',
                text: 'This attack does 30 more damage for each [P] Energy attached to this Pokémon.'
            }
        ];
        this.regulationMark = 'D';
        this.set = 'SSH';
        this.setNumber = '19';
        this.cardImage = 'assets/cardback.png';
        this.name = 'Orbeetle';
        this.fullName = 'Orbeetle SSH';
        this.BUGS_RADAR_MARKER = 'ORBEETLE_SSH_BUGS_RADAR_MARKER';
    }
    reduceEffect(store, state, effect) {
        // Ability: Bug's Radar
        // Ref: set-furious-fists/gothorita.ts (OrderCardsPrompt for rearrange top cards of opponent's deck)
        if ((0, prefabs_1.WAS_POWER_USED)(effect, 0, this)) {
            const player = effect.player;
            const opponent = game_1.StateUtils.getOpponent(state, player);
            if ((0, prefabs_1.IS_ABILITY_BLOCKED)(store, state, player, this)) {
                throw new game_1.GameError(game_1.GameMessage.BLOCKED_BY_EFFECT);
            }
            if (opponent.deck.cards.length === 0) {
                throw new game_1.GameError(game_1.GameMessage.CANNOT_USE_POWER);
            }
            (0, prefabs_1.USE_ABILITY_ONCE_PER_TURN)(player, this.BUGS_RADAR_MARKER, this);
            (0, prefabs_1.ABILITY_USED)(player, this);
            const deckTop = new game_1.CardList();
            opponent.deck.moveTo(deckTop, Math.min(3, opponent.deck.cards.length));
            store.prompt(state, new order_cards_prompt_1.OrderCardsPrompt(player.id, game_1.GameMessage.CHOOSE_CARDS_ORDER, deckTop, { allowCancel: false }), order => {
                if (order !== null) {
                    deckTop.applyOrder(order);
                }
                deckTop.moveToTopOfDestination(opponent.deck);
            });
        }
        (0, prefabs_1.REMOVE_MARKER_AT_END_OF_TURN)(effect, this.BUGS_RADAR_MARKER, this);
        // Attack 1: Brainwave
        // Ref: set-pokemon-go/blastoise.ts (CheckProvidedEnergyEffect for energy type count)
        if ((0, prefabs_1.WAS_ATTACK_USED)(effect, 0, this)) {
            const player = effect.player;
            const checkEnergy = new check_effects_1.CheckProvidedEnergyEffect(player, player.active);
            store.reduceEffect(state, checkEnergy);
            const psychicCount = checkEnergy.energyMap.reduce((sum, em) => sum + em.provides.filter(t => t === card_types_1.CardType.PSYCHIC || t === card_types_1.CardType.ANY).length, 0);
            effect.damage += psychicCount * 30;
        }
        return state;
    }
}
exports.Orbeetle = Orbeetle;
